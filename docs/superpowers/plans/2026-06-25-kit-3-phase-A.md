# Кит-3 «Электрические цепи» — Фаза A (вертикальный срез) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Поднять пакет `kit-3-circuits` и довести опыт **3.1 «Измерение сопротивления резистора»** (R=U/I) до полного канона — доказать паттерн сборки цепи (гибрид «гнёзда + провода») и аналитической симуляции end-to-end.

**Architecture:** Новый workspace `experiments/kit-3-circuits` по образцу kit-2 (KitShell + Router + IScreen, журнал v2). Новая инфра: pure `CircuitModel` (аналитика по топологиям) + `CircuitAssembly` (слот-модель цепи + валидатор топологии + анимация тока) + `lab-circuit-board` (монтажная панель с гнёздами). Приборы `lab-*` строятся по шаблону `lab-dynamometer`. Экран «Измерения» — мульти-таск по образцу friction (в Фазе A только задача 3.1; 3.2/3.3 в Фазе B).

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components, Vitest (happy-dom), Playwright, npm workspaces.

**Спека:** [docs/superpowers/specs/2026-06-25-wave-1-kit-3-circuits.md](../specs/2026-06-25-wave-1-kit-3-circuits.md). **Эпик:** [2026-06-24-fipi-full-coverage-epic.md](../specs/2026-06-24-fipi-full-coverage-epic.md).

## Global Constraints

(Копия из эпика §5; verbatim-значения.)
- **ФИПИ-якорь** в docstring каждого `*Experiment.ts`/SPEC: дословная цитата СПЕЦ Прил.2 сноска 3 / КОДИФ §1.29 + страница.
- **Journal v2 (§21)**: `renderJournalTable(host, SPEC, rows, {mode,onCellInput,onVerify})` + `JournalSpec` в `_shared-spa/.../specs.ts` + `verifyRow` + `parseRu`; 3 режима через `record-mode` ключ `'kit-3'`.
- **Чистая физика** в `src/physics/`, Vitest ≥95% + property-фаззинг; `RangeError` на невалидных входах (паттерн `DensityCalc`/`IndependenceCalc`).
- **D&D через mouse** в selfcheck (`page.mouse.move/down/up`); REST-state скрыт в покое.
- **A11y**: axe 0; `aria-live`; `svg[hidden]{display:none}` в shadow CSS каждого `lab-*`.
- **Алиасы** kit-3: `@shared/*` = `../_shared-spa/src/*`, плюс `@labosfera/shared-spa/*`, `@screens/*`, `@shell/*`, `@physics/*`, `@ui/*`, `@controller/*`, `@/*` (как в kit-2 tsconfig/vite).
- **Гейты** (из корня): `npm run typecheck` · `test` · `lint` (0 err) · `build` — все `--workspaces`. Плюс `selfcheck-<id>.mjs` PASS + ручной reality-check скрином.
- **Платформа**: только `C:\dev\Inter_OGE`; dev с `--host 127.0.0.1` (иначе Vite биндит IPv6 и Playwright не достучится).
- **Единичные кавычки** (repo style); не коммитить `dist/`/`tsbuildinfo`/`package-lock` в фиче-коммиты (использовать явные пути, не `git add -A`).

---

## File Structure (Фаза A)

| Файл | Ответственность |
|---|---|
| `experiments/kit-3-circuits/package.json` · `vite.config.ts` · `tsconfig.json` · `index.html` · `playwright.config.ts` | scaffold пакета (копия kit-2 с переименованием) |
| корень `package.json` `workspaces[]` | добавить `experiments/kit-3-circuits` |
| `kit-3-circuits/src/shell/{IScreen,KitShell,Router}.ts` | шимы над `@shared/shell/*` с union `ScreenId` кит-3 |
| `kit-3-circuits/src/styles/{tokens,reset,components,kit-shell}.css` + `circuit-experiment.css` | стили (копия kit-2 + стиль экрана) |
| `kit-3-circuits/src/ui/components/lab-{kit-nav,kit-header,equipment-card}.ts` | копия из kit-2 (shell-компоненты) |
| `kit-3-circuits/src/ui/components/lab-{power-source,voltmeter,ammeter,resistor,key,wire}.ts` | НОВЫЕ приборы (по шаблону lab-dynamometer) |
| `kit-3-circuits/src/ui/components/lab-circuit-board.ts` | НОВАЯ монтажная панель (скелет + гнёзда) |
| `kit-3-circuits/src/physics/circuit/CircuitModel.ts` (+ `__tests__/`) | pure-физика одиночного R |
| `kit-3-circuits/src/controller/CircuitAssembly.ts` (+ `__tests__/`) | слот-модель + валидатор топологии + ток-анимация |
| `_shared-spa/src/lib/journal/types.ts` | union `experimentId` += '3.1'..'3.9'; `kitId` += 'kit-3' |
| `_shared-spa/src/lib/journal/specs.ts` | `RESISTANCE_SPEC` (+ в ALL_SPECS) |
| `kit-3-circuits/src/screens/measurements/{MeasurementsScreen,MeasurementsExperiment}.ts` + `template.html` | экран опыта 3.1 |
| `kit-3-circuits/src/main.ts` | регистрация экрана + bootstrap |
| `kit-3-circuits/selfcheck-3-1.mjs` | self-check опыта 3.1 |
| `experiments/home/src/data/kits.ts` (+ test) | kit-3 status ready; опыт 3.1 done |

---

## Task 1: Scaffold пакета kit-3-circuits

**Files:** create the package skeleton; modify root `package.json`.

**Interfaces:**
- Produces: workspace `@labosfera/kit-3-circuits` со скриптами dev/build/test/test:e2e/typecheck/lint; алиасы как в kit-2; shell-шимы `IScreen`/`KitShell`/`Router`; `ScreenId = 'measurements' | 'iv-curve' | 'wire-resistance' | 'connections'`, `IconId = 'gauge' | 'iv' | 'wire' | 'link'`.

- [ ] **Step 1: Создать каталог и package.json**

Create `experiments/kit-3-circuits/package.json` (копия kit-2 с заменой имени/описания):
```json
{
  "name": "@labosfera/kit-3-circuits",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Виртуальный комплект №3 «Электрические цепи» — серия опытов по электричеству для подготовки к ОГЭ по физике (ФИПИ-2026), один SPA с PhET-style экранами. ООО ЛАБОСФЕРА",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write \"src/**/*.{ts,css,html,json,md}\"",
    "format:check": "prettier --check \"src/**/*.{ts,css,html,json,md}\""
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "happy-dom": "^15.11.7",
    "prettier": "^3.4.2",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.18.0",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
```

- [ ] **Step 2: vite.config.ts + tsconfig.json**

Create `experiments/kit-3-circuits/vite.config.ts` = копия kit-2 vite.config (verbatim) с заменой `manualChunks` на kit-3 экраны:
```ts
manualChunks: {
  'screen-measurements': ['./src/screens/measurements/MeasurementsScreen.ts'],
},
```
(остальное — base './', те же alias-блоки `@/@physics/@controller/@ui/@shell/@screens/@shared/@labosfera/shared-spa`, server.fs.allow ['..'], build target es2022 sourcemap, test happy-dom + coverage thresholds 80/75/80/80 — verbatim из kit-2).
Create `experiments/kit-3-circuits/tsconfig.json` = verbatim копия kit-2 tsconfig (paths-алиасы те же; `include: ["src","../_shared-spa/src"]`).

- [ ] **Step 3: index.html + playwright.config.ts**

Create `experiments/kit-3-circuits/index.html` = копия kit-2 index.html с заменой title/description и стартовых атрибутов:
```html
<title>Комплект №3 — Электрические цепи — ЛАБОСФЕРА</title>
...
<lab-kit-header id="kit-header" experiment-kicker="Опыт 3.1" experiment="Сопротивление резистора"></lab-kit-header>
<main id="screen-content" class="screen-content" role="main" aria-live="polite"></main>
<lab-kit-nav id="kit-nav" active="measurements"></lab-kit-nav>
```
Create `experiments/kit-3-circuits/playwright.config.ts` = копия kit-2 (verbatim) с заменой порта `4186` → `4187` (изолировать от kit-2): `baseURL: 'http://localhost:4187'`, `webServer.command: 'npm run preview -- --port 4187'`, `webServer.url: 'http://localhost:4187'`.

- [ ] **Step 4: Зарегистрировать workspace в корне**

Modify `C:\dev\Inter_OGE\package.json` — добавить в `workspaces` строку `"experiments/kit-3-circuits"` (после kit-2-forces). Затем из Git Bash: `npm install` (линкует новый workspace). Скопировать `.eslintrc`/eslint flat config из kit-2 если он локальный (проверить `experiments/kit-2-forces/eslint.config.*`; если есть — скопировать в kit-3).

- [ ] **Step 5: Shell-шимы**

Create `experiments/kit-3-circuits/src/shell/IScreen.ts`:
```ts
import type { IScreen as SharedIScreen, ScreenMeta as SharedScreenMeta } from '@shared/shell/IScreen';
export type ScreenId = 'measurements' | 'iv-curve' | 'wire-resistance' | 'connections';
export type IconId = 'gauge' | 'iv' | 'wire' | 'link';
export type IScreen = SharedIScreen<ScreenId, IconId>;
export type ScreenMeta = SharedScreenMeta<ScreenId, IconId>;
```
Create `experiments/kit-3-circuits/src/shell/KitShell.ts` (копия kit-2 шима, prefix `'kit-3-circuits:screen'`):
```ts
import { KitShell as SharedKitShell } from '@shared/shell/KitShell';
import type { IScreen as SharedIScreen } from '@shared/shell/IScreen';
import type { IconId, IScreen, ScreenId } from './IScreen';
const STORAGE_PREFIX = 'kit-3-circuits:screen';
export class KitShell extends SharedKitShell<ScreenId> {
  constructor(host: HTMLElement, screens: ReadonlyArray<IScreen>, defaultId: ScreenId) {
    super(host, screens as ReadonlyArray<SharedIScreen<ScreenId, IconId>>, defaultId, STORAGE_PREFIX);
  }
}
```
Create `experiments/kit-3-circuits/src/shell/Router.ts`: `export { Router } from '@shared/shell/Router';`

- [ ] **Step 6: Стили + shell-компоненты + main.ts заглушка**

Скопировать из kit-2 в kit-3 (verbatim): `src/styles/{tokens,reset,components,kit-shell}.css`; `src/ui/components/lab-kit-nav.ts`, `lab-kit-header.ts`, `lab-equipment-card.ts`, `lab-graph.ts`. Create `src/styles/circuit-experiment.css` (пустой каркас — заполнится в Task 6). Create `src/main.ts` минимальный bootstrap по образцу kit-2 main.ts (импорт стилей + shell-компонентов; `const screens: IScreen[] = []` пока пуст — экран добавится в Task 6; KitShell с defaultId `'measurements'`; роль-бейдж блок можно опустить в Фазе A). Чтобы dev не падал на пустом screens[], временно зарегистрировать заглушку-экран ИЛИ добавить экран в Task 6 и тут оставить `main.ts` импортирующим будущий `MeasurementsScreen` (создать файл в Task 6 — тогда Task 1 Step 6 main.ts закомментировать регистрацию до Task 6). **Решение:** в Task 1 main.ts регистрирует пустой массив + рендерит заглушку «Скоро»; Task 6 заменит на реальный экран.

- [ ] **Step 7: Гейты scaffold + commit**

Run: `npm run typecheck -w experiments/kit-3-circuits` → PASS; `npm run build -w experiments/kit-3-circuits` → PASS; `npm run dev -w experiments/kit-3-circuits -- --host 127.0.0.1 --port 5196 --strictPort` → curl 200 (шелл рендерит шапку+нав).
Commit (явные пути): `git add experiments/kit-3-circuits package.json package-lock.json && git commit -m "feat(kit-3): scaffold пакета Электрические цепи (shell, стили, конфиги)"`.
(package-lock коммитим ТОЛЬКО здесь — добавление workspace его меняет легитимно.)

---

## Task 2: CircuitModel — физика одиночного резистора

**Files:** Create `kit-3-circuits/src/physics/circuit/CircuitModel.ts` + `__tests__/CircuitModel.test.ts`.

**Interfaces:**
- Produces: `G`-нет; экспортирует `current(U,R)`, `resistance(U,I)`, `power(U,I)`, `workOfCurrent(U,I,t)`, константы пределов приборов; все бросают `RangeError` на невалидных входах.

- [ ] **Step 1: Тест физики**

Create `kit-3-circuits/src/physics/circuit/__tests__/CircuitModel.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { current, resistance, power, workOfCurrent } from '../CircuitModel';

describe('resistance R=U/I', () => {
  it('R1: U=1.5, I=0.32 → ≈4.69 Ом', () => {
    expect(resistance(1.5, 0.32)).toBeCloseTo(4.6875, 3);
  });
  it('бросает при I<=0 / NaN', () => {
    expect(() => resistance(1.5, 0)).toThrow(RangeError);
    expect(() => resistance(1.5, -1)).toThrow(RangeError);
    expect(() => resistance(NaN, 0.3)).toThrow(RangeError);
  });
});
describe('current I=U/R', () => {
  it('U=3, R=4.7 → ≈0.638 А', () => { expect(current(3, 4.7)).toBeCloseTo(0.6383, 3); });
  it('бросает при R<=0', () => { expect(() => current(3, 0)).toThrow(RangeError); });
});
describe('power P=U*I', () => {
  it('U=5.7, I=0.70 → ≈3.99 Вт', () => { expect(power(5.7, 0.70)).toBeCloseTo(3.99, 2); });
  it('бросает при отрицательных', () => { expect(() => power(-1, 0.5)).toThrow(RangeError); });
});
describe('workOfCurrent A=U*I*t', () => {
  it('U=2.9, I=0.51, t=60 → ≈88.7 Дж', () => { expect(workOfCurrent(2.9, 0.51, 60)).toBeCloseTo(88.74, 2); });
  it('бросает при t<=0', () => { expect(() => workOfCurrent(3, 0.5, 0)).toThrow(RangeError); });
});
```

- [ ] **Step 2: Запустить — FAIL** (`npm run test -w experiments/kit-3-circuits -- CircuitModel`).

- [ ] **Step 3: Реализовать**

Create `kit-3-circuits/src/physics/circuit/CircuitModel.ts`:
```ts
/**
 * Кит-3 «Электрические цепи» — pure-физика.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (стр.18) + КОДИФ §1.29:
 * измерение сопротивления резистора, мощности и работы тока — метод амперметра-вольтметра.
 * Аналитика по топологиям опытов (не общий нодальный солвер).
 */
function guard(...xs: number[]): void {
  for (const x of xs) if (!Number.isFinite(x)) throw new RangeError(`non-finite arg: ${x}`);
}
/** Сила тока I=U/R [А]. */
export function current(U_V: number, R_Ohm: number): number {
  guard(U_V, R_Ohm);
  if (R_Ohm <= 0) throw new RangeError(`R must be > 0, got ${R_Ohm}`);
  return U_V / R_Ohm;
}
/** Сопротивление R=U/I [Ом]. */
export function resistance(U_V: number, I_A: number): number {
  guard(U_V, I_A);
  if (I_A <= 0) throw new RangeError(`I must be > 0, got ${I_A}`);
  return U_V / I_A;
}
/** Мощность P=U·I [Вт]. */
export function power(U_V: number, I_A: number): number {
  guard(U_V, I_A);
  if (U_V < 0 || I_A < 0) throw new RangeError('U,I must be >= 0');
  return U_V * I_A;
}
/** Работа тока A=U·I·t [Дж]. */
export function workOfCurrent(U_V: number, I_A: number, t_s: number): number {
  guard(U_V, I_A, t_s);
  if (U_V < 0 || I_A < 0) throw new RangeError('U,I must be >= 0');
  if (t_s <= 0) throw new RangeError(`t must be > 0, got ${t_s}`);
  return U_V * I_A * t_s;
}
```

- [ ] **Step 4: Тест PASS** (`npm run test -w experiments/kit-3-circuits -- CircuitModel`).

- [ ] **Step 5: Property-фаззинг**

Add to the test file ≥12 invariant categories over random valid inputs (seeded by index, no Math.random): R=U/I>0; I=U/R>0; power=U·I≥0; round-trip current(resistance(U,I)? )≈; monotonic I↑ when U↑ at fixed R; monotonic I↓ when R↑ at fixed U; workOfCurrent = power·t; etc. ~50k iterations via nested loops over discretized ranges. Run → PASS.

- [ ] **Step 6: Commit** — `git add experiments/kit-3-circuits/src/physics && git commit -m "feat(kit-3): CircuitModel — R/I/P/работа одиночного резистора + фаззинг"`.

---

## Task 3: Приборы lab-* (источник, вольтметр, амперметр, резистор, ключ)

**Files:** Create `kit-3-circuits/src/ui/components/lab-{power-source,voltmeter,ammeter,resistor,key}.ts` + a smoke test `src/ui/components/__tests__/components.test.ts`.

**Interfaces:**
- Produces: web-components с реалистичным рендером (шаблон = `kit-2/src/ui/components/lab-dynamometer.ts`: `attachShadow({mode:'open'})`, `static observedAttributes`, `attributeChangedCallback`, shadow `<style>` с `svg[hidden]{display:none}`). Контракты атрибутов и геометрический API клемм ниже.

**Контракты (точные):**
- `lab-power-source`: attrs `voltage` (1.5–7.5), `on` (bool). Метод `getTerminalPositions(): {plus:{x,y}, minus:{x,y}}`. Событие `voltage-change {detail:{voltage}}` при кручении ручки (interactive).
- `lab-voltmeter`: attrs `range` ('3'|'6'), `value` (V). Двойная шкала + стрелка/LCD. `getTerminalPositions()`.
- `lab-ammeter`: attrs `range` ('3'|'0.6'), `value` (A). `getTerminalPositions()`.
- `lab-resistor`: attrs `variant` ('R1'|'R2'|'R3'), `resistance` (Ом, из variant). `getTerminalPositions()`.
- `lab-key`: attr `closed` (bool). Событие `toggle {detail:{closed}}` по клику. `getTerminalPositions()`.

- [ ] **Step 1: Smoke-тест компонентов**

Create `src/ui/components/__tests__/components.test.ts`:
```ts
import { describe, expect, it, beforeAll } from 'vitest';
import '../lab-power-source';
import '../lab-voltmeter';
import '../lab-ammeter';
import '../lab-resistor';
import '../lab-key';

describe('circuit lab-* components register & render', () => {
  for (const tag of ['lab-power-source','lab-voltmeter','lab-ammeter','lab-resistor','lab-key']) {
    it(`${tag} монтируется и имеет shadowRoot с svg`, () => {
      const el = document.createElement(tag);
      document.body.appendChild(el);
      expect(el.shadowRoot).not.toBeNull();
      expect(el.shadowRoot!.querySelector('svg')).not.toBeNull();
      el.remove();
    });
  }
  it('lab-resistor variant=R1 → resistance≈4.7', () => {
    const el = document.createElement('lab-resistor') as any;
    el.setAttribute('variant', 'R1');
    document.body.appendChild(el);
    expect(el.resistance).toBeCloseTo(4.7, 1);
    el.remove();
  });
  it('lab-key click → toggle event', () => {
    const el = document.createElement('lab-key') as any;
    document.body.appendChild(el);
    let fired = false;
    el.addEventListener('toggle', () => { fired = true; });
    el.shadowRoot.querySelector('svg').dispatchEvent(new MouseEvent('click', { bubbles: true }));
    expect(fired).toBe(true);
    el.remove();
  });
});
```

- [ ] **Step 2: Запустить — FAIL.**

- [ ] **Step 3: Реализовать 5 компонентов** по шаблону `lab-dynamometer.ts` (структура: модуль-`template` с shadow `<style>` incl `svg[hidden]{display:none}`, класс extends HTMLElement, `attachShadow`, `observedAttributes`, `attributeChangedCallback` → `#update()`, геттеры, `getTerminalPositions()` через `#svgToHost`). Реалистичные приборы: вольт/амперметр — корпус + двойная шкала + стрелка + LCD-плашка; источник — корпус + ручка напряжения + клеммы +/−; резистор — цилиндр с цветовыми кольцами + 2 вывода; ключ — рубильник (открыт/закрыт). Цвета/градиенты — из дизайн-токенов как у lab-dynamometer. `lab-resistor` resistance из variant: R1=4.7, R2=5.7, R3=8.2.

- [ ] **Step 4: Тест PASS.** Run `npm run test -w experiments/kit-3-circuits -- components`.

- [ ] **Step 5: typecheck + commit** — `npm run typecheck -w experiments/kit-3-circuits`; `git add experiments/kit-3-circuits/src/ui && git commit -m "feat(kit-3): приборы lab-* источник/вольтметр/амперметр/резистор/ключ"`.

---

## Task 4: lab-circuit-board + CircuitAssembly (сборка цепи)

**Files:** Create `kit-3-circuits/src/ui/components/lab-circuit-board.ts`; `kit-3-circuits/src/controller/CircuitAssembly.ts` + `__tests__/CircuitAssembly.test.ts`.

**Interfaces:**
- Produces: `<lab-circuit-board>` рисует скелет цепи с подписанными гнёздами (`data-slot="<id>"`, тип принимаемого прибора, признак series/parallel) + рельсы-провода; метод `getSlotRect(id): DOMRect`, `setCurrentAnimating(on: boolean)` (стрелки тока по рельсам).
- `CircuitAssembly` (pure-логика + DOM-glue): модель `{slots: Map<slotId, {accepts, role:'series'|'parallel'|'source'|'key', filledBy: eqId|null}>}`; `place(slotId, eqId)`, `remove(slotId)`, `validateTopology(): {ok:boolean, errors:string[]}` (амперметр в series-слоте, вольтметр в parallel-слоте, источник+ключ обязательны), `isComplete()`. Чистое ядро (без DOM) тестируется отдельно.

- [ ] **Step 1: Тест чистого ядра CircuitAssembly**

Create `src/controller/__tests__/CircuitAssembly.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { CircuitTopology } from '../CircuitAssembly';

// Топология опыта 3.1: source, key, ammeter(series), resistor(series), voltmeter(parallel∥resistor)
const SLOTS_3_1 = [
  { id: 'source', role: 'source', accepts: ['power-source'] },
  { id: 'key', role: 'key', accepts: ['key'] },
  { id: 'ammeter', role: 'series', accepts: ['ammeter'] },
  { id: 'resistor', role: 'series', accepts: ['resistor'] },
  { id: 'voltmeter', role: 'parallel', accepts: ['voltmeter'] },
] as const;

describe('CircuitTopology', () => {
  it('пустая → не complete, не ok', () => {
    const t = new CircuitTopology(SLOTS_3_1);
    expect(t.isComplete()).toBe(false);
    expect(t.validate().ok).toBe(false);
  });
  it('правильная схема → ok', () => {
    const t = new CircuitTopology(SLOTS_3_1);
    t.place('source','power-source'); t.place('key','key');
    t.place('ammeter','ammeter'); t.place('resistor','resistor'); t.place('voltmeter','voltmeter');
    expect(t.isComplete()).toBe(true);
    expect(t.validate().ok).toBe(true);
  });
  it('амперметр в parallel-слот не принимается (accepts)', () => {
    const t = new CircuitTopology(SLOTS_3_1);
    expect(t.place('voltmeter','ammeter')).toBe(false); // accepts mismatch
  });
  it('remove освобождает слот', () => {
    const t = new CircuitTopology(SLOTS_3_1);
    t.place('resistor','resistor'); t.remove('resistor');
    expect(t.slotFilledBy('resistor')).toBeNull();
  });
});
```

- [ ] **Step 2: Запустить — FAIL.**

- [ ] **Step 3: Реализовать `CircuitTopology` (pure) + `CircuitAssembly` (DOM-glue) + `lab-circuit-board`.**
`CircuitTopology` — чистый класс по тесту (Map слотов, place с проверкой accepts, remove, validate по role, isComplete). `CircuitAssembly` связывает `DragController` snap-zones (гнёзда из `lab-circuit-board`) с `CircuitTopology` + дёргает `board.setCurrentAnimating(topology.validate().ok && keyClosed)`. `lab-circuit-board` — SVG монтажная панель: рельсы + прямоугольные гнёзда с подписями (`data-slot`, `data-dropzone`), `getSlotRect`, `setCurrentAnimating` (анимация стрелок по рельсам через CSS/SMIL, отключается при prefers-reduced-motion).

- [ ] **Step 4: Тест PASS** (`npm run test -w experiments/kit-3-circuits -- CircuitAssembly`).

- [ ] **Step 5: typecheck + commit** — `git add experiments/kit-3-circuits/src/controller experiments/kit-3-circuits/src/ui/components/lab-circuit-board.ts && git commit -m "feat(kit-3): lab-circuit-board + CircuitAssembly (слоты, валидатор топологии, ток)"`.

---

## Task 5: RESISTANCE_SPEC + расширение union журнала

**Files:** Modify `_shared-spa/src/lib/journal/types.ts`; `_shared-spa/src/lib/journal/specs.ts` + `__tests__/resistance-spec.test.ts`.

**Interfaces:**
- Produces: `RESISTANCE_SPEC` (experimentId '3.1', kitId 'kit-3'); union `experimentId` включает '3.1'..'3.9'; `kitId` включает 'kit-3'.

- [ ] **Step 1: Расширить типы** в `_shared-spa/src/lib/journal/types.ts`:
```ts
readonly experimentId:
  | '1.1' | '1.2' | '1.3' | '1.4' | '1.5'
  | '2.1' | '2.2' | '2.3' | '2.4' | '2.5' | '2.6' | 'bonus-spring-work'
  | '3.1' | '3.2' | '3.3' | '3.4' | '3.5' | '3.6' | '3.7' | '3.8' | '3.9';
readonly kitId: 'kit-1' | 'kit-2' | 'kit-3';
```
(сохранить уже существующие литералы — свериться с текущим файлом перед правкой.)

- [ ] **Step 2: Тест SPEC**

Create `_shared-spa/__tests__/resistance-spec.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { RESISTANCE_SPEC } from '../src/lib/journal/specs';
import { verifyRow } from '../src/lib/journal/verify';

describe('RESISTANCE_SPEC', () => {
  it('R derived = U/I (U=1.5, I=0.32 → ≈4.69)', () => {
    const col = RESISTANCE_SPEC.columns.find(c => c.key === 'R_Ohm')!;
    expect(col.expectedFromRow!({ U_V: 1.5, I_A: 0.32 })).toBeCloseTo(4.6875, 3);
  });
  it('verifyRow: 4.7 при U=1.5 I=0.32 → ok', () => {
    const v = verifyRow(RESISTANCE_SPEC.columns, { idx:1, timestamp:1, values:{ U_V:1.5, I_A:0.32, R_Ohm:4.7 } });
    expect(v.R_Ohm).toBe('ok');
  });
});
```

- [ ] **Step 3: Запустить — FAIL.**

- [ ] **Step 4: Реализовать SPEC** в `_shared-spa/src/lib/journal/specs.ts` (+ в ALL_SPECS):
```ts
export const RESISTANCE_SPEC: JournalSpec = {
  experimentId: '3.1',
  kitId: 'kit-3',
  columns: [
    { key: 'idx', label: '№', source: 'meta', format: 'int' },
    { key: 'resistor', label: 'Резистор', source: 'meta' },
    { key: 'U_V', label: 'U, В', source: 'direct', unit: 'В', format: 'fixed2' },
    { key: 'I_A', label: 'I, А', source: 'direct', unit: 'А', format: 'fixed2' },
    {
      key: 'R_Ohm', label: 'R, Ом', source: 'derived', unit: 'Ом', format: 'fixed2',
      tolerance: 0.10, // ФИПИ-интервал R1=4.2–5.2 ≈ ±10%
      expectedFromRow: (row) => { const I = row.I_A ?? 0; return I > 0 ? (row.U_V ?? 0) / I : 0; },
    },
  ],
};
```

- [ ] **Step 5: Тесты PASS + typecheck _shared-spa** (`npm run test -w experiments/_shared-spa -- resistance-spec`; `npm run typecheck -w experiments/_shared-spa`).

- [ ] **Step 6: Commit** — `git add experiments/_shared-spa/src/lib/journal experiments/_shared-spa/__tests__/resistance-spec.test.ts && git commit -m "feat(journal): RESISTANCE_SPEC + union experimentId/kitId для kit-3"`.

---

## Task 6: Экран «Измерения» опыт 3.1 end-to-end

**Files:** Create `kit-3-circuits/src/screens/measurements/{MeasurementsScreen.ts, MeasurementsExperiment.ts, template.html}`; modify `src/main.ts`, `src/styles/circuit-experiment.css`; create `selfcheck-3-1.mjs`; modify `experiments/home/src/data/kits.ts` (+ test).

**Interfaces:**
- Consumes: `CircuitModel`, `CircuitAssembly`/`CircuitTopology`, `lab-*` приборы, `lab-circuit-board`, `RESISTANCE_SPEC`, `DragController`, журнал v2/record-mode.
- Produces: экран `MeasurementsScreen` (meta.id 'measurements', kicker 'Опыт 3.1', label 'Сопротивление резистора').

- [ ] **Step 1: template.html** — рабочая зона (`lab-circuit-board` со скелетом опыта 3.1 + гнёзда) + правая панель оборудования (`lab-equipment-card` с приборами, draggable) + обязательные слоты журнала v2 (`#record-mode-slot`, `#journal-host`, `#record-pending-slot`+`#record-pending-btn`+`#record-pending-summary`, `#live-region[aria-live]`, `#reset-btn`, шаги/hint-bar). Структуру слотов скопировать из density-solid/friction template (обязательные id журнала — точно те же).

- [ ] **Step 2: State-machine тест экрана**

Create `src/screens/measurements/__tests__/measurements.test.ts` по образцу friction-теста: mount в `document.body`; проверить наличие `#journal-host`/`#record-mode-slot`/`#live-region`; после программной сборки цепи (place всех приборов) + замыкания ключа + установки U → строка журнала с U,I и derived R; вердикт ok при вводе корректного R. (Через публичные методы Experiment, не через мышь — мышь в selfcheck.)

- [ ] **Step 3: MeasurementsScreen.ts + MeasurementsExperiment.ts** — фасад `IScreen` (meta + mount/unmount/saveState/loadState/reset) по образцу `FrictionScreen`; оркестратор по образцу `FrictionExperiment` + `DensityExperiment`: `Store` (state: размещённые приборы, keyClosed, voltage, измерения), `CircuitAssembly` (snap-zones гнёзд ↔ `CircuitTopology`), при `validate().ok && keyClosed` — считать I=current(U, R_резистора) через `CircuitModel`, оживить приборы (voltmeter.value=U, ammeter.value=I, board.setCurrentAnimating(true)); запись измерения → строка журнала; `renderJournalTable(host, RESISTANCE_SPEC, rows, {mode,onCellInput,onVerify})` (паттерн friction §5); record-mode ключ `'kit-3'`; ФИПИ-якорь docstring (цитата СПЕЦ Прил.2 сноска 3 + методичка 2.2.7). `reset()` чистит `#journalDrafts`/`#journalVerdicts`/state; `destroy()` снимает record-mode toggle + DragController.

- [ ] **Step 4: Зарегистрировать экран в main.ts** — заменить заглушку: `import { MeasurementsScreen } from '@screens/measurements/MeasurementsScreen'`; `const screens = [new MeasurementsScreen()]`; KitShell defaultId 'measurements'; импорт circuit-experiment.css + регистрация circuit lab-* компонентов (как kit-2 main.ts регистрирует приборы). Заполнить `circuit-experiment.css` стилями экрана (рабочая зона/панель/гнёзда; по образцу friction-experiment.css).

- [ ] **Step 5: State-machine тест PASS + typecheck + build** (`npm run test -w experiments/kit-3-circuits`; `npm run typecheck -w experiments/kit-3-circuits`; `npm run build -w experiments/kit-3-circuits`).

- [ ] **Step 6: selfcheck-3-1.mjs** (шаблон PLAYBOOK Шаг 7): dev на 127.0.0.1, `?screen=measurements`, через `page.mouse.move/down/up` собрать цепь (приборы в гнёзда), замкнуть ключ, выставить U; multi-state скрины; REST-state (гнёзда-подсветка скрыты в покое); overlay-dup=0; 3 режима записи; assert ФИПИ-инвариант: R в журнале ≈ 4,7 (4,2–5,2). Run `node experiments/kit-3-circuits/selfcheck-3-1.mjs` после dev → PASS (если браузер env-blocked — написать файл, отметить, контроллер reality-check'нет).

- [ ] **Step 7: Каталог** — в `experiments/home/src/data/kits.ts`: kit-3 `status: 'ready'`; опыт с `id: 'measurements'` (3.1) `done: true`. Обновить `experiments/home/src/data/__tests__/kits.test.ts` если есть пин на kit-3 (kitFipiProgress(kit-3).done → 1). Run `npm run test -w experiments/home -- kits.test` → PASS.
> ВАЖНО: kit-3 опыты 3.1/3.2/3.3 будут жить в одном экране `measurements` — в каталоге их `id` = `'measurements'` (как friction ×4). Слаг экрана = `'measurements'`, `experimentUrl` даст `?screen=measurements`.

- [ ] **Step 8: Гейты монорепо + commit**

Run (корень): `npm run typecheck && npm run test && npm run lint && npm run build` → зелёные.
Commit: `git add experiments/kit-3-circuits/src experiments/kit-3-circuits/selfcheck-3-1.mjs experiments/home/src/data && git commit -m "feat(kit-3): опыт 3.1 «сопротивление резистора» end-to-end (вертикальный срез)"`.

---

## Self-Review (Фаза A) — выполнено автором

**Покрытие спеки (Фаза A scope):** пакет (Task 1) ✔; CircuitModel одиночный R (Task 2) ✔; приборы lab-* (Task 3) ✔; сборка цепи lab-circuit-board+CircuitAssembly (Task 4) ✔; журнал RESISTANCE_SPEC+union (Task 5) ✔; экран 3.1 end-to-end+каталог (Task 6) ✔. Опыты 3.2–3.9 — вне Фазы A (outline ниже).
**Плейсхолдеры:** код дан для всей новой логики (scaffold, CircuitModel, CircuitTopology, RESISTANCE_SPEC, тесты); приборы/экран — контракты + точные шаги + эталоны (lab-dynamometer, friction) для боилерплейта (канон PLAYBOOK Шаг 4-6), не «similar to».
**Типы:** ScreenId 'measurements' консистентен (shell/main/каталог id); experimentId '3.1'..'3.9' добавлены ДО использования RESISTANCE_SPEC '3.1'; `resistance`/`current`/`power`/`workOfCurrent` сигнатуры едины.
**Развилка main.ts (Task 1 Step 6 заглушка → Task 6 реальный экран):** разрешена — Task 1 рендерит «Скоро», Task 6 заменяет; каждый коммит компилируется.

---

## Outline Фаз B–F (детализирую после приземления Фазы A)

- **Фаза B — опыты 3.2 (мощность, P=UI, R3, U=6В) + 3.3 (работа тока, A=UIt, R2, t=60с)** на экране `measurements`: добавить task-switcher (A-resistance/B-power/C-work) по образцу friction; `POWER_SPEC`, `WORK_CURRENT_SPEC`; секундомер для 3.3; per-task валидация U (3В/6В). Каталог 3.2/3.3 done.
- **Фаза C — опыт 3.4 ВАХ** I(U): экран `iv-curve`; свип напряжения; `lab-lamp` (нелинейная R(T)); график I–U (резистор линейный / лампа вогнутая) через `lab-graph`; `IV_CURVE_SPEC`.
- **Фаза D — опыты 3.5/3.6/3.7** R(длина/сечение/ρ): экран `wire-resistance`; проволочные резисторы (варианты l/S/материал); `R_LENGTH_SPEC`/`R_AREA_SPEC`/`R_RHO_SPEC`; R=ρl/S в CircuitModel.
- **Фаза E — опыты 3.8/3.9** соединения: экран `connections`; топологии «последовательно» (U=ΣU, правило напряжений) и «параллельно» (I=ΣI, правило токов); 2 резистора + лампочка; `SERIES_VOLTAGE_SPEC`/`PARALLEL_CURRENT_SPEC`; расширить `CircuitModel`/`CircuitTopology` на 2-элементные топологии.
- **Фаза F — финал**: каталог kit-3 9/9; постер kit-3 в цвет (vector через gen-instruments); REFERENCE §30 строка kit-3; полные гейты + e2e + reality-check всех 4 экранов.

## Execution Handoff
План Фазы A сохранён. Исполнение — **subagent-driven-development** (рекомендовано), агент экранов/инфры — `inter-oge-experiment`.

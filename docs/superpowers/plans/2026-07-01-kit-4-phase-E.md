# Кит-4 «Оптика» Фаза E — преломление (опыты 4.3 + 4.6) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать опыты ФИПИ 4.3 «показатель преломления стекла» (n = sin i / sin r) и 4.6 «зависимость угла преломления от угла падения» (r(i)) как новый экран `refraction` в пакете `experiments/kit-4-optics`, мульти-таск A/B, в полном каноне REFERENCE §13/§21/§27/§30.

**Architecture:** Новый экран `refraction` зеркалит `lens-bench`: `RefractionScreen` (IScreen-фасад) → `RefractionExperiment` (оркестратор: Store + drag + журнал v2 + result-panel + graph) + новый SVG-компонент `lab-protractor-disc` (круговой транспортир + полуцилиндр n≈1,5 + падающий/преломлённый/отражённый лучи + перетаскиваемый хэндл угла) + чистая модель `RefractionModel` (Снелл) + два журнал-спека в `_shared-spa`. Реиспользуется `OpticalDragController` (generic) и `lab-graph` (для r(i) и линеаризации sin r–sin i).

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components (Shadow DOM), npm workspaces (`@labosfera/kit-4-optics`, `@labosfera/shared-spa`, `labosfera-home`), Vitest (happy-dom) + property-фаззинг, Playwright + @axe-core (selfcheck).

## Global Constraints

Действуют на КАЖДУЮ задачу (из спеки волны-2 §8, значения дословно):

1. **ФИПИ-якорь** — docstring `RefractionModel.ts` и `*Experiment.ts` содержит дословную цитату сноски (4), стр.19: «…показателя преломления стекла; … зависимости угла преломления от угла падения на границе воздух – стекло.» + КОДИФ §1.29.
2. **Journal v2 (§21)** — журнал строится ТОЛЬКО через `renderJournalTable` + `JournalSpec` + `verifyRow` из `@labosfera/shared-spa`; 3 режима записи через shared `record-mode` c реальным ключом `inter-oge.record-mode.kit-4`. Никакого hardcode колонок в оркестраторе.
3. **Чистая физика** — `RefractionModel` pure, DOM-free, в `physics/optics/`; Vitest ≥95–100% строк + property-фаззинг (≥12 категорий / ≥50k итераций суммарно, по образцу `LensModel.test.ts`).
4. **Bootstrap (§14/§16)** — экран как `IScreen`; оркестратор владеет Store+Drag+Journal; `template.html` с обязательными слотами (`#record-mode-slot`, `#journal-host`, `#record-pending-slot/-btn/-summary`, `#result-panel`, `#hint-bar`, `#live-region`); регистрация в `KitShell` + Router (`?screen=refraction`).
5. **D&D через mouse** — selfcheck перетаскивает приборы ТОЛЬКО реальным `page.mouse.move/down/up`; REST-state (`.slot-rect` opacity 0 в покое); дроп подтверждать через `card[data-placed]`. Programmatic `placeInSlot` — только как явно помеченный fallback, если карточка реально скрыта.
6. **A11y** — axe 0 нарушений (правила НЕ отключать); роли/aria-live; клавиатура (хэндл угла — `role="slider"` + стрелки ±1°); `prefers-reduced-motion`; **скринридер и `#live-region`/`#result-panel` НЕ палят числовой ответ n в semi-auto/fully-manual** (i, r — это данные-измерения, их показывать МОЖНО; n — вычисляемый ответ, только в fully-auto).
7. **SVG-приборы (§27/§28)** — `lab-*`, shadow DOM, `svg[hidden]{display:none}` И `[hidden]{display:none}` для внутренних SVG-групп (IDL `.hidden` на группах не даёт display:none); функциональная деталь ≥15% высоты viewBox; `createElementNS` для всех SVG-узлов.
8. **Каталог** — `home/src/data/kits.ts` kit-4: ремап id опытов 4.3/4.6 на слаг зарегистрированного экрана `refraction`, `done:true`. Статус кита остаётся `planned` (→`ready` во Фазе F вместе с REFERENCE §30-блоком).
9. **Гейты зелёные** — из КОРНЯ `C:\dev\Inter_OGE`: `npm run typecheck`, `npm run lint` (0 предупреждений), `npm run test`, `npm run build` — все 0; плюс `node experiments/kit-4-optics/selfcheck-4-3.mjs` и `selfcheck-4-6.mjs` PASS FAIL=0; плюс reality-check скрином (мои глаза).
10. **Технологии** — TS strict; Vite 6; vanilla Web Components; npm workspaces; работать ТОЛЬКО из `C:\dev\Inter_OGE`. npm запускать через Git Bash.

**Физические константы (используются дословно во всех задачах):**
- `n_воздух = 1`, `n_стекло = 1,5` (полуцилиндр, СПЕЦ стр.19: «показатель преломления ≈ 1,5»).
- Снелл воздух→стекло: `sin i = n·sin r` ⇒ `r = asin(sin i / 1,5)`; ПВО нет (r<i всегда), домен i∈[0°,85°].
- Эталонные пары (i°→r° округл. до целого протрактора): 15→10, 30→19, 45→28, 60→35, 75→40.
- `refractionAngle(45,1,1.5) = 28,126°`; `refractiveIndex(45,28) = sin45/sin28 = 1,506`.

**Правила коммитов и файлов (пользовательские, обязательны):**
- Коммитить ТОЛЬКО явные пути (никогда `git add -A`/`git add .`); не коммитить `dist/`, `*.tsbuildinfo`, `package-lock.json`, скриншоты.
- `selfcheck-*.mjs` в `.gitignore` → добавлять `git add -f`.
- Сообщение коммита на русском, императив, заканчивается строкой:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- Ветка `feat/fipi-full-coverage`; merge/push — только по подтверждению владельца (в этом плане НЕ выполняются).

**Честность тестов (урок «ЛГУЩИЙ ТЕСТ» — пойман в A/B/C/D пять раз подряд, ловить агрессивно):**
- Каждый ассерт ОБЯЗАН уметь дать красный. Для derived-инпута в fully-manual проверять `input[data-key].value`, НЕ `textContent` у `<td>` (у ячейки с input textContent всегда '').
- Для a11y-live: двойной `requestAnimationFrame` + `expect(live.length > 0)` + guard `hidden === false`; доказывать честность сломом.
- В selfcheck НЕТ programmatic-fallback, маскирующего невидимый UI: если кнопка/элемент должны быть видны — явный ассерт видимости + FAIL при отсутствии.

---

## File Structure

**Создаём:**
- `experiments/kit-4-optics/src/physics/optics/RefractionModel.ts` — чистая физика Снелла (T1).
- `experiments/kit-4-optics/src/physics/optics/__tests__/RefractionModel.test.ts` — unit + фаззинг (T1).
- `experiments/kit-4-optics/src/ui/components/lab-protractor-disc.ts` — SVG-прибор: транспортир + полуцилиндр + лучи + хэндл угла (T3 каркас, T4 лучи/интеракция).
- `experiments/kit-4-optics/src/ui/components/__tests__/lab-protractor-disc.test.ts` — тесты компонента (T3/T4).
- `experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts` — оркестратор (T5 каркас, T7 журнал, T8 график).
- `experiments/kit-4-optics/src/screens/refraction/RefractionScreen.ts` — IScreen-фасад (T6).
- `experiments/kit-4-optics/src/screens/refraction/template.html` — разметка экрана (T6).
- `experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts` — state-machine тесты оркестратора (T5/T7/T8).
- `experiments/kit-4-optics/src/styles/refraction-experiment.css` — стили экрана (T6).
- `experiments/kit-4-optics/selfcheck-4-3.mjs`, `selfcheck-4-6.mjs` — Playwright reality-check (T9).

**Модифицируем:**
- `experiments/_shared-spa/src/lib/journal/specs.ts` — `REFRACTION_INDEX_SPEC` (4.3), `REFRACTION_ANGLE_SPEC` (4.6), в `ALL_SPECS` (T2). `types.ts` НЕ трогаем (union уже содержит '4.3','4.6').
- `experiments/_shared-spa/__tests__/*` — тесты новых спеков (T2).
- `experiments/kit-4-optics/src/shell/IScreen.ts` — `ScreenId` += `'refraction'` (T6). `IconId` НЕ трогаем (уже `'lens' | 'prism'`).
- `experiments/kit-4-optics/src/main.ts` — импорт `lab-protractor-disc`, `refraction-experiment.css`, регистрация `new RefractionScreen()` в `screens[]` (T6).
- `experiments/home/src/data/kits.ts` — ремап id 4.3/4.6 → `refraction`, `done:true` (T9).

**Реиспользуем без изменений:** `controller/OpticalDragController.ts`, `ui/components/lab-graph.ts`, `ui/components/lab-equipment-card.ts`, `ui/components/lab-kit-nav.ts`, `ui/components/lab-kit-header.ts`, `controller/Store.ts`, shared `record-mode`/`journal/render`/`journal/verify`.

---

## Task 1: RefractionModel — чистая физика Снелла

**Files:**
- Create: `experiments/kit-4-optics/src/physics/optics/RefractionModel.ts`
- Test: `experiments/kit-4-optics/src/physics/optics/__tests__/RefractionModel.test.ts`

**Interfaces:**
- Produces:
  - `guard(...xs: number[]): void` — бросает `RangeError` на non-finite (зеркало `LensModel.guard`).
  - `refractionAngle(iDeg: number, n1: number, n2: number): number` — угол преломления в градусах; `r = asin(n1·sin i / n2)`.
  - `refractiveIndex(iDeg: number, rDeg: number): number` — `n = sin i / sin r`.

- [ ] **Step 1: Написать падающие тесты**

Создать `__tests__/RefractionModel.test.ts`. Пример-ориентир — `LensModel.test.ts` (тот же стиль: точные значения + property-фаззинг по категориям через `for`-циклы, БЕЗ внешних библиотек).

```ts
import { describe, it, expect } from 'vitest';
import { guard, refractionAngle, refractiveIndex } from '../RefractionModel';

const near = (a: number, b: number, eps = 1e-6) => expect(Math.abs(a - b)).toBeLessThan(eps);

describe('refractionAngle (Снелл воздух→стекло, n=1.5)', () => {
  it('i=0 → r=0 (нормальное падение не преломляется)', () => near(refractionAngle(0, 1, 1.5), 0));
  it('i=30 → r≈19.471°', () => near(refractionAngle(30, 1, 1.5), 19.47122, 1e-4));
  it('i=45 → r≈28.126°', () => near(refractionAngle(45, 1, 1.5), 28.12608, 1e-4));
  it('i=60 → r≈35.264°', () => near(refractionAngle(60, 1, 1.5), 35.26439, 1e-4));
  it('r<i при i>0 для n2>n1 (преломление к нормали)', () => {
    for (let i = 1; i <= 85; i++) expect(refractionAngle(i, 1, 1.5)).toBeLessThan(i);
  });
  it('монотонность: i растёт → r растёт', () => {
    for (let i = 1; i <= 84; i++) {
      expect(refractionAngle(i + 1, 1, 1.5)).toBeGreaterThan(refractionAngle(i, 1, 1.5));
    }
  });
  it('ограничение сверху: r ≤ asin(1/1.5)=41.81° для всех i', () => {
    for (let i = 0; i <= 89; i++) expect(refractionAngle(i, 1, 1.5)).toBeLessThanOrEqual(41.811);
  });
});

describe('refractiveIndex (обратная задача)', () => {
  it('n = sin i / sin r', () => near(refractiveIndex(45, 28.12608), 1.5, 1e-4));
  it('взаимная обратность refractionAngle↔refractiveIndex (фаззинг)', () => {
    for (let ni = 12; ni <= 20; ni++) {          // n 1.2..2.0
      const n = ni / 10;
      for (let i = 1; i <= 85; i++) {
        const r = refractionAngle(i, 1, n);
        near(refractiveIndex(i, r), n, 1e-6);
      }
    }
  });
});

describe('guard / домен', () => {
  it('guard бросает на non-finite', () => {
    expect(() => guard(NaN)).toThrow(RangeError);
    expect(() => guard(1, Infinity)).toThrow(RangeError);
    expect(() => guard(1, 2, 3)).not.toThrow();
  });
  it('refractionAngle бросает при ПВО/вне домена (sin r>1)', () => {
    // стекло→воздух при большом i: n1=1.5,n2=1,i=60 → sin r=1.299>1
    expect(() => refractionAngle(60, 1.5, 1)).toThrow(RangeError);
  });
  it('refractiveIndex бросает при sin r≈0', () => {
    expect(() => refractiveIndex(30, 0)).toThrow(RangeError);
  });
  it('refractionAngle бросает на non-finite аргумент', () => {
    expect(() => refractionAngle(NaN, 1, 1.5)).toThrow(RangeError);
  });
});

// mustFix M5 — фаззинг ≥12 категорий / ≥50k итераций (образец LensModel.test.ts «12 инвариантов»).
describe('property fuzzing — ≥12 инвариантных категорий / ≥50k итераций', () => {
  const toDeg = (rad: number) => rad / (Math.PI / 180);
  it('инварианты на сетке i×n (≥50k итераций)', () => {
    let iters = 0;
    // сетка: i 0..85 (86) × n 1.00..2.00 шаг 0.01 (101) = 8686 базовых точек × 6 проверок ≈ 52k
    for (let iDeg = 0; iDeg <= 85; iDeg++) {
      for (let nk = 100; nk <= 200; nk++) {
        const n = nk / 100;
        if (iDeg === 0) near(refractionAngle(0, 1, n), 0);          // (1) i=0 → r=0
        const r = refractionAngle(iDeg, 1, n);
        expect(Number.isFinite(r)).toBe(true);                      // (2) r конечен
        expect(r).toBeGreaterThanOrEqual(0);                        // (3) r≥0
        expect(r).toBeLessThanOrEqual(iDeg + 1e-9);                 // (4) r≤i при n>1 (к нормали)
        expect(r).toBeLessThanOrEqual(toDeg(Math.asin(1 / n)) + 1e-6); // (5) r≤asin(1/n)
        if (iDeg > 0) near(refractiveIndex(iDeg, r), n, 1e-6);      // (6) round-trip
        iters += 6;
      }
    }
    for (let nk = 100; nk <= 200; nk += 5) {                        // (7) монотонность r(i)
      const n = nk / 100;
      for (let iDeg = 0; iDeg < 85; iDeg++) {
        expect(refractionAngle(iDeg + 1, 1, n)).toBeGreaterThan(refractionAngle(iDeg, 1, n));
        iters++;
      }
    }
    for (let iDeg = 1; iDeg <= 80; iDeg++) {                        // (8) обратимость хода: r,n,1 → i
      for (let nk = 105; nk <= 200; nk += 5) {
        const n = nk / 100;
        near(refractionAngle(refractionAngle(iDeg, 1, n), n, 1), iDeg, 1e-6);
        iters++;
      }
    }
    for (let iDeg = 45; iDeg <= 85; iDeg += 5) {                    // (9) ПВО стекло→воздух
      if (iDeg > toDeg(Math.asin(1 / 1.5))) expect(() => refractionAngle(iDeg, 1.5, 1)).toThrow(RangeError);
      iters++;
    }
    for (let iDeg = 1; iDeg <= 85; iDeg++) {                        // (10) n=sin i/sin r; (11) n>1; (12) детерминизм
      const r = refractionAngle(iDeg, 1, 1.5);
      near(Math.sin(iDeg * Math.PI / 180) / Math.sin(r * Math.PI / 180), 1.5, 1e-9);
      expect(refractiveIndex(iDeg, r)).toBeGreaterThan(1);
      expect(refractionAngle(iDeg, 1, 1.5)).toBe(r);
      iters += 3;
    }
    expect(iters).toBeGreaterThanOrEqual(50000);
  });
});
```

- [ ] **Step 2: Прогнать тесты — убедиться, что падают**

Run (из `C:\dev\Inter_OGE`): `npm run test --workspace=@labosfera/kit-4-optics`
Expected: FAIL — `RefractionModel` не существует (Cannot find module).

- [ ] **Step 3: Реализовать `RefractionModel.ts`**

```ts
/**
 * Кит-4 «Оптика» — pure-физика преломления (закон Снелла).
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№4 (стр.19) + сноска (4):
 * «... показателя преломления стекла; ... зависимости угла преломления от угла
 *  падения на границе воздух – стекло.»
 * КОДИФ §1.29. Геометрическая оптика (не волновая).
 * Углы — в ГРАДУСАХ (как читает ученик по круговому транспортиру).
 */

/** Бросает RangeError если хотя бы один аргумент не является конечным числом. */
export function guard(...xs: number[]): void {
  for (const x of xs) if (!Number.isFinite(x)) throw new RangeError(`non-finite arg: ${x}`);
}

const DEG = Math.PI / 180;

/**
 * Угол преломления r [°] по закону Снелла: n1·sin i = n2·sin r ⇒ r = asin(n1·sin i / n2).
 * i=0 → r=0. Для воздух→стекло (n2>n1) всегда r<i.
 * Бросает RangeError при sin r ∉ [−1,1] (полное внутреннее отражение / вне домена).
 */
export function refractionAngle(iDeg: number, n1: number, n2: number): number {
  guard(iDeg, n1, n2);
  if (n2 === 0) throw new RangeError('refractionAngle: n2=0');
  const s = (n1 * Math.sin(iDeg * DEG)) / n2;
  if (s < -1 || s > 1) throw new RangeError(`refractionAngle: вне домена (sin r=${s}) — ПВО`);
  return Math.asin(s) / DEG;
}

/**
 * Показатель преломления n = sin i / sin r (обработка данных опыта 4.3).
 * Бросает RangeError при sin r ≈ 0 (n не определён).
 */
export function refractiveIndex(iDeg: number, rDeg: number): number {
  guard(iDeg, rDeg);
  const sr = Math.sin(rDeg * DEG);
  if (Math.abs(sr) < 1e-12) throw new RangeError('refractiveIndex: sin r ≈ 0 (n не определён)');
  return Math.sin(iDeg * DEG) / sr;
}
```

- [ ] **Step 4: Прогнать тесты — убедиться, что зелёные**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: PASS (все тесты RefractionModel зелёные).

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/physics/optics/RefractionModel.ts \
        experiments/kit-4-optics/src/physics/optics/__tests__/RefractionModel.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): RefractionModel — закон Снелла (pure + фаззинг) для опытов 4.3/4.6

refractionAngle (r=asin(n1·sin i/n2)), refractiveIndex (n=sin i/sin r), guard.
ФИПИ-якорь СПЕЦ компл.№4 стр.19: показатель преломления + r(i).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Журнал-спеки REFRACTION_INDEX_SPEC (4.3) + REFRACTION_ANGLE_SPEC (4.6)

**Files:**
- Modify: `experiments/_shared-spa/src/lib/journal/specs.ts` (добавить 2 спека + записи в `ALL_SPECS`, после `TWO_LENS_SPEC`)
- Test: `experiments/_shared-spa/__tests__/refraction-specs.test.ts` (создать)

**Interfaces:**
- Consumes: `JournalSpec`, `ColumnSpec` из `./types` (union уже содержит '4.3','4.6' — правки types.ts НЕ нужны).
- Produces:
  - `REFRACTION_INDEX_SPEC: JournalSpec` (experimentId '4.3'): колонки `idx`(meta), `i_deg`(direct,°,int), `r_deg`(direct,°,int), `n`(derived, fixed2, tol 0.05, `= sin i/sin r`).
  - `REFRACTION_ANGLE_SPEC: JournalSpec` (experimentId '4.6'): `idx`(meta), `i_deg`(direct,°,int), `r_deg`(direct,°,int). Без derived (r — измеряемая величина; правило нелинейности — в графике/result-panel, по образцу `SERIES_VOLTAGE_SPEC` 3.8).
  - Обе в `ALL_SPECS` → `getSpecByExperimentId('4.3'|'4.6')` возвращает их.

- [ ] **Step 1: Написать падающие тесты**

Создать `experiments/_shared-spa/__tests__/refraction-specs.test.ts`. Ориентир по стилю — существующий `lens-specs.test.ts` (тот же каталог).

```ts
import { describe, it, expect } from 'vitest';
import { REFRACTION_INDEX_SPEC, REFRACTION_ANGLE_SPEC, getSpecByExperimentId } from '../src/lib/journal/specs';
import { verifyRow } from '../src/lib/journal/verify';

describe('REFRACTION_INDEX_SPEC (4.3)', () => {
  it('experimentId 4.3, kit-4, зарегистрирован в ALL_SPECS', () => {
    expect(REFRACTION_INDEX_SPEC.experimentId).toBe('4.3');
    expect(REFRACTION_INDEX_SPEC.kitId).toBe('kit-4');
    expect(getSpecByExperimentId('4.3')).toBe(REFRACTION_INDEX_SPEC);
  });
  it('колонки: idx, i_deg(direct), r_deg(direct), n(derived)', () => {
    const keys = REFRACTION_INDEX_SPEC.columns.map((c) => c.key);
    expect(keys).toEqual(['idx', 'i_deg', 'r_deg', 'n']);
    const n = REFRACTION_INDEX_SPEC.columns.find((c) => c.key === 'n')!;
    expect(n.source).toBe('derived');
  });
  it('n derived = sin i / sin r (i=45,r=28 → ≈1.506)', () => {
    const n = REFRACTION_INDEX_SPEC.columns.find((c) => c.key === 'n')!;
    const val = n.expectedFromRow!({ i_deg: 45, r_deg: 28 });
    expect(Math.abs(val - 1.506)).toBeLessThan(0.005);
  });
  it('n derived не делит на ноль при r=0 (guard → 0)', () => {
    const n = REFRACTION_INDEX_SPEC.columns.find((c) => c.key === 'n')!;
    expect(n.expectedFromRow!({ i_deg: 0, r_deg: 0 })).toBe(0);
  });
  it('verifyRow: верное n=1.51 при i=45,r=28 → ok; неверное 1.0 → wrong', () => {
    const okRow = { idx: 1, timestamp: 0, values: { idx: 1, i_deg: 45, r_deg: 28, n: 1.51 } };
    const badRow = { idx: 1, timestamp: 0, values: { idx: 1, i_deg: 45, r_deg: 28, n: 1.0 } };
    expect(verifyRow(REFRACTION_INDEX_SPEC.columns, okRow)['n']).toBe('ok');
    expect(verifyRow(REFRACTION_INDEX_SPEC.columns, badRow)['n']).toBe('wrong');
  });
});

describe('REFRACTION_ANGLE_SPEC (4.6)', () => {
  it('experimentId 4.6, зарегистрирован', () => {
    expect(REFRACTION_ANGLE_SPEC.experimentId).toBe('4.6');
    expect(getSpecByExperimentId('4.6')).toBe(REFRACTION_ANGLE_SPEC);
  });
  it('колонки: idx, i_deg(direct), r_deg(direct); без derived', () => {
    const keys = REFRACTION_ANGLE_SPEC.columns.map((c) => c.key);
    expect(keys).toEqual(['idx', 'i_deg', 'r_deg']);
    expect(REFRACTION_ANGLE_SPEC.columns.every((c) => c.source !== 'derived')).toBe(true);
  });
});
```

- [ ] **Step 2: Прогнать — убедиться, что падают**

Run: `npm run test --workspace=@labosfera/shared-spa`
Expected: FAIL — `REFRACTION_INDEX_SPEC` не экспортируется.

- [ ] **Step 3: Реализовать спеки в `specs.ts`**

Вставить ПЕРЕД `export const ALL_SPECS` (после `TWO_LENS_SPEC`). Физика инлайнится (не импорт из kit-4), по канону §21.A.4:

```ts
/**
 * Опыт 4.3 «Измерение показателя преломления стекла».
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№4 (стр.19), сноска (4):
 * «...показателя преломления стекла...». КОДИФ §1.29.
 *
 * Метод: узкий луч на плоскую грань полуцилиндра в центре кругового транспортира →
 *   снять i (в воздухе) и r (в стекле) от нормали → n = sin i / sin r ≈ 1,5.
 * i,r (по транспортиру) — direct; n — derived (ученик считает; программа в fully-auto).
 * Инлайн-формула считает ТОЛЬКО из прямых i_deg,r_deg (§21.A.4).
 */
const REFR_DEG = Math.PI / 180;

export const REFRACTION_INDEX_SPEC: JournalSpec = {
  experimentId: '4.3',
  kitId: 'kit-4',
  columns: [
    { key: 'idx', label: '№', source: 'meta', format: 'int' },
    { key: 'i_deg', label: 'i, °', source: 'direct', unit: '°', format: 'int' },
    { key: 'r_deg', label: 'r, °', source: 'direct', unit: '°', format: 'int' },
    {
      key: 'n', label: 'n', source: 'derived', format: 'fixed2', tolerance: 0.05,
      // n = sin i / sin r (закон преломления; из прямых i_deg,r_deg)
      expectedFromRow: (r) => {
        const sr = Math.sin((r.r_deg ?? 0) * REFR_DEG);
        return sr > 1e-9 ? Math.sin((r.i_deg ?? 0) * REFR_DEG) / sr : 0;
      },
    },
  ],
};

/**
 * Опыт 4.6 «Исследование зависимости угла преломления от угла падения».
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№4 (стр.19), сноска (4):
 * «...зависимости угла преломления от угла падения на границе воздух – стекло.». КОДИФ §1.29.
 *
 * Метод: варьировать i → снимать r по транспортиру → таблица + график r(i) (нелинейный;
 *   в осях sin r–sin i — прямая). Вывод — в #result-panel (не построчная проверка), как 3.8.
 * i,r — direct (обе измеряемые). Derived-колонок нет.
 */
export const REFRACTION_ANGLE_SPEC: JournalSpec = {
  experimentId: '4.6',
  kitId: 'kit-4',
  columns: [
    { key: 'idx', label: '№', source: 'meta', format: 'int' },
    { key: 'i_deg', label: 'i, °', source: 'direct', unit: '°', format: 'int' },
    { key: 'r_deg', label: 'r, °', source: 'direct', unit: '°', format: 'int' },
  ],
};
```

Добавить обе в массив `ALL_SPECS` (после `TWO_LENS_SPEC,`):

```ts
  TWO_LENS_SPEC,
  REFRACTION_INDEX_SPEC,
  REFRACTION_ANGLE_SPEC,
];
```

- [ ] **Step 4: Прогнать — убедиться, что зелёные**

Run: `npm run test --workspace=@labosfera/shared-spa`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/_shared-spa/src/lib/journal/specs.ts \
        experiments/_shared-spa/__tests__/refraction-specs.test.ts
git commit -m "$(cat <<'EOF'
feat(journal): REFRACTION_INDEX_SPEC (4.3) + REFRACTION_ANGLE_SPEC (4.6)

4.3: i,r direct → n derived (=sin i/sin r, tol 5%); 4.6: i,r direct (правило r(i) — в графике/result-panel).
Оба в ALL_SPECS. types.ts не тронут (union уже знает 4.3/4.6).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: lab-protractor-disc — статический каркас (транспортир + полуцилиндр + гнёзда)

**Files:**
- Create: `experiments/kit-4-optics/src/ui/components/lab-protractor-disc.ts`
- Test: `experiments/kit-4-optics/src/ui/components/__tests__/lab-protractor-disc.test.ts`

**Interfaces (весь публичный контракт компонента; лучи/интеракция — в T4):**
- Produces (custom element `<lab-protractor-disc>`):
  - `getSlotRect(id: 'semicylinder' | 'emitter'): DOMRect` — viewport-координаты гнезда (для D&D; зеркало `lab-optical-bench.getSlotRect`).
  - `setSlotHover(id: string, active: boolean): void` — подсветка гнезда при hover.
  - `setPlaced(kind: 'semicylinder' | 'emitter', on: boolean): void` — показать/скрыть прибор в гнезде (полуцилиндр / осветитель).
  - `setDragging(on: boolean): void` — добавляет/снимает класс `dragging-active` на host (REST → показать все `.slot-rect`).
  - Атрибут-ридбэк: `refractiveIndex` (свойство, default 1.5) — используется в T4.

**Геометрия (SVG-единицы), контракт для рендера:**
- `viewBox="0 0 420 420"`, центр `C=(210,210)`, радиус диска `R=180`.
- **Плоская грань** полуцилиндра = горизонтальный диаметр (`y=210`, от x=30 до x=390). **Стекло** = НИЖНИЙ полукруг (y>210), тонированный (полупрозрачный голубой). **Воздух** = верхний полукруг.
- **Нормаль** = вертикальная пунктирная линия через центр (x=210, от y=30 до y=390).
- **Транспортир:** минорные штрихи каждые 1°, мажорные + подпись каждые 10°, по всему ободу; углы отсчитываются от вертикальной нормали (0° сверху и снизу, 90° у плоской грани слева/справа). Подписи 0–90 в каждом квадранте.
- **Гнёзда** (`.slot-rect`, `data-slot`): `semicylinder` — центр диска (принимает полуцилиндр); `emitter` — слева на уровне грани (принимает осветитель). REST: `.slot-rect{opacity:0}`, `:host(.dragging-active) .slot-rect{opacity:1}` и `.slot-rect.drop-zone--active{opacity:1}` (точный паттерн — из `lab-optical-bench`).
- Стили обязательно включают `svg[hidden]{display:none}` И `[hidden]{display:none}` (внутренние группы).

- [ ] **Step 1: Написать падающие тесты (happy-dom)**

Создать `__tests__/lab-protractor-disc.test.ts`. Ориентир — `__tests__/lab-optical-bench.test.ts`.

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import '../lab-protractor-disc';

function mount(): HTMLElement & {
  getSlotRect(id: string): DOMRect;
  setPlaced(kind: string, on: boolean): void;
  setDragging(on: boolean): void;
} {
  const el = document.createElement('lab-protractor-disc') as any;
  document.body.appendChild(el);
  return el;
}

describe('lab-protractor-disc — статический каркас', () => {
  beforeEach(() => { document.body.innerHTML = ''; });

  it('монтируется с shadowRoot и SVG', () => {
    const el = mount();
    expect(el.shadowRoot).not.toBeNull();
    expect(el.shadowRoot!.querySelector('svg')).not.toBeNull();
  });

  it('рисует нормаль, плоскую грань, тело стекла', () => {
    const el = mount();
    const sr = el.shadowRoot!;
    expect(sr.querySelector('.normal-line')).not.toBeNull();
    expect(sr.querySelector('.flat-face')).not.toBeNull();
    expect(sr.querySelector('.glass-body')).not.toBeNull();
  });

  it('рисует градусные подписи транспортира (мажорные каждые 10°)', () => {
    const el = mount();
    const labels = el.shadowRoot!.querySelectorAll('.tick-label');
    expect(labels.length).toBeGreaterThanOrEqual(9); // 0..90 в одном квадранте минимум
  });

  it('2 гнезда (.slot-rect) с data-slot semicylinder/emitter', () => {
    const el = mount();
    const slots = Array.from(el.shadowRoot!.querySelectorAll('[data-slot]')).map(
      (s) => s.getAttribute('data-slot'),
    );
    expect(slots).toContain('semicylinder');
    expect(slots).toContain('emitter');
  });

  it('setDragging(true/false) переключает класс host.dragging-active (реальную REST-opacity проверяет selfcheck — happy-dom не считает CSS)', () => {
    const el = mount();
    el.setDragging(true);
    expect(el.classList.contains('dragging-active')).toBe(true);
    el.setDragging(false);
    expect(el.classList.contains('dragging-active')).toBe(false);
  });

  it('setPlaced(semicylinder,true) показывает полуцилиндр (группа не hidden)', () => {
    const el = mount();
    const glass = el.shadowRoot!.querySelector('.glass-body') as SVGElement;
    el.setPlaced('semicylinder', false);
    expect(glass.hasAttribute('hidden')).toBe(true);
    el.setPlaced('semicylinder', true);
    expect(glass.hasAttribute('hidden')).toBe(false);
  });

  it('getSlotRect возвращает DOMRect для semicylinder и emitter', () => {
    const el = mount();
    const r1 = el.getSlotRect('semicylinder');
    const r2 = el.getSlotRect('emitter');
    expect(r1).toBeInstanceOf(DOMRect);
    expect(r2).toBeInstanceOf(DOMRect);
  });
});
```

- [ ] **Step 2: Прогнать — убедиться, что падают**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: FAIL — компонент не существует.

- [ ] **Step 3: Реализовать статический каркас**

Создать `lab-protractor-disc.ts` по образцу структуры `lab-optical-bench.ts` (константы геометрии → `template` со `<style>` (обязательно `svg[hidden]{display:none}` и `[hidden]{display:none}`) → класс `extends HTMLElement` с `attachShadow({mode:'open'})`). В этой задаче:
- Отрисовать в `connectedCallback` (или в `#render`): диск-обод, `.glass-body` (path нижнего полукруга, класс, атрибут `hidden` по умолчанию до `setPlaced`), `.flat-face` (линия по диаметру), `.normal-line` (пунктирная вертикаль), штрихи транспортира (`.tick`, `.tick-label` — циклом; `createElementNS`), 2 группы гнёзд с `<rect class="slot-rect">` внутри контейнера `[data-slot]`.
- `getSlotRect(id)`: найти `[data-slot="…"]`, вернуть `.getBoundingClientRect()` (в happy-dom вернётся DOMRect с нулями — это ок для unit-теста; реальные координаты проверяет selfcheck).
- `setSlotHover(id,on)`: toggle класс `drop-zone--active` на `[data-slot=id]`.
- `setPlaced(kind,on)`: для `semicylinder` — toggle `hidden` на `.glass-body` (+ подпись «n≈1,5»); для `emitter` — toggle `hidden` на группе осветителя (`.emitter-group`).
- `setDragging(on)`: `this.classList.toggle('dragging-active', on)`.
- `connectedCallback`: host `role="group"` + `aria-label` («Круговой транспортир с полуцилиндром для опыта на преломление») + `<title>` на SVG как описание. **НЕ `role="img"`** (mustFix M4: внутри будет фокусируемый `role="slider"`-хэндл из T4; `role="img"` делает потомков презентационными и прячет слайдер от скринридера/axe). По умолчанию `setPlaced('semicylinder', false)` и `setPlaced('emitter', false)` (приборы в лотке, не на диске).

- [ ] **Step 4: Прогнать — зелёные**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/ui/components/lab-protractor-disc.ts \
        experiments/kit-4-optics/src/ui/components/__tests__/lab-protractor-disc.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): lab-protractor-disc — статический каркас (транспортир + полуцилиндр + гнёзда)

Круговой транспортир (штрихи/подписи), полуцилиндр n≈1,5 плоской гранью на диаметре,
нормаль, 2 гнезда (semicylinder/emitter), REST-state, getSlotRect/setPlaced/setDragging.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: lab-protractor-disc — лучи, перетаскиваемый хэндл угла, a11y

**Files:**
- Modify: `experiments/kit-4-optics/src/ui/components/lab-protractor-disc.ts`
- Modify: `experiments/kit-4-optics/src/ui/components/__tests__/lab-protractor-disc.test.ts`

**Interfaces (добавляемый публичный контракт):**
- `setIncidenceAngle(iDeg: number): void` — задать угол падения (целое, клампится в [0,85]); пересчитывает r по Снеллу (n=`refractiveIndex` свойство, default 1.5), перерисовывает падающий/преломлённый/отражённый лучи, обновляет ридауты i/r и `aria-valuenow` хэндла.
- `get incidenceAngleDeg(): number`, `get refractionAngleDeg(): number` (округл. до целого — как читает ученик).
- Свойство `refractiveIndex` (get/set, default 1.5).
- Событие `angle-change` (`CustomEvent<{ i: number; r: number }>`, `bubbles:true, composed:true`) — эмитится при drag/keyboard/`setIncidenceAngle` (i,r — целые).
- Хэндл конца падающего луча: `role="slider"`, `aria-valuemin="0"`, `aria-valuemax="85"`, `aria-valuenow=i`, `aria-label="Угол падения, градусы"`, `tabindex="0"`; стрелки ←/↓ = −1°, →/↑ = +1° (уважать `prefers-reduced-motion` для анимаций, не для функции).

**Геометрия лучей (контракт):**
- i отсчитывается от ВЕРХНЕЙ нормали; падающий луч — из точки на верхнем ободе под углом i к нормали (со стороны хэндла), к центру.
- Преломлённый луч — в НИЖНЮю (стеклянную) половину под углом r=`refractionAngle(i,1,n)` к нижней нормали, по радиусу к ободу (нормальный выход на цилиндрической грани — без второго преломления).
- Отражённый луч (форк 3) — в верхнюю половину под углом i по ДРУГУЮ сторону нормали, тусклый (`opacity ~0.35`, класс `.ray-reflected`).
- Дуги i (между падающим и нормалью) и r (между преломлённым и нормалью) + текстовые ридауты «i = N°» / «r = N°».

- [ ] **Step 1: Дописать падающие тесты**

Добавить в тот же тест-файл:

```ts
describe('lab-protractor-disc — лучи и угол', () => {
  beforeEach(() => { document.body.innerHTML = ''; });

  it('setIncidenceAngle(45) → refractionAngleDeg≈28 (Снелл n=1.5)', () => {
    const el = mount() as any;
    el.setPlaced('semicylinder', true);
    el.setIncidenceAngle(45);
    expect(el.incidenceAngleDeg).toBe(45);
    expect(el.refractionAngleDeg).toBe(28); // round(28.126)
  });

  it('setIncidenceAngle клампит в [0,85]', () => {
    const el = mount() as any;
    el.setIncidenceAngle(120);
    expect(el.incidenceAngleDeg).toBe(85);
    el.setIncidenceAngle(-10);
    expect(el.incidenceAngleDeg).toBe(0);
  });

  it('эмитит angle-change с целыми i,r', () => {
    const el = mount() as any;
    let detail: any = null;
    el.addEventListener('angle-change', (e: CustomEvent) => { detail = e.detail; });
    el.setIncidenceAngle(60);
    expect(detail).toEqual({ i: 60, r: 35 }); // round(35.264)
  });

  it('рисует падающий, преломлённый и отражённый лучи', () => {
    const el = mount() as any;
    el.setPlaced('semicylinder', true);
    el.setIncidenceAngle(45);
    const sr = el.shadowRoot!;
    expect(sr.querySelector('.ray-incident')).not.toBeNull();
    expect(sr.querySelector('.ray-refracted')).not.toBeNull();
    expect(sr.querySelector('.ray-reflected')).not.toBeNull();
  });

  it('хэндл угла: role=slider, aria-valuenow синхронизирован', () => {
    const el = mount() as any;
    el.setIncidenceAngle(30);
    const handle = el.shadowRoot!.querySelector('.angle-handle')!;
    expect(handle.getAttribute('role')).toBe('slider');
    expect(handle.getAttribute('aria-valuenow')).toBe('30');
  });

  it('стрелка вправо на хэндле → +1°', () => {
    const el = mount() as any;
    el.setIncidenceAngle(30);
    const handle = el.shadowRoot!.querySelector('.angle-handle') as HTMLElement;
    handle.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowRight', bubbles: true }));
    expect(el.incidenceAngleDeg).toBe(31);
  });

  it('refractiveIndex по умолчанию 1.5', () => {
    const el = mount() as any;
    expect(el.refractiveIndex).toBe(1.5);
  });
});
```

- [ ] **Step 2: Прогнать — падают**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: FAIL — `setIncidenceAngle`/лучи ещё не реализованы.

- [ ] **Step 3: Реализовать лучи + интеракцию**

- Импортировать `refractionAngle` из `../../physics/optics/RefractionModel`.
- Хранить `#i` (целое), `#n=1.5`. `setIncidenceAngle(i)`: `#i = Math.round(clamp(i,0,85))`; `r = Math.round(refractionAngle(#i,1,#n))`; перерисовать лучи/дуги/ридауты; `aria-valuenow=#i`; `dispatchEvent(new CustomEvent('angle-change',{detail:{i:#i,r},bubbles:true,composed:true}))`.
- Геттеры `incidenceAngleDeg`/`refractionAngleDeg` (последний = `Math.round(refractionAngle(#i,1,#n))`).
- Отрисовка лучей (SVG-линии, `createElementNS`, классы `.ray-incident`/`.ray-refracted`/`.ray-reflected`), дуги (`path` arc), ридауты (`text`).
- Хэндл `.angle-handle` (circle/g, `role="slider"` + aria + `tabindex=0`): `pointerdown/move/up` на хэндле → угол = atan2 от центра к курсору спроецированный на верхний обод → `setIncidenceAngle`. `keydown` ArrowRight/ArrowUp → +1, ArrowLeft/ArrowDown → −1.
- Учитывать `svg[hidden]`/`[hidden]` для скрытия лучей до размещения полуцилиндра (лучи скрыты, пока `semicylinder` не placed).

- [ ] **Step 4: Прогнать — зелёные**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/ui/components/lab-protractor-disc.ts \
        experiments/kit-4-optics/src/ui/components/__tests__/lab-protractor-disc.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): lab-protractor-disc — лучи Снелла + перетаскиваемый хэндл угла + a11y

Падающий/преломлённый/тусклый отражённый лучи; setIncidenceAngle/getters; событие angle-change;
хэндл role=slider со стрелками ±1°; refractiveIndex=1.5.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: RefractionExperiment — каркас оркестратора (Store, задачи A/B, D&D, угол, hint)

**Files:**
- Create: `experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts`
- Create: `experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts`

**Interfaces:**
- Consumes: `Store` (`controller/Store`), `OpticalDragController` (`controller/OpticalDragController`), `<lab-protractor-disc>` API из T3/T4, `HintEngine`-паттерн (скопировать локальный класс из `LensBenchExperiment.ts`, НЕ импортировать — он приватный там).
- Produces:
  - `type RefractionTaskId = 'A-index' | 'B-angle'`.
  - `type RefractionEquipmentId = 'semicylinder' | 'emitter'`; `type RefractionKind = 'semicylinder' | 'emitter'`.
  - `interface RefractionRefs` — DOM-ссылки (зеркало `ExperimentRefs` из lens-bench, минус bench-специфика; см. ниже).
  - `class RefractionExperiment`:
    - `constructor(refs: RefractionRefs)`.
    - getters: `activeTask: RefractionTaskId`, `incidenceAngleDeg: number`, `refractionAngleDeg: number`, `measurements: ReadonlyArray<RefractionMeasurement>`, `bothPlaced: boolean`.
    - `setActiveTask(task: RefractionTaskId): void`.
    - `setIncidenceAngle(i: number): void` (проксирует в диск + обновляет состояние).
    - `recordMeasurement(): void` (полноценно — в T7; в T5 — заглушка, добавляющая {i,r} в measurements).
    - `reset(): void`, `destroy(): void`.
  - `interface RefractionMeasurement { task: RefractionTaskId; iDeg: number; rDeg: number }`.

`RefractionRefs` (поля, все обязательны кроме журнальных `?`):
```ts
export interface RefractionRefs {
  stage: HTMLElement;
  disc: HTMLElement & {
    getSlotRect(id: string): DOMRect;
    setSlotHover(id: string, on: boolean): void;
    setPlaced(kind: string, on: boolean): void;
    setDragging(on: boolean): void;
    setIncidenceAngle(i: number): void;
    readonly incidenceAngleDeg: number;
    readonly refractionAngleDeg: number;
  };
  dragOverlay: HTMLElement;
  hintBar: HTMLElement;
  liveRegion: HTMLElement;
  resetBtn: HTMLButtonElement;
  steps: HTMLElement;
  resultPanel: HTMLElement;
  cards: NodeListOf<LabEquipmentCard>;
  graphHost?: HTMLElement;         // T8
  graphToggleBtn?: HTMLButtonElement; // T8
  recordModeSlot?: HTMLElement;    // T7
  journalHost?: HTMLElement;       // T7
  recordPendingSlot?: HTMLElement; // T7
  recordPendingBtn?: HTMLButtonElement; // T7
  recordPendingSummary?: HTMLElement;   // T7
}
```

- [ ] **Step 1: Написать падающие state-machine тесты (happy-dom)**

Создать `__tests__/refraction.test.ts`. Ориентир — `screens/lens-bench/__tests__/lens-bench.test.ts` (как строить фейковые refs + монтировать). Построить минимальный DOM (`steps` с двумя `[data-task]`-кнопками, `disc` = реальный `<lab-protractor-disc>`, пустые слоты журнала).

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import '../../../ui/components/lab-protractor-disc';
import { RefractionExperiment, type RefractionRefs } from '../RefractionExperiment';

function buildRefs(): RefractionRefs { /* создать DOM-узлы, вернуть refs (см. паттерн lens-bench.test) */ }

describe('RefractionExperiment — каркас', () => {
  let exp: RefractionExperiment;
  beforeEach(() => { document.body.innerHTML = ''; exp = new RefractionExperiment(buildRefs()); });

  it('дефолтная задача A-index', () => expect(exp.activeTask).toBe('A-index'));
  it('setActiveTask(B-angle) переключает', () => { exp.setActiveTask('B-angle'); expect(exp.activeTask).toBe('B-angle'); });
  it('setIncidenceAngle(45) → refractionAngleDeg=28', () => {
    exp.setIncidenceAngle(45);
    expect(exp.incidenceAngleDeg).toBe(45);
    expect(exp.refractionAngleDeg).toBe(28);
  });
  it('bothPlaced=false до размещения приборов', () => expect(exp.bothPlaced).toBe(false));
  it('reset возвращает угол к дефолту, сохраняет активную задачу', () => {
    exp.setActiveTask('B-angle'); exp.setIncidenceAngle(70); exp.reset();
    expect(exp.activeTask).toBe('B-angle');
  });
  it('recordMeasurement (заглушка) добавляет {i,r} после размещения', () => {
    // эмулировать размещение через внутренний placeInSlot (debug API) + угол
    (exp as any).placeInSlot('semicylinder', 'semicylinder');
    (exp as any).placeInSlot('emitter', 'emitter');
    exp.setIncidenceAngle(45);
    exp.recordMeasurement();
    expect(exp.measurements.at(-1)).toMatchObject({ iDeg: 45, rDeg: 28 });
  });
});
```

- [ ] **Step 2: Прогнать — падают**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: FAIL — `RefractionExperiment` не существует.

- [ ] **Step 3: Реализовать каркас**

Скопировать структуру из `LensBenchExperiment.ts` (Store, локальный `HintEngine`, wireUp табов через `[data-task]`, drag через `OpticalDragController` с 2 снап-зонами из `disc.getSlotRect`, `#recordPlacement`, `reset`/`destroy`). Отличия:
- Состояние `RefractionState { activeTask; iDeg; placed: Set<RefractionEquipmentId>; measurements: RefractionMeasurement[] }`.
- Снап-зоны: `semicylinder` (accepts `['semicylinder']`) и `emitter` (accepts `['emitter']`); onDrop → `#recordPlacement` → `disc.setPlaced(kind, true)` + отметить в `placed`; когда оба placed → `disc.setIncidenceAngle(iDeg)` (лучи появляются).
- `attach` карточек (mustFix M2): селектор `card.querySelector('.eq-glyph')` — НЕ копировать `lab-lens/lab-light-object`-селектор lens-bench (вернёт null → D&D не подключится, а happy-dom это скрывает, т.к. тесты идут через `placeInSlot`). `canDrag: () => card.getAttribute('status') !== 'placed'`; `onDragStart/End` → `disc.setDragging(true/false)` + `stage.classList` как в lens-bench.
- Подписка на `disc` событие `angle-change` → обновить `iDeg` в Store + `#afterAngleChange` (в T7 — pending).
- `setIncidenceAngle(i)` → `disc.setIncidenceAngle(i)` + Store.
- `#hints` (локальный HintEngine): A-index → «Наведите луч на полуцилиндр, снимите i и r, посчитайте n = sin i / sin r»; B-angle → «Меняйте угол падения и записывайте r — постройте график r(i)».
- Debug API для тестов/selfcheck: `placeInSlot(kind, equipmentId)` (programmatic) — **самостоятельный, БЕЗ `#topology`** (shouldFix S3: в lens-bench он зовёт `#topology.place`, которого здесь нет): сам добавляет в `state.placed`, зовёт `disc.setPlaced(kind,true)` и, когда оба placed, `disc.setIncidenceAngle(iDeg)`. `window.refractionExperiment` навешивается в screen (T6).
- `recordMeasurement()` (в T5 — минимальная): если `bothPlaced` → push `{task, iDeg, rDeg:refractionAngleDeg}` в measurements.

- [ ] **Step 4: Прогнать — зелёные**

Run: `npm run test --workspace=@labosfera/kit-4-optics`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts \
        experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): RefractionExperiment — каркас (Store, задачи A/B, D&D 2 гнезда, угол, hint)

Store+HintEngine+OpticalDragController (semicylinder/emitter), setActiveTask/setIncidenceAngle,
подписка angle-change, getters, reset/destroy. Журнал/график — далее.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: RefractionScreen + template.html + css + регистрация (мультиэкран)

**Files:**
- Create: `experiments/kit-4-optics/src/screens/refraction/RefractionScreen.ts`
- Create: `experiments/kit-4-optics/src/screens/refraction/template.html`
- Create: `experiments/kit-4-optics/src/styles/refraction-experiment.css`
- Modify: `experiments/kit-4-optics/src/shell/IScreen.ts` (`ScreenId` += `'refraction'`)
- Modify: `experiments/kit-4-optics/src/main.ts` (импорты + регистрация)

**Interfaces:**
- Consumes: `RefractionExperiment` + `RefractionRefs` (T5); `IScreen`/`ScreenMeta` (shell).
- Produces: `class RefractionScreen implements IScreen` (зеркало `LensBenchScreen`) с `meta = { id:'refraction', label:'Показатель преломления', kicker:'Опыт 4.3 · Преломление', icon:'prism', tooltip:'...' }`.

- [ ] **Step 1: `ScreenId` += 'refraction'**

В `IScreen.ts` изменить строку 14:
```ts
export type ScreenId = 'lens-bench' | 'refraction';
```

- [ ] **Step 2: `template.html`**

Скопировать структуру `screens/lens-bench/template.html` (workbench: `#steps` role=tablist + `#hint-bar` + `#reset-btn`; stage; measurement-panel с `#record-mode-slot`, `#journal-host`, `#record-pending-slot/-btn/-summary`, `#result-panel`; `#drag-overlay`; equipment-panel; `#live-region`). Отличия:
- `#steps`: ДВЕ кнопки-таба: `data-task="A-index"` («Показатель преломления (4.3)») и `data-task="B-angle"` («Угол преломления (4.6)»), с `role="tab"`, `aria-selected`, `tabindex`.
- Сцена: `<lab-protractor-disc id="protractor-disc" aria-label="Круговой транспортир с полуцилиндром"></lab-protractor-disc>` вместо скамьи; БЕЗ слайдеров положения (угол задаётся хэндлом на диске).
- Блок графика (для 4.6, скрыт вне B): контейнер `<div id="graph-block" hidden><lab-graph id="refraction-graph" aria-label="График зависимости угла преломления от угла падения"></lab-graph><button id="graph-toggle-btn" type="button" aria-pressed="false">Оси: r(i) ↔ sin</button></div>.
- equipment-panel: две карточки `lab-equipment-card` — `data-eq="semicylinder"` и `data-eq="emitter"`, обе `status="available"`, `data-draggable=…`, `data-dropzone`/`data-dropzone-id` по образцу lens-bench. **Внутри КАЖДОЙ карточки — конкретный перетаскиваемый элемент со стабильным селектором** (mustFix M2): `<svg class="eq-glyph">…</svg>` (глиф полуцилиндра / осветителя, функц-деталь ≥15% viewBox). В lens-bench drag вешается на внутренний `lab-lens`/`lab-light-object`; здесь такого компонента нет → T5 `attach` цепляется на `.eq-glyph`, а НЕ на тег-селектор lens-bench (иначе `querySelector` вернёт null и D&D не подключится).

- [ ] **Step 3: `refraction-experiment.css`**

Скопировать `styles/lens-bench-experiment.css` и адаптировать (селекторы layout те же — `.app-main-v3/.workbench/.stage/.measurement-panel/.equipment-panel`; убрать bench-специфику слайдеров; добавить стили `#graph-block`/`#graph-toggle-btn`). Убедиться, что `#graph-block[hidden]{display:none}`.

- [ ] **Step 4: `RefractionScreen.ts`**

Зеркало `LensBenchScreen.ts`: `mount` инжектит `template.html`, достаёт `disc` (cast с методами T3/T4), собирает `RefractionRefs`, создаёт `RefractionExperiment`, вешает `window.refractionExperiment` для selfcheck; `unmount` → reset+destroy+replaceChildren; `reset`; `saveState(){return {}}`.

- [ ] **Step 5: Регистрация в `main.ts`**

Добавить импорты после существующих (строки ~32–36):
```ts
import './ui/components/lab-protractor-disc';
import './styles/refraction-experiment.css';
```
Импорт экрана рядом с `LensBenchScreen`:
```ts
import { RefractionScreen } from '@screens/refraction/RefractionScreen';
```
Расширить массив (строка ~73):
```ts
const screens: IScreen[] = [
  new LensBenchScreen(),
  new RefractionScreen(),
];
```
(default остаётся `'lens-bench'`; `lab-graph` уже импортирован в main.ts строкой 29.)

- [ ] **Step 6: Дописать тест монтирования экрана**

Добавить в `__tests__/refraction.test.ts` блок, монтирующий `RefractionScreen` в `host` и проверяющий: `#protractor-disc` присутствует, обе `[data-task]`-кнопки есть, `window.refractionExperiment` установлен, `unmount` очищает.

- [ ] **Step 7: Прогнать unit + typecheck**

Run: `npm run test --workspace=@labosfera/kit-4-optics` — PASS.
Run (из корня): `npm run typecheck` — 0 (в т.ч. новый `ScreenId`).

- [ ] **Step 8: Reality-check (мои глаза) — экран монтируется**

Запустить dev: `npm run dev --workspace=@labosfera/kit-4-optics -- --host 127.0.0.1 --port 5192 --strictPort` (Git Bash).
Открыть `http://127.0.0.1:5192/?screen=refraction`; убедиться: транспортир+полуцилиндр видны, есть 2 таба, нижняя навигация показывает 2 экрана, **иконка вкладки refraction (`prism`) рендерится глифом, а не пустым квадратом** (shouldFix S4: тип `IconId` уже разрешает 'prism', но SVG-глиф в `lab-kit-nav`/shell мог отсутствовать — при пустом квадрате добавить глиф 'prism' в рендерер иконок). Скрин.

- [ ] **Step 9: Коммит**

```bash
git add experiments/kit-4-optics/src/screens/refraction/RefractionScreen.ts \
        experiments/kit-4-optics/src/screens/refraction/template.html \
        experiments/kit-4-optics/src/styles/refraction-experiment.css \
        experiments/kit-4-optics/src/shell/IScreen.ts \
        experiments/kit-4-optics/src/main.ts \
        experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): экран refraction — RefractionScreen + template + css + регистрация (2 экрана)

IScreen-фасад (icon prism), шаблон (табы A/B, транспортир, блок графика, журнал-слоты),
ScreenId+='refraction', регистрация в main.ts. Экран монтируется на ?screen=refraction.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: RefractionExperiment — журнал v2 (3 режима) + опыт 4.3 (n) + result-panel

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts`
- Modify: `experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts`

**Interfaces:**
- Consumes: `renderJournalTable` (`@labosfera/shared-spa/lib/journal/render`), `verifyRow` (`.../journal/verify`), record-mode API (`@labosfera/shared-spa/lib/record-mode`), `getSpecByExperimentId`/`REFRACTION_INDEX_SPEC`/`REFRACTION_ANGLE_SPEC` (`.../journal/specs`), `JournalSpec`/`JournalRow`/`JournalVerdict` (`.../journal/types`). Точные вызовы — скопировать из `LensBenchExperiment.#refreshUi`/`#buildJournalRow`/`#wireUp` (record-mode toggle) — строки 615–1044.
- Produces: полноценные `recordMeasurement()`, `#refreshUi()`, `#buildJournalRow()`, `#pendingSignature()`, `#handleRecordModeChange()`, result-panel для A-index.

**Правила журнала:**
- `SPEC_BY_TASK: Record<RefractionTaskId, JournalSpec> = { 'A-index': REFRACTION_INDEX_SPEC, 'B-angle': REFRACTION_ANGLE_SPEC }`.
- В строку записываются `i_deg`, `r_deg` (direct, всегда заполнены из текущего угла диска). Для 4.3 `n` (derived): заполняется программой ТОЛЬКО в fully-auto (иначе null — ученик вводит).
- **A11y no-leak (Global Constraint 6, критично — 3 линзы verify M1):** значение `n` (в т.ч. подстрока «1,5»/«1.5») попадает в `#result-panel` И `#live-region` ТОЛЬКО в fully-auto. i, r показывать МОЖНО (это измерения-данные). В semi-auto/fully-manual result-panel для 4.3 = качественный текст БЕЗ какого-либо числа n: **«Показатель преломления стекла постоянен и не зависит от угла падения — вычислите n = sin i / sin r по своим измерениям.»** (НИ «≈1,5», НИ иного значения n). Приёмочный чек-лист по режимам: semi-auto → result-panel без n, `input[data-key="n"]` присутствует и пустой; fully-manual → то же; fully-auto → n в ячейке (readonly-текст) и число в result-panel. Демо-углы 4.3: **45/60/75°** (n=1,506/1,510/1,503; избегать i=30, где целочисленное r=19 даёт n=1,54).
- Запись доступна (pending видна в semi-auto), когда `bothPlaced && iDeg>0` (i=0 не даёт преломления). Сигнатура анти-дубля: `${task}-${iDeg}`.
- result-panel A-index (fully-auto): «n = <среднее по строкам> ≈ 1,5 (постоянно для стекла)».

- [ ] **Step 1: Дописать падающие тесты**

Добавить в `__tests__/refraction.test.ts`:

```ts
describe('RefractionExperiment — журнал 4.3', () => {
  // buildRefs() ДОЛЖЕН включать recordModeSlot/journalHost/recordPendingSlot/-btn/-summary/resultPanel
  it('запись строки 4.3 рендерит журнал с i_deg,r_deg', () => {
    const exp = new RefractionExperiment(buildRefs());
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    exp.setIncidenceAngle(45); exp.recordMeasurement();
    const host = /* refs.journalHost */;
    expect(host.querySelectorAll('.lab-journal-body tr').length).toBeGreaterThan(0);
    expect(host.querySelector('[data-key="i_deg"]')).not.toBeNull();
  });

  it('ЛГУЩИЙ-ТЕСТ guard: в fully-manual n — редактируемый input, проверяем input.value (НЕ textContent td)', async () => {
    localStorage.setItem('inter-oge.record-mode.kit-4','fully-manual');
    const exp = new RefractionExperiment(buildRefs());
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    exp.setIncidenceAngle(45); exp.recordMeasurement();
    const input = /* refs.journalHost */.querySelector('input[data-key="n"]') as HTMLInputElement;
    expect(input).not.toBeNull();
    expect(input.value).toBe(''); // пусто в fully-manual — ученик вводит; ассерт умеет краснеть
    localStorage.removeItem('inter-oge.record-mode.kit-4');
  });

  it('a11y no-leak: semi-auto — n не в result-panel, не в live-region; input[n] пуст (S5/S6)', async () => {
    localStorage.setItem('inter-oge.record-mode.kit-4','semi-auto');
    const refs = buildRefs();
    const exp = new RefractionExperiment(refs);
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    exp.setIncidenceAngle(45); exp.recordMeasurement();
    // result-panel: n≈1.5 не утекает (паттерн «1,5»/«1.5»)
    expect(/1[.,]5\d?/.test(refs.resultPanel.textContent ?? '')).toBe(false);
    // S6: n derived НЕ подставлен программой — поле ввода пусто (ассерт умеет краснеть)
    const nInput = refs.journalHost!.querySelector('input[data-key="n"]') as HTMLInputElement;
    expect(nInput).not.toBeNull();
    expect(nInput.value).toBe('');
    // S5: live-region читаем ПОСЛЕ двойного rAF (announce откладывает запись); i/r озвучены, n — нет
    await new Promise((res) => requestAnimationFrame(() => requestAnimationFrame(() => res(null))));
    const live = refs.liveRegion.textContent ?? '';
    expect(live.length).toBeGreaterThan(0);       // пустой live = провал (ассерт умеет краснеть)
    expect(/1[.,]5\d?/.test(live)).toBe(false);    // n не озвучен
    localStorage.removeItem('inter-oge.record-mode.kit-4');
  });

  it('fully-auto: derived n readonly ≈1.5', () => {
    localStorage.setItem('inter-oge.record-mode.kit-4','fully-auto');
    const refs = buildRefs();
    const exp = new RefractionExperiment(refs);
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    exp.setIncidenceAngle(45); exp.recordMeasurement();
    const td = refs.journalHost!.querySelector('td[data-key="n"]')!;
    expect(td.querySelector('input')).toBeNull();
    expect(/1[.,]5/.test(td.textContent ?? '')).toBe(true);
    localStorage.removeItem('inter-oge.record-mode.kit-4');
  });
});
```

- [ ] **Step 2: Прогнать — падают** — `npm run test --workspace=@labosfera/kit-4-optics` → FAIL.

- [ ] **Step 3: Реализовать журнальную проводку** (перенести и адаптировать из `LensBenchExperiment`): record-mode toggle в `#wireUp`, `#refreshUi` (renderJournalTable c `SPEC_BY_TASK[activeTask]`, pending-плашка, verifyRow по ✓), `#buildJournalRow` (i_deg/r_deg всегда; n = null кроме fully-auto), `#pendingSignature`, `recordMeasurement` (push + `#refreshUi` + announce i/r в live-region, БЕЗ n), result-panel A-index.

- [ ] **Step 4: Прогнать — зелёные** — PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts \
        experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): refraction журнал v2 (3 режима) + опыт 4.3 n=sin i/sin r + result-panel

renderJournalTable/verifyRow/record-mode kit-4; n derived только в fully-auto (no-leak);
result-panel «n≈1,5 постоянно». Честный тест input.value для fully-manual.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: RefractionExperiment — опыт 4.6: график r(i) + переключатель sin r–sin i + result-panel

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts`
- Modify: `experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts`
- Modify (ОБЯЗАТЕЛЬНО, mustFix M3): `experiments/kit-4-optics/src/ui/components/lab-graph.ts` — добавить `gridXStep?`/`gridYStep?` в `GraphData` (default 1 → обратная совместимость с I(U) и др.). Шаг используется И в `#drawGrid`, И в `#drawAxisTicks` (поддержать дробный шаг). Без этого r(i) (xMax 90, yMax 45) даст ~130 линий сетки (нечитаемо), а sin-режим (xMax 1) — только метки 0 и 1 (shouldFix S7). Микро-тест: вызов без поля рисует прежнее число линий (обратная совместимость).

**Interfaces:**
- Consumes: `<lab-graph>` `.data` сеттер (`GraphData`) из `lab-graph.ts`.
- Produces: `#refreshGraph()` в оркестраторе (виден только при `activeTask==='B-angle'`), состояние `graphMode: 'ri' | 'sin'` (переключатель).

**Правила графика:**
- Показывать `#graph-block` только в B-angle (иначе `hidden`).
- `graphMode='ri'` (default): `series=[{id:'ri', color:'#a855f7', fit:'curve', points: measurements.map((m,k)=>({id:String(k), x:m.iDeg, y:m.rDeg, label:`i=${m.iDeg}°, r=${m.rDeg}°`}))}]`, `xLabel:'i, °'`, `yLabel:'r, °'`, `xMax:90`, `yMax:45`, **`gridXStep:10, gridYStep:5`** (обязательно, M3).
- `graphMode='sin'`: точки `{x: sin(i°), y: sin(r°)}`, `fit:'line'` (МНК через 0 — прямая наклона 1/n≈0.667), `xLabel:'sin i'`, `yLabel:'sin r'`, `xMax:1`, `yMax:1`, **`gridXStep:0.2, gridYStep:0.2`** (M3/S7).
- **Линия/кривая рисуются только при ≥2 точках** (`lab-graph.#render` требует `points.length>=2`, shouldFix S2): result-panel/hint в B-angle подсказывают «снимите ≥2 угла»; selfcheck-4-6 делает свип ≥2 углов ПЕРЕД проверкой линии/кривой.
- `#graph-toggle-btn`: клик переключает `graphMode`, обновляет `aria-pressed` и `#refreshGraph`.
- result-panel B-angle: «Угол преломления растёт с углом падения, но всё медленнее (нелинейно). В осях sin r–sin i зависимость линейна (закон Снелла).» БЕЗ утечки n в semi-auto (для 4.6 n вообще не колонка — безопасно; текст качественный).

- [ ] **Step 1: Дописать падающие тесты**

```ts
describe('RefractionExperiment — график 4.6', () => {
  it('B-angle: после 3 записей lab-graph.data.series[0].points.length === 3, режим ri', () => {
    const refs = buildRefs(); // включает graphHost, graphToggleBtn
    const exp = new RefractionExperiment(refs);
    exp.setActiveTask('B-angle');
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    for (const i of [30,45,60]) { exp.setIncidenceAngle(i); exp.recordMeasurement(); }
    const graph = refs.graphHost!.querySelector('#refraction-graph') as any;
    expect(graph.data.series[0].points.length).toBe(3);
    expect(graph.data.xLabel).toBe('i, °');
    expect(graph.data.series[0].fit).toBe('curve');
  });
  it('переключатель на sin: xLabel sin i, fit line, точки = sin значений', () => {
    const refs = buildRefs();
    const exp = new RefractionExperiment(refs);
    exp.setActiveTask('B-angle');
    (exp as any).placeInSlot('semicylinder','semicylinder');
    (exp as any).placeInSlot('emitter','emitter');
    exp.setIncidenceAngle(45); exp.recordMeasurement();
    refs.graphToggleBtn!.click();
    const graph = refs.graphHost!.querySelector('#refraction-graph') as any;
    expect(graph.data.xLabel).toBe('sin i');
    expect(graph.data.series[0].fit).toBe('line');
    expect(Math.abs(graph.data.series[0].points[0].x - Math.sin(45*Math.PI/180))).toBeLessThan(1e-6);
  });
  it('график скрыт в A-index', () => {
    const refs = buildRefs();
    const exp = new RefractionExperiment(refs);
    exp.setActiveTask('A-index');
    expect(refs.graphHost!.hidden || refs.graphHost!.closest('#graph-block')?.hidden).toBeTruthy();
  });
});
```

- [ ] **Step 2: Прогнать — падают** → FAIL.

- [ ] **Step 3: Реализовать** `#refreshGraph()`, состояние `graphMode`, переключатель, видимость `#graph-block` по задаче, result-panel B-angle. Если сетка r(i) визуально перегружена — добавить `gridXStep`/`gridYStep` в `lab-graph` (обратно-совместимо) + микро-тест на дефолт.

- [ ] **Step 4: Прогнать — зелёные** → PASS.

- [ ] **Step 5: Коммит**

```bash
git add experiments/kit-4-optics/src/screens/refraction/RefractionExperiment.ts \
        experiments/kit-4-optics/src/screens/refraction/__tests__/refraction.test.ts \
        experiments/kit-4-optics/src/ui/components/lab-graph.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): опыт 4.6 — график r(i) + переключатель sin r–sin i + result-panel

lab-graph: fit curve (r(i)) ↔ fit line МНК (sin r–sin i, наклон 1/n); блок графика только в B-angle;
result-panel о нелинейности/линеаризации.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Каталог (ремап id 4.3/4.6) + selfcheck-4-3.mjs + selfcheck-4-6.mjs

**Files:**
- Modify: `experiments/home/src/data/kits.ts` (kit-4 опыты 4.3/4.6)
- Create: `experiments/kit-4-optics/selfcheck-4-3.mjs`
- Create: `experiments/kit-4-optics/selfcheck-4-6.mjs`

**Interfaces:** нет новых экспортов; каталог указывает на слаг `refraction`, selfcheck'и — Playbook-скрипты (в `.gitignore` → `git add -f`).

- [ ] **Step 1: Ремап каталога**

В `kits.ts` (строки 147 и 150) заменить `id` и `done`:
```ts
{ id: 'refraction', title: 'Показатель преломления стекла', resultVerb: 'Измерь показатель преломления стекла', fipiTask: '4.3', isFipi: true, done: true },
```
```ts
{ id: 'refraction', title: 'Угол преломления от угла падения', resultVerb: 'Исследуй зависимость угла преломления от угла падения', fipiTask: '4.6', isFipi: true, done: true },
```
Статус кита-4 — оставить `'planned'` (переключение на `'ready'` + REFERENCE §30 — Фаза F).

- [ ] **Step 2: selfcheck-4-3.mjs**

Скопировать каркас `selfcheck-4-5.mjs` (helpers pass/fail/skip/screenshot, launch, axe), адаптировать: `SCREEN='?screen=refraction'`, port 5192, `window.refractionExperiment`. Проверки (честные, без маскирующих fallback — Global Constraint 5):
1. Навигация `?screen=refraction`, 200 OK, `lab-protractor-disc` смонтирован.
2. Переключение на таб `[data-task="A-index"]`: `aria-selected="true"`, неактивные `false`.
3. REST-state: `.slot-rect` opacity 0 в покое.
4. Реальный mouse-D&D: полуцилиндр → гнездо `semicylinder`, осветитель → `emitter`; подтверждение `card[data-placed]`.
5. `setIncidenceAngle(45)` (typeof-guard: метод существует как функция) → `incidenceAngleDeg===45`, `refractionAngleDeg===28`; лучи `.ray-incident/.ray-refracted/.ray-reflected` присутствуют в shadowRoot.
6. semi-auto: ЯВНЫЙ ассерт `#record-pending-btn` ВИДНА → клик; строка журнала; ввод n=1.51 → verdict ok; ввод n=1.0 → verdict wrong.
7. a11y no-leak: `#result-panel`/`#live-region` НЕ содержат «1,5»/«1.5» как ответа n в semi-auto (паттерн не ловит инструкцию).
8. fully-auto: `td[data-key="n"]` readonly с числом ≈1,5 (не input).
9. 3 режима (ключ `inter-oge.record-mode.kit-4`).
10. axe 0 (правила не отключать).
Запуск в docstring.

- [ ] **Step 3: selfcheck-4-6.mjs**

Аналогично, таб `[data-task="B-angle"]`. Специфика:
1–4. как выше (навигация/таб B/REST/D&D).
5. Свип углов: `setIncidenceAngle` для [15,30,45,60,75], после каждого `recordMeasurement` (или через pending-кнопку в semi-auto реальным кликом). Проверить монотонность r по строкам журнала (r растёт с i).
6. График: `#graph-block` виден; `#refraction-graph` имеет N точек; переключатель `#graph-toggle-btn` меняет `xLabel` c «i, °» на «sin i» (читать через evaluate по `.data`).
7. result-panel B-angle содержит вывод о нелинейности.
8. 3 режима; 9. axe 0.

- [ ] **Step 4: Прогнать selfcheck'и (dev на 5192)**

Git Bash: `npm run dev --workspace=@labosfera/kit-4-optics -- --host 127.0.0.1 --port 5192 --strictPort &`
`node experiments/kit-4-optics/selfcheck-4-3.mjs` → PASS FAIL=0.
`node experiments/kit-4-optics/selfcheck-4-6.mjs` → PASS FAIL=0.

- [ ] **Step 5: Коммит**

```bash
git add experiments/home/src/data/kits.ts
git add -f experiments/kit-4-optics/selfcheck-4-3.mjs experiments/kit-4-optics/selfcheck-4-6.mjs
git commit -m "$(cat <<'EOF'
feat(kit-4): каталог 4.3/4.6 → экран refraction (done) + selfcheck-4-3/4-6

Ремап id опытов 4.3/4.6 на слаг refraction, done:true (статус кита planned → ready во Фазе F).
Playwright selfcheck'и: D&D мышью, n≈1,5, монотонность r(i), переключатель осей, axe 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Финал фазы (после Task 9, вне пронумерованных задач — оркестратор SDD)

1. **Финал whole-branch review** — Workflow (opus, диапазон BASE..HEAD Фазы E), 4–5 линз (архитектура/физика/a11y/честность тестов/каталог).
2. **Reality-check моими глазами** (Playwright 5192): 4.3 (снять i/r, посчитать n, no-leak), 4.6 (свип + график r(i) + переключатель sin), отражённый луч виден, изоляция A↔B, REST-state.
3. **Consolidated fix** (1 агент, все mustFix+shouldFix одним заходом).
4. **Merge-gate из КОРНЯ** `C:\dev\Inter_OGE`: `npm run typecheck && npm run lint && npm run test && npm run build` — все 0.
5. **Bookkeeping:** леджер `.superpowers/sdd/progress.md` (Фаза E complete + RESUME POINT Фаза F + DEFER), память (`project_inter_oge_fipi_coverage_epic` + MEMORY.md), рефлексия `.business/история/`.
6. Ветку `feat/fipi-full-coverage` НЕ мержить/пушить без подтверждения владельца.

---

## Self-Review (проведён при написании плана)

**1. Spec coverage (спека волны-2 §3/§4.2/§5/§6/§7):**
- 4.3 (n=sin i/sin r) → T1 (модель), T2 (REFRACTION_INDEX_SPEC), T7 (журнал+result-panel), T9 (selfcheck). ✔
- 4.6 (r(i)) → T2 (REFRACTION_ANGLE_SPEC), T8 (график+переключатель), T9 (selfcheck). ✔
- Экран refraction (мульти-таск A/B) → T5/T6. ✔
- `RefractionModel` (Снелл, pure+фаззинг) → T1. ✔
- `lab-protractor-disc` (транспортир+полуцилиндр+лучи+отражённый) → T3/T4. ✔
- Каталог ремап → T9. ✔
- Форки: (1) хэндл-drag → T4; (2) 3 целевых угла n постоянно → T7/T9; (3) тусклый отражённый луч → T4; (4) r(i)+переключатель sin → T8. ✔

**2. Placeholder scan:** код приведён для pure-единиц (модель, спеки) полностью; для компонента/оркестратора даны точные публичные контракты + тест-кейсы + ссылки на файлы-эталоны с номерами строк (реализация — транскрипция паттерна lens-bench). Нет «TBD/итд».

**3. Type consistency:** `RefractionTaskId` ('A-index'|'B-angle'), `RefractionEquipmentId`/`RefractionKind` ('semicylinder'|'emitter'), `RefractionMeasurement {task,iDeg,rDeg}`, `RefractionRefs`, `ScreenId+='refraction'`, keys журнала `i_deg`/`r_deg`/`n` — согласованы во всех задачах (T2 keys = T7/T8/T9 селекторы; T4 `angle-change {i,r}` = T5 подписка; T5 `setIncidenceAngle` = T4 метод диска). `IconId` не трогаем (уже 'prism'). `types.ts` union не трогаем (уже 4.3/4.6).

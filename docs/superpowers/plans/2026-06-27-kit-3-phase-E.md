# Кит-3 Фаза E «Соединения проводников» (опыты 3.8/3.9) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Опыты ФИПИ **3.8 правило напряжений (последовательное, U=U1+U2)** и **3.9 правило токов (параллельное, I=I1+I2)** на новом мульти-таск экране `connections` пакета `kit-3-circuits`. После Фазы E kit-3 = 9/9 опытов.

**Architecture:** Новый экран `connections` (slug `ScreenId` уже добавлен в Фазе A — не трогать types) по образцу `wire-resistance` (мульти-таск task-switcher A-series/B-parallel). Один подвижный прибор (вольтметр для 3.8, амперметр для 3.9) перетаскивается учеником в 3 position-слота; фиксированный прибор (амперметр для 3.8, вольтметр для 3.9) сидит в своём слоте. Новый компонент `lab-connection-board` (атрибут `topology="series"|"parallel"`) — по образцу `lab-circuit-board` (структура/API идентичны). Физика: `seriesResistance` + `parallelResistance` в `CircuitModel.ts`. Правило (U1+U2≈U_общ / I1+I2≈I_общ) — в `#result-panel` с `aria-live="polite"` (не в грейдируемых колонках журнала — тот же приём, что в Фазе D).

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components, Vitest (happy-dom), Playwright, npm workspaces.

**Спека:** docs/superpowers/specs/2026-06-25-wave-1-kit-3-circuits.md §6, §4. **Эпик:** docs/superpowers/specs/2026-06-24-fipi-full-coverage-epic.md.

## Global Constraints

(Копия из эпика §5 / спеки; verbatim. Каждая задача неявно включает эти требования.)

- **ФИПИ-якорь** в docstring `ConnectionsExperiment.ts` и каждого SPEC: дословная цитата СПЕЦ Прил.2 компл.№3 (сноска 3) п.8 (3.8) / п.9 (3.9) + КОДИФ §1.29.
- **Оборудование:** R1=4,7 Ом (`lab-resistor variant="R1"`), R2=5,7 Ом (`lab-resistor variant="R2"`). Разные номиналы → U1≠U2, I1≠I2. Лампочку не включаем; грейдимую R-колонку не добавляем.
- **Journal v2 (§21):** `renderJournalTable(host, SPEC, rows, {mode,onCellInput,onVerify})` + `JournalSpec` в `_shared-spa/.../journal/specs.ts` + `verifyRow` + `parseRu`; 3 режима через `record-mode` ключ `'kit-3'`.
- **DRAG-ЗОНЫ: префикс `circuit-slot-`** (НЕ свой). `#rewireAssemblySlots`: `removeSnapZone('circuit-slot-X')` → `addSnapZone('circuit-slot-X')`, зеркало `wire-resistance`. Дубль-зона с другим префиксом → `onDrop` не ставит `card[data-placed]` (повторялось в Фазах C и D).
- **Вольтметр range="6"** + слайдер `max="6.0"` + clamp `setVoltage` `Math.max(1.0, Math.min(6.0, v))`; амперметр `range="3"`; подписи «Вольтметр 0–6 В»/«Амперметр 0–3 А».
- **Фильтр оборудования по задаче:** переключать `<section hidden>` (`card.closest('section')`), не `card.hidden` напрямую.
- **`reset(false)`** сбрасывает `#lastRecordedSignature=''` и `#lastAnnouncedConclusion=''`; вызывает фильтр оборудования; сбрасывает позицию подвижного прибора.
- **key-btn:** динамический `aria-label` в `#refreshUi` рядом с `aria-pressed`.
- **Новый экран** обязан иметь `connections-experiment.css` + import в `main.ts`; компоненты импортированы; экран зарегистрирован в `screens[]`.
- **`_shared-spa` тесты** живут в `__tests__/**` от КОРНЯ пакета (не co-located).
- **Один подвижный прибор в одной позиции:** при размещении в новую position-позицию убрать из прочих `position`-слотов (через `topology.remove` + UI-сброс карточки) — обеспечивает ЭКРАН, не `CircuitTopology.place()`.
- **Чистая физика** в `src/physics/circuit/`, Vitest + property-фаззинг; `RangeError` на невалидных входах (guard-стиль как в `current`/`lampCurrent`).
- **D&D через mouse** в selfcheck (`page.mouse.move/down/up`); подтверждать дроп через `card[data-placed]`; REST-state скрыт в покое.
- **A11y:** axe 0; `aria-live`; `svg[hidden]{display:none}` в shadow CSS каждого `lab-*`; `role="img"` + `<title>` на SVG.
- **Гейты** (корень, Git Bash): `npm run typecheck` · `test` · `lint` (0 err) · `build` — все `--workspaces`. Плюс `selfcheck-3-8-3-9.mjs` PASS + ручной reality-check скрином.
- **Платформа:** только `C:\dev\Inter_OGE`; dev `--host 127.0.0.1 --port <free> --strictPort`.
- **Единичные кавычки** (repo style); не коммитить `dist/`/`tsbuildinfo`/`package-lock`; явные пути в `git add`; selfcheck-*.mjs gitignored → `git add -f`.
- **Коммиты:** по-русски, императив; в конце каждого `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Уроки Фаз B/C/D (вшить обязательно)

- Вольтметр range=6, слайдер max="6.0", clamp [1.0; 6.0]; амперметр range=3 — иначе стрелка врёт.
- DRAG-зоны с префиксом `circuit-slot-` (не свой). `#rewireAssemblySlots` заменяет зоны.
- Фильтр оборудования на секцию (`card.closest('section')`), не на карточку напрямую.
- `reset(false)` сбрасывает `#lastRecordedSignature=''` и `#lastAnnouncedConclusion=''`.
- key-btn: динамический `aria-label` в `#refreshUi`.
- Новый экран: свой CSS-файл + import в main.ts; компоненты зарегистрированы.
- `_shared-spa` тесты — в `__tests__/` от корня пакета.

## Физика задачи (справочник)

R1 = 4,7 Ом, R2 = 5,7 Ом.

**3.8 Серия (последовательное):**
- R_общ = R1 + R2 = 10,4 Ом
- I_общ = U / R_общ (фиксированный амперметр в главной линии)
- U1 = I_общ · R1; U2 = I_общ · R2; U1 + U2 = U_источника ✓
- При U=6 В: I=0,577 А ≤ 3 А (амперметр), U1=2,71 В, U2=3,29 В ≤ 6 В (вольтметр)

**3.9 Параллель:**
- R_общ = R1·R2/(R1+R2) = 4,7·5,7/10,4 ≈ 2,575 Ом
- U на узлах = U_источника (фиксированный вольтметр)
- I1 = U/R1; I2 = U/R2; I_общ = I1+I2
- При U=6 В: I1=1,277 А, I2=1,053 А, I_общ=2,33 А ≤ 3 А (амперметр)

---

## File Structure

| Файл | Ответственность |
|---|---|
| `kit-3-circuits/src/physics/circuit/CircuitModel.ts` (+ `__tests__/CircuitModel.test.ts`) | += `seriesResistance(...R)` + `parallelResistance(...R)` + фаззинг |
| `_shared-spa/src/lib/journal/specs.ts` (+ `__tests__/connection-specs.test.ts`) | += `SERIES_VOLTAGE_SPEC` (3.8) + `PARALLEL_CURRENT_SPEC` (3.9); в `ALL_SPECS` |
| `kit-3-circuits/src/ui/components/lab-connection-board.ts` (+ `__tests__/components.test.ts`) | НОВЫЙ компонент: topology="series"\|"parallel", слоты series/parallel, getSlotRect, setCurrentAnimating |
| `kit-3-circuits/src/screens/connections/ConnectionsScreen.ts` | Фасад IScreen (meta id='connections', icon='connections', kicker='Опыт 3.8–3.9', label='Соединения проводников') |
| `kit-3-circuits/src/screens/connections/ConnectionsExperiment.ts` | Оркестратор (мульти-таск A-series/B-parallel; подвижный прибор в 3 position-слота; result-panel правило; журнал v2) |
| `kit-3-circuits/src/screens/connections/template.html` | Разметка экрана (task-switcher + `<lab-connection-board>` + панель + секции оборудования) |
| `kit-3-circuits/src/styles/connections-experiment.css` | Стили экрана (по образцу wire-resistance-experiment.css) |
| `kit-3-circuits/src/main.ts` | += `ConnectionsScreen` + `import './ui/components/lab-connection-board'` + CSS |
| `kit-3-circuits/selfcheck-3-8-3-9.mjs` | e2e self-check 2 опытов (реальный mouse-D&D; перестановка подвижного прибора в 3 позиции; инварианты U1+U2≈U_общ / I1+I2≈I_общ) |
| `experiments/home/src/data/kits.ts` | опыты 3.8/3.9 done=true (kit-3 7→9) |

union `experimentId` ('3.8'/'3.9') и `ScreenId`/`IconId` ('connections'/'connections') УЖЕ есть (Фаза A) — не трогать.

---

## Task 1: CircuitModel.seriesResistance + parallelResistance + фаззинг

**Files:**
- Modify: `kit-3-circuits/src/physics/circuit/CircuitModel.ts`
- Test: `kit-3-circuits/src/physics/circuit/__tests__/CircuitModel.test.ts`

**Interfaces:**
- Produces:
  - `export function seriesResistance(...R_Ohm: number[]): number` — ΣR; guard: ≥1 аргумент, все конечные >0; `RangeError` иначе.
  - `export function parallelResistance(...R_Ohm: number[]): number` — 1/Σ(1/R); тот же guard.
  - (Вспомогательные для показаний приборов — через уже существующую `current(U, R)` из CircuitModel.)

- [ ] **Step 1: Падающие тесты** — добавить в `kit-3-circuits/src/physics/circuit/__tests__/CircuitModel.test.ts`:

```ts
import {
  seriesResistance,
  parallelResistance,
  current,
} from '../CircuitModel';

describe('seriesResistance R = ΣR', () => {
  it('два резистора: R1=4,7 R2=5,7 → 10,4 Ом', () => {
    expect(seriesResistance(4.7, 5.7)).toBeCloseTo(10.4, 5);
  });
  it('один резистор: passthrough', () => {
    expect(seriesResistance(4.7)).toBeCloseTo(4.7, 6);
  });
  it('три резистора: 1+2+3=6', () => {
    expect(seriesResistance(1, 2, 3)).toBeCloseTo(6, 6);
  });
  it('R_послед ≥ max(Ri)', () => {
    const r = seriesResistance(4.7, 5.7, 2.0);
    expect(r).toBeGreaterThanOrEqual(Math.max(4.7, 5.7, 2.0));
  });
  it('RangeError на пустом аргументе', () => {
    // @ts-expect-error тест граничного случая
    expect(() => seriesResistance()).toThrow(RangeError);
  });
  it('RangeError на нечисловом', () => {
    expect(() => seriesResistance(NaN, 5.7)).toThrow(RangeError);
  });
  it('RangeError на нуле', () => {
    expect(() => seriesResistance(0, 5.7)).toThrow(RangeError);
  });
  it('RangeError на отрицательном', () => {
    expect(() => seriesResistance(-1, 5.7)).toThrow(RangeError);
  });
  it('фаззинг: seriesResistance ≥ max(Ri) для случайных положительных', () => {
    for (let i = 0; i < 300; i++) {
      const a = 0.5 + i * 0.03;
      const b = 0.3 + i * 0.02;
      const r = seriesResistance(a, b);
      expect(r).toBeGreaterThanOrEqual(Math.max(a, b) - 1e-9);
      expect(r).toBeCloseTo(a + b, 9);
    }
  });
});

describe('parallelResistance R = 1/Σ(1/R)', () => {
  it('R1=4,7 R2=5,7 → ≈2,575 Ом', () => {
    expect(parallelResistance(4.7, 5.7)).toBeCloseTo((4.7 * 5.7) / (4.7 + 5.7), 5);
  });
  it('один резистор: passthrough', () => {
    expect(parallelResistance(4.7)).toBeCloseTo(4.7, 6);
  });
  it('два равных R: R_пар = R/2', () => {
    expect(parallelResistance(6.0, 6.0)).toBeCloseTo(3.0, 6);
  });
  it('R_пар ≤ min(Ri)', () => {
    const r = parallelResistance(4.7, 5.7);
    expect(r).toBeLessThanOrEqual(Math.min(4.7, 5.7) + 1e-9);
  });
  it('RangeError на пустом аргументе', () => {
    // @ts-expect-error тест граничного случая
    expect(() => parallelResistance()).toThrow(RangeError);
  });
  it('RangeError на нечисловом', () => {
    expect(() => parallelResistance(NaN, 5.7)).toThrow(RangeError);
  });
  it('RangeError на нуле', () => {
    expect(() => parallelResistance(0, 5.7)).toThrow(RangeError);
  });
  it('RangeError на отрицательном', () => {
    expect(() => parallelResistance(-1, 5.7)).toThrow(RangeError);
  });
  it('фаззинг: parallelResistance ≤ min(Ri)', () => {
    for (let i = 0; i < 300; i++) {
      const a = 0.5 + i * 0.03;
      const b = 0.3 + i * 0.02;
      const r = parallelResistance(a, b);
      expect(r).toBeLessThanOrEqual(Math.min(a, b) + 1e-9);
      expect(r).toBeGreaterThan(0);
    }
  });
  it('фаззинг: серия ≥ параллель для тех же резисторов', () => {
    for (let i = 1; i <= 200; i++) {
      const a = i * 0.05;
      const b = i * 0.07;
      expect(seriesResistance(a, b)).toBeGreaterThan(parallelResistance(a, b));
    }
  });
});

describe('серия/параллель — физические инварианты R1=4,7 R2=5,7', () => {
  const R1 = 4.7;
  const R2 = 5.7;
  const U = 4.5;

  it('серия: U1 + U2 = U_источника', () => {
    const R_ser = seriesResistance(R1, R2);
    const I_ser = current(U, R_ser);
    const U1 = I_ser * R1;
    const U2 = I_ser * R2;
    expect(U1 + U2).toBeCloseTo(U, 6);
  });

  it('параллель: I1 + I2 = I_общ', () => {
    const I1 = current(U, R1);
    const I2 = current(U, R2);
    const R_par = parallelResistance(R1, R2);
    const I_total = current(U, R_par);
    expect(I1 + I2).toBeCloseTo(I_total, 6);
  });

  it('серия: U1 < U_общ, U2 < U_общ (ни одно не всё напряжение)', () => {
    const R_ser = seriesResistance(R1, R2);
    const I_ser = current(U, R_ser);
    const U1 = I_ser * R1;
    const U2 = I_ser * R2;
    expect(U1).toBeLessThan(U);
    expect(U2).toBeLessThan(U);
  });

  it('параллель: I1 + I2 > max(I1, I2) (сумма больше каждой ветви)', () => {
    const I1 = current(U, R1);
    const I2 = current(U, R2);
    expect(I1 + I2).toBeGreaterThan(Math.max(I1, I2));
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`seriesResistance is not a function`).

Run (Git Bash): `npm --workspace=@labosfera/kit-3-circuits run test -- CircuitModel`

- [ ] **Step 3: Реализация** — добавить в `kit-3-circuits/src/physics/circuit/CircuitModel.ts` (после `wireResistance`, перед концом файла). Использовать существующий `guard(...xs: number[])` helper из начала файла:

```ts
/**
 * Суммарное сопротивление последовательного соединения: R = ΣR_i.
 * Опыт 3.8 «Правило напряжений» — U = U1 + U2.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.8; КОДИФ §1.29.
 */
export function seriesResistance(...R_Ohm: number[]): number {
  if (R_Ohm.length === 0) throw new RangeError('seriesResistance: нужен хотя бы один резистор');
  guard(...R_Ohm);
  for (const r of R_Ohm) {
    if (r <= 0) throw new RangeError(`seriesResistance: R должен быть >0, получено ${r}`);
  }
  return R_Ohm.reduce((sum, r) => sum + r, 0);
}

/**
 * Суммарное сопротивление параллельного соединения: R = 1/Σ(1/R_i).
 * Опыт 3.9 «Правило токов» — I = I1 + I2.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.9; КОДИФ §1.29.
 */
export function parallelResistance(...R_Ohm: number[]): number {
  if (R_Ohm.length === 0) throw new RangeError('parallelResistance: нужен хотя бы один резистор');
  guard(...R_Ohm);
  for (const r of R_Ohm) {
    if (r <= 0) throw new RangeError(`parallelResistance: R должен быть >0, получено ${r}`);
  }
  return 1 / R_Ohm.reduce((sum, r) => sum + 1 / r, 0);
}
```

- [ ] **Step 4: Прогнать — PASS.**

Run: `npm --workspace=@labosfera/kit-3-circuits run test -- CircuitModel`

- [ ] **Step 5: Commit.**

```bash
git add experiments/kit-3-circuits/src/physics/circuit/CircuitModel.ts \
        experiments/kit-3-circuits/src/physics/circuit/__tests__/CircuitModel.test.ts
git commit -m "feat(kit-3): CircuitModel.seriesResistance + parallelResistance + фаззинг (опыты 3.8–3.9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: SERIES_VOLTAGE_SPEC + PARALLEL_CURRENT_SPEC

**Files:**
- Modify: `experiments/_shared-spa/src/lib/journal/specs.ts` (добавить после `R_RHO_SPEC`; добавить в `ALL_SPECS`)
- Create: `experiments/_shared-spa/__tests__/connection-specs.test.ts`

**Interfaces:**
- Consumes: `ColumnSpec`, `JournalSpec` из `./types` (уже импортированы в specs.ts).
- Produces:
  - `export const SERIES_VOLTAGE_SPEC: JournalSpec` (experimentId `'3.8'`, kitId `'kit-3'`). Колонки: `idx`(meta,int), `point`(meta,«Точка»), `U_V`(direct,'В',fixed2).
  - `export const PARALLEL_CURRENT_SPEC: JournalSpec` (experimentId `'3.9'`). Колонки: `idx`(meta,int), `point`(meta,«Точка»), `I_A`(direct,'А',fixed2).
  - Оба в `ALL_SPECS`.

- [ ] **Step 1: Падающие тесты** — создать `experiments/_shared-spa/__tests__/connection-specs.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { SERIES_VOLTAGE_SPEC, PARALLEL_CURRENT_SPEC, ALL_SPECS } from '../src/lib/journal/specs';

describe('SERIES_VOLTAGE_SPEC (3.8)', () => {
  it('experimentId = "3.8", kitId = "kit-3"', () => {
    expect(SERIES_VOLTAGE_SPEC.experimentId).toBe('3.8');
    expect(SERIES_VOLTAGE_SPEC.kitId).toBe('kit-3');
  });
  it('зарегистрирован в ALL_SPECS', () => {
    expect(ALL_SPECS).toContain(SERIES_VOLTAGE_SPEC);
  });
  it('колонки: idx(meta), point(meta), U_V(direct)', () => {
    const cols = SERIES_VOLTAGE_SPEC.columns;
    expect(cols.find((c) => c.key === 'idx')?.source).toBe('meta');
    expect(cols.find((c) => c.key === 'point')?.source).toBe('meta');
    expect(cols.find((c) => c.key === 'U_V')?.source).toBe('direct');
    expect(cols.find((c) => c.key === 'U_V')?.unit).toBe('В');
  });
  it('нет derived-колонок (правило — в result-panel)', () => {
    const derived = SERIES_VOLTAGE_SPEC.columns.filter((c) => c.source === 'derived');
    expect(derived.length).toBe(0);
  });
  it('колонка U_V format=fixed2', () => {
    const col = SERIES_VOLTAGE_SPEC.columns.find((c) => c.key === 'U_V')!;
    expect(col.format).toBe('fixed2');
  });
});

describe('PARALLEL_CURRENT_SPEC (3.9)', () => {
  it('experimentId = "3.9", kitId = "kit-3"', () => {
    expect(PARALLEL_CURRENT_SPEC.experimentId).toBe('3.9');
    expect(PARALLEL_CURRENT_SPEC.kitId).toBe('kit-3');
  });
  it('зарегистрирован в ALL_SPECS', () => {
    expect(ALL_SPECS).toContain(PARALLEL_CURRENT_SPEC);
  });
  it('колонки: idx(meta), point(meta), I_A(direct)', () => {
    const cols = PARALLEL_CURRENT_SPEC.columns;
    expect(cols.find((c) => c.key === 'idx')?.source).toBe('meta');
    expect(cols.find((c) => c.key === 'point')?.source).toBe('meta');
    expect(cols.find((c) => c.key === 'I_A')?.source).toBe('direct');
    expect(cols.find((c) => c.key === 'I_A')?.unit).toBe('А');
  });
  it('нет derived-колонок', () => {
    const derived = PARALLEL_CURRENT_SPEC.columns.filter((c) => c.source === 'derived');
    expect(derived.length).toBe(0);
  });
  it('колонка I_A format=fixed2', () => {
    const col = PARALLEL_CURRENT_SPEC.columns.find((c) => c.key === 'I_A')!;
    expect(col.format).toBe('fixed2');
  });
});
```

- [ ] **Step 2: Прогнать — FAIL.**

Run: `npm --workspace=@labosfera/shared-spa run test -- connection-specs`

- [ ] **Step 3: Реализация** — добавить в `experiments/_shared-spa/src/lib/journal/specs.ts` (после `R_RHO_SPEC`, перед `ALL_SPECS`):

```ts
/**
 * Опыт 3.8 «Правило напряжений для последовательного соединения» — U = U1 + U2.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.8
 * «последовательное соединение проводников: U = U1 + U2»; КОДИФ §1.29.
 * Методика: ученик снимает U1 (across R1), U2 (across R2), U_общ (across R1+R2)
 * вольтметром в 3 позициях; правило U1+U2=U_общ — в result-panel.
 * Нет derived-колонок (правило — не построчная проверка, а итоговый вывод).
 */
export const SERIES_VOLTAGE_SPEC: JournalSpec = {
  experimentId: '3.8',
  kitId: 'kit-3',
  columns: [
    { key: 'idx',   label: '№',     source: 'meta', format: 'int' },
    { key: 'point', label: 'Точка', source: 'meta' },
    { key: 'U_V',   label: 'U, В',  source: 'direct', unit: 'В', format: 'fixed2' },
  ],
};

/**
 * Опыт 3.9 «Правило токов для параллельного соединения» — I = I1 + I2.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.9
 * «параллельное соединение проводников: I = I1 + I2»; КОДИФ §1.29.
 * Методика: ученик снимает I1 (ветвь R1), I2 (ветвь R2), I_общ (главная линия)
 * амперметром в 3 позициях; правило I1+I2=I_общ — в result-panel.
 */
export const PARALLEL_CURRENT_SPEC: JournalSpec = {
  experimentId: '3.9',
  kitId: 'kit-3',
  columns: [
    { key: 'idx',   label: '№',     source: 'meta', format: 'int' },
    { key: 'point', label: 'Точка', source: 'meta' },
    { key: 'I_A',   label: 'I, А',  source: 'direct', unit: 'А', format: 'fixed2' },
  ],
};
```

Добавить `SERIES_VOLTAGE_SPEC,` и `PARALLEL_CURRENT_SPEC,` в `ALL_SPECS` (после `R_RHO_SPEC,`):

```ts
export const ALL_SPECS: ReadonlyArray<JournalSpec> = [
  // ... существующие ...
  R_LENGTH_SPEC,
  R_AREA_SPEC,
  R_RHO_SPEC,
  SERIES_VOLTAGE_SPEC,    // ← добавить
  PARALLEL_CURRENT_SPEC,  // ← добавить
];
```

- [ ] **Step 4: Прогнать — PASS.**

Run: `npm --workspace=@labosfera/shared-spa run test`

- [ ] **Step 5: Commit.**

```bash
git add experiments/_shared-spa/src/lib/journal/specs.ts \
        experiments/_shared-spa/__tests__/connection-specs.test.ts
git commit -m "feat(journal): SERIES_VOLTAGE_SPEC (3.8) + PARALLEL_CURRENT_SPEC (3.9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Компонент lab-connection-board

**Files:**
- Create: `kit-3-circuits/src/ui/components/lab-connection-board.ts`
- Modify (добавить блок): `kit-3-circuits/src/ui/components/__tests__/components.test.ts`

**Interfaces:**
- Produces: custom element `<lab-connection-board topology="series"|"parallel">`.
  - `static observedAttributes = ['topology']`; при смене атрибута перерисовывает SVG + пересчитывает слоты.
  - API идентичен `lab-circuit-board`: `getSlotRect(id: string): DOMRect`; `setCurrentAnimating(on: boolean): void`.
  - `connectedCallback`: `role="img"` на host, `aria-label` соответствующий топологии; `<title>` в SVG.
  - Shadow CSS: `svg[hidden]{display:none}`.

**Слот-контракт (data-slot id):**

*topology="series" (3.8 — последовательное):*
- `source` — источник питания
- `key` — ключ
- `ammeter` — амперметр (fixed, в главной линии; последовательно R1+R2)
- `r1` — резистор R1 (inline-последовательно)
- `r2` — резистор R2 (inline-последовательно, правее R1)
- `v-pos-r1` — позиция вольтметра: параллельно R1
- `v-pos-r2` — позиция вольтметра: параллельно R2
- `v-pos-total` — позиция вольтметра: параллельно всей последовательной цепи (R1+R2)

*topology="parallel" (3.9 — параллельное):*
- `source` — источник питания
- `key` — ключ
- `voltmeter` — вольтметр (fixed, параллельно обоим ветвям, на узлах)
- `r1` — резистор R1 (верхняя ветвь)
- `r2` — резистор R2 (нижняя ветвь)
- `a-pos-r1` — позиция амперметра: в ветви R1
- `a-pos-r2` — позиция амперметра: в ветви R2
- `a-pos-main` — позиция амперметра: в главной линии (до разветвления)

**Что рисует SVG (структурные требования, координаты — за реализатором):**

*series:* два рельса (верхний — главная линия, нижний — обратный провод); в верхней линии последовательно source→key→ammeter→R1→R2→правый столб; три пунктирных гнезда вольтметра: v-pos-r1 параллельно R1, v-pos-r2 параллельно R2, v-pos-total параллельно (R1+R2) — все три соединены вертикальными пунктирными линиями к верхнему и нижнему рельсу; подписи позиций «across R1»/«across R2»/«across total».

*parallel:* два вертикальных узла (левый «+», правый «−»); между ними — параллельные ветви: верхняя (a-pos-r1 + r1) и нижняя (a-pos-r2 + r2); источник и ключ — в главной линии; a-pos-main — в главной линии (до развилки); вольтметр (fixed) — параллельно узлам (пунктир); узловые точки (кружки) в местах разветвления.

Геометрия констант SVG (по образцу `lab-circuit-board`): `SVG_W=520`, `SVG_H=340`, `SLOT_W=72`, `SLOT_H=48`. Точные координаты x/y — проектирует реализатор.

**Реализация:** Создать `lab-connection-board.ts` по образцу `lab-circuit-board.ts` (прочитать его перед написанием). Отличия:
- `static observedAttributes = ['topology']`.
- Два отдельных блока SVG-шаблона (для series и для parallel) внутри одного `<template>` — переключать через `hidden` на корневом `<svg>` или двух `<g>`. Проще сделать два независимых `<svg>` (`id="svg-series"` и `id="svg-parallel"`) в одном shadow-root; показывать нужный через `svg[hidden]` rule.
- `attributeChangedCallback('topology', ...)` переключает видимость SVG и обновляет `aria-label`.
- `getSlotRect(id)` ищет в активном SVG `[data-slot="${id}"]` через `shadowRoot.querySelector`.
- `setCurrentAnimating(on)` устанавливает/снимает атрибут `current-animating` на host (анимация через `:host([current-animating])` в CSS, аналогично `lab-circuit-board`).

- [ ] **Step 1: Падающие тесты** — добавить в `kit-3-circuits/src/ui/components/__tests__/components.test.ts`:

```ts
import '../lab-connection-board';

describe('lab-connection-board — topology="series"', () => {
  function makeBoard(topology: string) {
    const el = document.createElement('lab-connection-board') as any;
    el.setAttribute('topology', topology);
    document.body.appendChild(el);
    return el;
  }

  afterEach(() => {
    document.querySelectorAll('lab-connection-board').forEach((el) => el.remove());
  });

  it('topology="series": присутствуют слоты source, key, ammeter, r1, r2', () => {
    const el = makeBoard('series');
    const ids = ['source', 'key', 'ammeter', 'r1', 'r2'];
    for (const id of ids) {
      expect(
        el.shadowRoot.querySelector(`[data-slot="${id}"]`),
        `слот ${id} должен быть в series`,
      ).not.toBeNull();
    }
  });

  it('topology="series": присутствуют слоты v-pos-r1, v-pos-r2, v-pos-total', () => {
    const el = makeBoard('series');
    for (const id of ['v-pos-r1', 'v-pos-r2', 'v-pos-total']) {
      expect(
        el.shadowRoot.querySelector(`[data-slot="${id}"]`),
        `слот ${id} должен быть в series`,
      ).not.toBeNull();
    }
  });

  it('topology="series": НЕТ слотов a-pos-* (они только в parallel)', () => {
    const el = makeBoard('series');
    expect(el.shadowRoot.querySelector('[data-slot="a-pos-r1"]')).toBeNull();
    expect(el.shadowRoot.querySelector('[data-slot="a-pos-main"]')).toBeNull();
  });

  it('topology="parallel": присутствуют слоты source, key, voltmeter, r1, r2', () => {
    const el = makeBoard('parallel');
    for (const id of ['source', 'key', 'voltmeter', 'r1', 'r2']) {
      expect(
        el.shadowRoot.querySelector(`[data-slot="${id}"]`),
        `слот ${id} должен быть в parallel`,
      ).not.toBeNull();
    }
  });

  it('topology="parallel": присутствуют слоты a-pos-r1, a-pos-r2, a-pos-main', () => {
    const el = makeBoard('parallel');
    for (const id of ['a-pos-r1', 'a-pos-r2', 'a-pos-main']) {
      expect(
        el.shadowRoot.querySelector(`[data-slot="${id}"]`),
        `слот ${id} должен быть в parallel`,
      ).not.toBeNull();
    }
  });

  it('topology="parallel": НЕТ слотов v-pos-* (они только в series)', () => {
    const el = makeBoard('parallel');
    expect(el.shadowRoot.querySelector('[data-slot="v-pos-r1"]')).toBeNull();
    expect(el.shadowRoot.querySelector('[data-slot="v-pos-total"]')).toBeNull();
  });

  it('getSlotRect(существующий слот) возвращает DOMRect', () => {
    const el = makeBoard('series');
    const rect = el.getSlotRect('ammeter');
    expect(rect).toBeInstanceOf(DOMRect);
  });

  it('getSlotRect(несуществующий слот) возвращает пустой DOMRect (не бросает)', () => {
    const el = makeBoard('series');
    const rect = el.getSlotRect('nonexistent-slot');
    expect(rect).toBeInstanceOf(DOMRect);
    expect(rect.width).toBe(0);
  });

  it('setCurrentAnimating(true) ставит атрибут current-animating', () => {
    const el = makeBoard('series');
    el.setCurrentAnimating(true);
    expect(el.hasAttribute('current-animating')).toBe(true);
  });

  it('setCurrentAnimating(false) снимает атрибут', () => {
    const el = makeBoard('series');
    el.setCurrentAnimating(true);
    el.setCurrentAnimating(false);
    expect(el.hasAttribute('current-animating')).toBe(false);
  });

  it('смена topology через атрибут переключает активный SVG', () => {
    const el = makeBoard('series');
    // В series активен svg-series, svg-parallel скрыт
    const svgSeries = el.shadowRoot.querySelector('#svg-series');
    const svgParallel = el.shadowRoot.querySelector('#svg-parallel');
    expect(svgSeries?.hidden).toBe(false);
    expect(svgParallel?.hidden).toBe(true);

    el.setAttribute('topology', 'parallel');
    expect(svgSeries?.hidden).toBe(true);
    expect(svgParallel?.hidden).toBe(false);
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`lab-connection-board` не определён).

Run: `npm --workspace=@labosfera/kit-3-circuits run test -- components`

- [ ] **Step 3: Реализация** — создать `kit-3-circuits/src/ui/components/lab-connection-board.ts`.

Эталон структуры файла — `lab-circuit-board.ts` (прочитать перед написанием). Сохранить те же: `const template = document.createElement('template')`, `attachShadow({mode:'open'})`, `static observedAttributes`, `connectedCallback`, `getSlotRect(id)`, `setCurrentAnimating(on)`, `customElements.define(...)`. Shadow CSS обязательно включает `svg[hidden]{display:none}`.

Скелет SVG для `id="svg-series"` (последовательное): горизонтальная цепь source→key→ammeter→r1→r2 на верхнем рельсе; нижний рельс — обратный провод; три пунктирных вертикальных разветвления к v-pos-r1/v-pos-r2/v-pos-total. Каждое гнездо — `<g data-slot="ID" class="circuit-slot"><rect class="slot-rect" .../><text class="slot-label slot-label-top">...</text></g>` (тот же CSS-класс `slot-rect`/`slot-label` что в `lab-circuit-board`).

Скелет SVG для `id="svg-parallel"` (параллельное): два узла (левый — «+», правый — «−»); source и key в главной линии (слева от левого узла); a-pos-main в главной линии между key и узлом; от левого узла вниз расходятся две ветви — верхняя (a-pos-r1→r1) и нижняя (a-pos-r2→r2) — сходятся в правом узле; вольтметр (fixed slot `voltmeter`) — на пунктирном проводе параллельно узлам.

Переключение активного SVG в `#update(topology)`:
```ts
#update(topology: string): void {
  const svgSer = this.shadowRoot!.querySelector<SVGSVGElement>('#svg-series');
  const svgPar = this.shadowRoot!.querySelector<SVGSVGElement>('#svg-parallel');
  if (svgSer) svgSer.hidden = topology !== 'series';
  if (svgPar) svgPar.hidden = topology !== 'parallel';
  const label = topology === 'series'
    ? 'Монтажная панель — последовательное соединение R1 и R2 (опыт 3.8)'
    : 'Монтажная панель — параллельное соединение R1 и R2 (опыт 3.9)';
  this.setAttribute('aria-label', label);
}
```

`getSlotRect(id)` ищет только в активном SVG (`topology !== 'parallel'` → ищем в `#svg-series`, иначе в `#svg-parallel`):
```ts
getSlotRect(id: string): DOMRect {
  const topology = this.getAttribute('topology') ?? 'series';
  const svgId = topology === 'parallel' ? '#svg-parallel' : '#svg-series';
  const slotEl = this.shadowRoot?.querySelector<SVGGElement>(`${svgId} [data-slot="${id}"]`);
  return slotEl ? slotEl.getBoundingClientRect() : new DOMRect(0, 0, 0, 0);
}
```

В конце файла: `customElements.define('lab-connection-board', LabConnectionBoard);`

- [ ] **Step 4: Прогнать — PASS.**

Run: `npm --workspace=@labosfera/kit-3-circuits run test -- components`

- [ ] **Step 5: Commit.**

```bash
git add experiments/kit-3-circuits/src/ui/components/lab-connection-board.ts \
        experiments/kit-3-circuits/src/ui/components/__tests__/components.test.ts
git commit -m "feat(kit-3): компонент lab-connection-board (series/parallel topology, 8 слотов)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Экран connections (подвижный прибор + журнал + result-panel)

**Files:**
- Create: `kit-3-circuits/src/screens/connections/ConnectionsScreen.ts`
- Create: `kit-3-circuits/src/screens/connections/ConnectionsExperiment.ts`
- Create: `kit-3-circuits/src/screens/connections/template.html`
- Create: `kit-3-circuits/src/styles/connections-experiment.css`
- Modify: `kit-3-circuits/src/main.ts`
- Create (test): `kit-3-circuits/src/screens/connections/__tests__/connections.test.ts`

**Interfaces:**
- Consumes: `seriesResistance`, `parallelResistance`, `current` из `@physics/circuit/CircuitModel`; `SERIES_VOLTAGE_SPEC`, `PARALLEL_CURRENT_SPEC` из journal; `CircuitTopology`, `CircuitAssembly`, `CircuitDragController`; `lab-connection-board`, `lab-resistor`, `lab-voltmeter`, `lab-ammeter`, `lab-power-source`, `lab-key`.
- Produces: `window.connectionsExperiment` тест-API:
  - `setActiveTask('A-series' | 'B-parallel'): void`
  - `placeInSlot(slotId: ConnectionSlotId, equipmentId: ConnectionEquipmentId): boolean`
  - `setVoltage(v: number): void`
  - `setKeyClosed(closed: boolean): void`
  - `recordMeasurement(): void`
  - `reset(clearData?: boolean): void`
  - геттеры: `measurements: ReadonlyArray<ConnectionMeasurement>`, `voltage: number`, `activeTask: ConnectionTaskId`, `movableInstrumentPosition(): string | null`

**Типы (определить в ConnectionsExperiment.ts):**

```ts
export type ConnectionTaskId = 'A-series' | 'B-parallel';

/** Все слоты на board (серия + параллель объединены) */
export type ConnectionSlotId =
  | 'source' | 'key' | 'r1' | 'r2'
  | 'ammeter'                          // fixed для series; position для parallel
  | 'voltmeter'                         // fixed для parallel; position для series
  | 'v-pos-r1' | 'v-pos-r2' | 'v-pos-total'  // position-слоты вольтметра (series)
  | 'a-pos-r1' | 'a-pos-r2' | 'a-pos-main';  // position-слоты амперметра (parallel)

export type ConnectionEquipmentId =
  | 'power-source' | 'voltmeter' | 'ammeter' | 'key' | 'r1' | 'r2';

export type EqKind = 'power-source' | 'voltmeter' | 'ammeter' | 'resistor' | 'key';

export interface ConnectionMeasurement {
  readonly id: string;
  readonly timestamp: number;
  readonly task: ConnectionTaskId;
  readonly point: 'R1' | 'R2' | 'total';   // позиция подвижного прибора
  readonly value: number;                    // U_V для series, I_A для parallel
}
```

**Топологии CircuitTopology (SLOTS_SERIES / SLOTS_PARALLEL):**

```ts
// Серия: source, key, ammeter (fixed/series), r1 (series), r2 (series),
//         v-pos-r1 (parallel), v-pos-r2 (parallel), v-pos-total (parallel)
const SLOTS_SERIES = [
  { id: 'source',      role: 'source' as const,   accepts: ['power-source'] },
  { id: 'key',         role: 'key' as const,       accepts: ['key'] },
  { id: 'ammeter',     role: 'series' as const,    accepts: ['ammeter'] },
  { id: 'r1',          role: 'series' as const,    accepts: ['resistor'] },
  { id: 'r2',          role: 'series' as const,    accepts: ['resistor'] },
  { id: 'v-pos-r1',    role: 'parallel' as const,  accepts: ['voltmeter'] },
  { id: 'v-pos-r2',    role: 'parallel' as const,  accepts: ['voltmeter'] },
  { id: 'v-pos-total', role: 'parallel' as const,  accepts: ['voltmeter'] },
] as const;

// Параллель: source, key, voltmeter (fixed/parallel), r1 (series в ветви), r2 (series в ветви),
//             a-pos-r1 (series), a-pos-r2 (series), a-pos-main (series)
const SLOTS_PARALLEL = [
  { id: 'source',     role: 'source' as const,   accepts: ['power-source'] },
  { id: 'key',        role: 'key' as const,       accepts: ['key'] },
  { id: 'voltmeter',  role: 'parallel' as const,  accepts: ['voltmeter'] },
  { id: 'r1',         role: 'series' as const,    accepts: ['resistor'] },
  { id: 'r2',         role: 'series' as const,    accepts: ['resistor'] },
  { id: 'a-pos-r1',   role: 'series' as const,    accepts: ['ammeter'] },
  { id: 'a-pos-r2',   role: 'series' as const,    accepts: ['ammeter'] },
  { id: 'a-pos-main', role: 'series' as const,    accepts: ['ammeter'] },
] as const;
```

**Validate — только нужные слоты заполнены (переопределить `isCircuitReady`):**

`CircuitTopology.validate()` требует ВСЕ слоты заполнены. Для connections подвижный прибор занимает только одну position-позицию из трёх, поэтому нельзя требовать все 8 слотов. Решение: не использовать `topology.validate()` для проверки «готова ли цепь», а сделать локальный хелпер `#isCircuitReady()` в `ConnectionsExperiment`:

```ts
#isCircuitReady(): boolean {
  const st = this.#store.get();
  const task = st.activeTask;
  // Обязательные слоты (не position)
  const required: ConnectionSlotId[] =
    task === 'A-series'
      ? ['source', 'key', 'ammeter', 'r1', 'r2']
      : ['source', 'key', 'voltmeter', 'r1', 'r2'];
  for (const id of required) {
    if (!st.placed[id]) return false;
  }
  // Хотя бы одна position-позиция подвижного прибора занята
  return this.#movablePosition() !== null;
}
```

**Логика подвижного прибора (ключевое решение):**

Для series: подвижный прибор = voltmeter; position-слоты = `['v-pos-r1','v-pos-r2','v-pos-total']`.
Для parallel: подвижный прибор = ammeter; position-слоты = `['a-pos-r1','a-pos-r2','a-pos-main']`.

При drag в новую position-позицию — убрать прибор из других position-слотов:

```ts
// В onDrop для position-slot:
onDrop: ({ equipmentId }) => {
  const kind = this.#kindForEquipment(equipmentId as ConnectionEquipmentId);
  if (!kind) return false;

  // Убрать из других position-слотов (один прибор — одна позиция)
  const posSlots = this.#positionSlots();
  for (const pid of posSlots) {
    if (pid !== slotId && this.#topology.slotFilledBy(pid) !== null) {
      this.#topology.remove(pid);
      this.#store.update((st) => {
        const placed = { ...st.placed };
        delete placed[pid as ConnectionSlotId];
        return { placed };
      });
      // Вернуть карточку в available
      const prevCard = this.#cardByEquipmentId.get(
        this.#store.get().placed[pid as ConnectionSlotId]?.equipmentId as ConnectionEquipmentId,
      );
      if (prevCard) {
        prevCard.setAttribute('status', 'available');
        prevCard.removeAttribute('data-placed');
      }
    }
  }

  const ok = this.#topology.place(slotId, kind);
  if (!ok) return false;
  // ... обновить store + card ...
  return true;
},
```

**`movableInstrumentPosition()` — публичный геттер для теста и result-panel:**

```ts
#movablePosition(): 'R1' | 'R2' | 'total' | null {
  const st = this.#store.get();
  const task = st.activeTask;
  if (task === 'A-series') {
    if (st.placed['v-pos-r1'])    return 'R1';
    if (st.placed['v-pos-r2'])    return 'R2';
    if (st.placed['v-pos-total']) return 'total';
  } else {
    if (st.placed['a-pos-r1'])   return 'R1';
    if (st.placed['a-pos-r2'])   return 'R2';
    if (st.placed['a-pos-main']) return 'total';
  }
  return null;
}
movableInstrumentPosition(): string | null { return this.#movablePosition(); }
```

**`recordMeasurement()`:**

```ts
recordMeasurement(): void {
  const st = this.#store.get();
  if (!this.#isCircuitReady()) return;
  if (!st.keyClosed) return;

  const pos = this.#movablePosition();
  if (!pos) return;

  const U = st.voltage;
  const R1 = 4.7;
  const R2 = 5.7;
  let value: number;

  if (st.activeTask === 'A-series') {
    // Вольтметр в позиции pos; ток в главной линии = U / (R1+R2)
    const R_ser = seriesResistance(R1, R2);
    const I_ser = current(U, R_ser);
    if (pos === 'R1')    value = I_ser * R1;
    else if (pos === 'R2') value = I_ser * R2;
    else                  value = U; // total
  } else {
    // Амперметр в позиции pos; U = U_источника
    if (pos === 'R1')    value = current(U, R1);
    else if (pos === 'R2') value = current(U, R2);
    else                  value = current(U, R1) + current(U, R2); // total
  }

  const measurement: ConnectionMeasurement = {
    id: `cm-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`,
    timestamp: Date.now(),
    task: st.activeTask,
    point: pos,
    value,
  };

  this.#store.update((s) => ({ measurements: [...s.measurements, measurement] }));
  this.#lastRecordedSignature = this.#pendingSignature();
  this.#refreshUi();
}
```

**`#pendingSignature()`:** `${st.activeTask}-${st.voltage.toFixed(2)}-${this.#movablePosition() ?? ''}`

**Result-panel (правило U1+U2≈U_общ / I1+I2≈I_общ):** активируется когда для активной задачи есть measurements по всем трём точкам (R1, R2, total). Допуск ~2%:

```ts
#refreshResultPanel(taskMeasurements: ConnectionMeasurement[]): void {
  const panel = this.#refs.resultPanel;
  const task = this.#store.get().activeTask;

  const byPoint = new Map<string, number>();
  for (const m of taskMeasurements) {
    if (!byPoint.has(m.point)) byPoint.set(m.point, m.value);
  }

  if (!byPoint.has('R1') || !byPoint.has('R2') || !byPoint.has('total')) {
    panel.hidden = true;
    return;
  }

  const v1 = byPoint.get('R1')!;
  const v2 = byPoint.get('R2')!;
  const vTotal = byPoint.get('total')!;
  const sum = v1 + v2;
  const rel = Math.abs(sum - vTotal) / (Math.abs(vTotal) || 1);
  const ok = rel <= 0.02;
  const checkmark = ok ? '✓' : '✗';

  let text: string;
  if (task === 'A-series') {
    text = `U1 + U2 = ${v1.toFixed(2).replace('.', ',')} + ${v2.toFixed(2).replace('.', ',')} = ${sum.toFixed(2).replace('.', ',')} В ≈ U(общ) = ${vTotal.toFixed(2).replace('.', ',')} В → правило напряжений выполняется ${checkmark}`;
  } else {
    text = `I1 + I2 = ${v1.toFixed(2).replace('.', ',')} + ${v2.toFixed(2).replace('.', ',')} = ${sum.toFixed(2).replace('.', ',')} А ≈ I(общ) = ${vTotal.toFixed(2).replace('.', ',')} А → правило токов выполняется ${checkmark}`;
  }

  panel.hidden = false;
  if (text !== this.#lastAnnouncedConclusion) {
    this.#lastAnnouncedConclusion = text;
    panel.textContent = text;
  }
}
```

**Переключение задачи (`setActiveTask`):** меняет `board.setAttribute('topology', ...)`, пересоздаёт `CircuitTopology` (новый набор слотов), перерегистрирует snap-зоны через `#rewireAssemblySlots`, применяет фильтр оборудования, сбрасывает цепь (сохраняя данные другой задачи).

**template.html** — копия `wire-resistance/template.html` с заменами:
- Task-кнопки: A `data-task="A-series"` «Последовательное (3.8)»; B `data-task="B-parallel"` «Параллельное (3.9)».
- `<lab-connection-board id="circuit-board" topology="series">` вместо `<lab-circuit-board>`.
- Оборудование: source, voltmeter (`range="6"` «0–6 В»), ammeter (`range="3"` «0–3 А»), key, r1 (`<lab-resistor variant="R1">`), r2 (`<lab-resistor variant="R2">`).
- Секции для задачи A: `<section class="equipment-group instrument-group--A-series">` — содержит карточки source, key, ammeter (фиксированный), r1, r2, voltmeter (подвижный). Секция B: `<section class="equipment-group instrument-group--B-parallel">` — source, key, voltmeter (фиксированный), r1, r2, ammeter (подвижный). При фильтре переключается `section.hidden`.
- `#result-panel` с `aria-live="polite"` `aria-atomic="true"`.
- `id="drag-overlay"`, `#live-region`, `#hint-bar`, `#reset-btn`, `#steps`, `#journal-host`, `#record-mode-slot`, `#record-pending-slot`, `#record-pending-btn`, `#voltage-input` (`max="6.0"`), `#voltage-readout`.

**ConnectionsScreen.ts** — фасад (по образцу `WireResistanceScreen.ts`):
- `meta = { id: 'connections', icon: 'connections', kicker: 'Опыт 3.8–3.9', label: 'Соединения проводников' }`.
- `mount(host)`: вставляет template, создаёт `ConnectionsExperiment(refs)`, присваивает `window.connectionsExperiment`.
- `unmount()`: вызывает `experiment.destroy()`, удаляет `window.connectionsExperiment`.

**main.ts** — добавить:
```ts
import { ConnectionsScreen } from '@screens/connections/ConnectionsScreen';
import './ui/components/lab-connection-board';
import './styles/connections-experiment.css';
// в массив screens[]:
new ConnectionsScreen(),
```

- [ ] **Step 1: Падающие тесты** — создать `kit-3-circuits/src/screens/connections/__tests__/connections.test.ts`:

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';

import '../../../ui/components/lab-power-source';
import '../../../ui/components/lab-voltmeter';
import '../../../ui/components/lab-ammeter';
import '../../../ui/components/lab-resistor';
import '../../../ui/components/lab-key';
import '../../../ui/components/lab-connection-board';
import '../../../ui/components/lab-equipment-card';

import { ConnectionsScreen } from '../ConnectionsScreen';
import { seriesResistance, parallelResistance, current } from '../../../physics/circuit/CircuitModel';

const R1 = 4.7;
const R2 = 5.7;

function mountScreen() {
  const host = document.createElement('div');
  document.body.appendChild(host);
  const screen = new ConnectionsScreen();
  screen.mount(host);
  const exp = (window as unknown as { connectionsExperiment: any }).connectionsExperiment;
  return { host, screen, exp };
}

function assembleBase(exp: any, task: 'A-series' | 'B-parallel') {
  exp.setActiveTask(task);
  exp.placeInSlot('source', 'power-source');
  exp.placeInSlot('key', 'key');
  exp.placeInSlot('r1', 'r1');
  exp.placeInSlot('r2', 'r2');
  if (task === 'A-series') {
    exp.placeInSlot('ammeter', 'ammeter');
  } else {
    exp.placeInSlot('voltmeter', 'voltmeter');
  }
}

describe('ConnectionsScreen — опыт 3.8 серия', () => {
  let screen: ConnectionsScreen;

  afterEach(() => { screen?.unmount(); });

  it('A-series: 3 позиции вольтметра дают U1<U_общ, U2<U_общ, U1+U2≈U_общ', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);

    const measure = (pos: string) => {
      exp.placeInSlot(pos, 'voltmeter');
      exp.recordMeasurement();
      const last = exp.measurements.at(-1);
      return last?.value as number;
    };

    const U1 = measure('v-pos-r1');
    const U2 = measure('v-pos-r2');
    const Utot = measure('v-pos-total');

    expect(U1).toBeLessThan(Utot);
    expect(U2).toBeLessThan(Utot);
    expect(U1 + U2).toBeCloseTo(Utot, 1);
  });

  it('A-series: значения U1, U2 соответствуют физике (I·R)', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    const U = 4.5;
    exp.setVoltage(U);
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);
    exp.placeInSlot('v-pos-r1', 'voltmeter');
    exp.recordMeasurement();
    const m = exp.measurements.at(-1)!;
    const R_ser = seriesResistance(R1, R2);
    const I_ser = current(U, R_ser);
    expect(m.value).toBeCloseTo(I_ser * R1, 3);
  });

  it('A-series: слайдер ≤ 6,0 (урок Фазы C)', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(7.5);
    expect(exp.voltage).toBeLessThanOrEqual(6.0);
  });

  it('reset(false) сохраняет measurements + сбрасывает цепь + сбрасывает #lastRecordedSignature', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);
    exp.placeInSlot('v-pos-r1', 'voltmeter');
    exp.recordMeasurement();
    const countBefore = exp.measurements.length;

    exp.reset(false);
    expect(exp.measurements.length).toBe(countBefore);

    // После reset(false) можно снова записать ту же позицию
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);
    exp.placeInSlot('v-pos-r1', 'voltmeter');
    exp.recordMeasurement();
    expect(exp.measurements.length).toBe(countBefore + 1);
  });
});

describe('ConnectionsScreen — опыт 3.9 параллель', () => {
  let screen: ConnectionsScreen;

  afterEach(() => { screen?.unmount(); });

  it('B-parallel: 3 позиции амперметра дают I1+I2≈I_общ', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'B-parallel');
    exp.setKeyClosed(true);

    const measure = (pos: string) => {
      exp.placeInSlot(pos, 'ammeter');
      exp.recordMeasurement();
      return (exp.measurements.at(-1)?.value as number);
    };

    const I1 = measure('a-pos-r1');
    const I2 = measure('a-pos-r2');
    const Itot = measure('a-pos-main');

    expect(I1 + I2).toBeCloseTo(Itot, 2);
  });

  it('B-parallel: I1 = U/R1, I2 = U/R2 по физике', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    const U = 4.5;
    exp.setVoltage(U);
    assembleBase(exp, 'B-parallel');
    exp.setKeyClosed(true);
    exp.placeInSlot('a-pos-r1', 'ammeter');
    exp.recordMeasurement();
    const m = exp.measurements.at(-1)!;
    expect(m.value).toBeCloseTo(current(U, R1), 3);
  });

  it('B-parallel: вольтметр range=6 (урок Фазы C)', () => {
    const { host, screen: s } = mountScreen(); screen = s;
    const vm = host.querySelector('lab-voltmeter');
    expect(vm?.getAttribute('range')).toBe('6');
  });
});

describe('ConnectionsScreen — result-panel правило', () => {
  let screen: ConnectionsScreen;

  afterEach(() => { screen?.unmount(); });

  it('A-series: result-panel появляется после 3 точек и содержит правило', () => {
    const { host, exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);
    exp.placeInSlot('v-pos-r1', 'voltmeter');
    exp.recordMeasurement();
    exp.placeInSlot('v-pos-r2', 'voltmeter');
    exp.recordMeasurement();
    exp.placeInSlot('v-pos-total', 'voltmeter');
    exp.recordMeasurement();

    const panel = host.querySelector('#result-panel') as HTMLElement;
    expect(panel.hidden).toBe(false);
    expect(panel.getAttribute('aria-live')).toBe('polite');
    expect(panel.textContent).toMatch(/правило напряжений/);
    expect(panel.textContent).toMatch(/[✓✗]/);
  });

  it('B-parallel: result-panel содержит правило токов после 3 точек', () => {
    const { host, exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'B-parallel');
    exp.setKeyClosed(true);
    exp.placeInSlot('a-pos-r1', 'ammeter');
    exp.recordMeasurement();
    exp.placeInSlot('a-pos-r2', 'ammeter');
    exp.recordMeasurement();
    exp.placeInSlot('a-pos-main', 'ammeter');
    exp.recordMeasurement();

    const panel = host.querySelector('#result-panel') as HTMLElement;
    expect(panel.hidden).toBe(false);
    expect(panel.textContent).toMatch(/правило токов/);
  });

  it('result-panel не ре-триггерит aria-live при повторных refreshUi без изменений', () => {
    const { host, exp, screen: s } = mountScreen(); screen = s;
    exp.setVoltage(4.5);
    assembleBase(exp, 'A-series');
    exp.setKeyClosed(true);
    for (const pos of ['v-pos-r1', 'v-pos-r2', 'v-pos-total']) {
      exp.placeInSlot(pos, 'voltmeter');
      exp.recordMeasurement();
    }
    const panel = host.querySelector('#result-panel') as HTMLElement;
    const before = panel.textContent;
    exp.setKeyClosed(false);
    exp.setKeyClosed(true);
    expect(panel.textContent).toBe(before);
  });
});

describe('ConnectionsScreen — подвижный прибор: один в позиции', () => {
  let screen: ConnectionsScreen;

  afterEach(() => { screen?.unmount(); });

  it('A-series: перемещение вольтметра в v-pos-r2 убирает его из v-pos-r1', () => {
    const { exp, screen: s } = mountScreen(); screen = s;
    assembleBase(exp, 'A-series');
    exp.placeInSlot('v-pos-r1', 'voltmeter');
    expect(exp.movableInstrumentPosition()).toBe('R1');

    exp.placeInSlot('v-pos-r2', 'voltmeter');
    expect(exp.movableInstrumentPosition()).toBe('R2');
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`ConnectionsScreen` не определён).

Run: `npm --workspace=@labosfera/kit-3-circuits run test -- connections`

- [ ] **Step 3a: Создать `template.html`** — по образцу `wire-resistance/template.html`. Обязательные `id`: `#steps`, `#hint-bar`, `#reset-btn`, `#stage`, `#circuit-board` (`<lab-connection-board topology="series">`), `#key-control`, `#key-btn`, `#key-btn-label`, `#measurement-panel`, `#measurement-toggle`, `#measurement-count`, `#measurement-body`, `#voltage-control`, `#voltage-input` (`max="6.0"`, `min="1.0"`, `step="0.5"`, `value="4.5"`), `#voltage-readout`, `#journal-empty`, `#journal-host`, `#record-mode-slot`, `#record-pending-slot`, `#record-pending-btn`, `#record-pending-summary`, `#result-panel` (`aria-live="polite"` `aria-atomic="true"`), `#drag-overlay`, `#live-region`.

Task-switcher в `<ol id="steps">`:
```html
<li class="step" data-task="A-series" data-state="active" tabindex="0" role="button" aria-current="true">
  <span class="step-num">A</span>
  <span class="step-label">Последовательное <small>(3.8)</small></span>
</li>
<li class="step" data-task="B-parallel" tabindex="0" role="button" aria-current="false">
  <span class="step-num">B</span>
  <span class="step-label">Параллельное <small>(3.9)</small></span>
</li>
```

Оборудование — карточки с `data-eq`, `data-draggable`, `data-dropzone`, `data-dropzone-id`. Резисторы: `data-eq="r1"` (`<lab-resistor variant="R1">`) с `data-draggable="resistor"`, `data-eq="r2"` (`<lab-resistor variant="R2">`). Вольтметр: `<lab-voltmeter range="6" value="0">`. Амперметр: `<lab-ammeter range="3" value="0">`. Карточки НЕ разбиваются на две секции для фильтрации по задаче (резисторы, source, key — общие для обеих задач; только подвижный прибор отличается). Фиксированный прибор задачи A (ammeter) и задачи B (voltmeter) должны быть в отдельных секциях с `data-task-instrument="A-series"` / `"B-parallel"` для фильтрации через `section.hidden`.

- [ ] **Step 3b: Создать `connections-experiment.css`** — копия `wire-resistance-experiment.css`, переименовать классы под `connections-` префикс если нужно (иначе просто скопировать — нет коллизий с другими экранами).

- [ ] **Step 3c: Создать `ConnectionsScreen.ts`** — по образцу `WireResistanceScreen.ts`. Имя файла шаблона: `./template.html`. `meta = { id: 'connections' as const, icon: 'connections' as const, kicker: 'Опыт 3.8–3.9', label: 'Соединения проводников' }`. `mount(host)` → `window.connectionsExperiment = experiment`.

- [ ] **Step 3d: Создать `ConnectionsExperiment.ts`** — по образцу `WireResistanceExperiment.ts` (прочитать перед написанием). Ключевые отличия:
  - Типы из Interfaces выше.
  - Две топологии (`SLOTS_SERIES` / `SLOTS_PARALLEL`); при `setActiveTask` пересоздать topology + `#rewireAssemblySlots`.
  - `#rewireAssemblySlots()` — зеркало `WireResistanceExperiment.#rewireAssemblySlots()` (те же `removeSnapZone('circuit-slot-X')` + `addSnapZone('circuit-slot-X')`).
  - onDrop для position-слотов: убрать прибор из других position-слотов перед размещением.
  - `#isCircuitReady()` вместо `topology.validate()`.
  - `recordMeasurement()` — вычислять value по физике как описано выше.
  - `#refreshResultPanel()` — как описано выше.
  - `#refreshEquipmentFilter()` — переключать `section[data-task-instrument]` hidden по activeTask.
  - `reset(false)` сбрасывает `#lastRecordedSignature=''` и `#lastAnnouncedConclusion=''`.
  - ФИПИ-якорь в docstring.

- [ ] **Step 3e: Правка `main.ts`** — добавить импорт + регистрацию как описано в Interfaces.

- [ ] **Step 4: Прогнать — PASS.**

Run: `npm --workspace=@labosfera/kit-3-circuits run test`

Убедиться: `wire-resistance` тесты не сломаны, `measurements` тесты не сломаны.

- [ ] **Step 5: typecheck.**

Run: `npm run typecheck --workspace=@labosfera/kit-3-circuits`

- [ ] **Step 6: Commit.**

```bash
git add experiments/kit-3-circuits/src/screens/connections/ \
        experiments/kit-3-circuits/src/styles/connections-experiment.css \
        experiments/kit-3-circuits/src/main.ts
git commit -m "feat(kit-3): экран connections — опыты 3.8/3.9, подвижный прибор в 3 позиции, result-panel правило

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: selfcheck-3-8-3-9 + каталог + гейты

**Files:**
- Create: `kit-3-circuits/selfcheck-3-8-3-9.mjs`
- Modify: `experiments/home/src/data/kits.ts`

**Interfaces:**
- Consumes: `window.connectionsExperiment` тест-API.
- Инфраструктура `dragInstrumentToSlot` — verbatim из `selfcheck-3-5-3-7.mjs` (прочитать файл; скопировать функции `pass`, `fail`, `skip`, `screenshot`, `dragInstrumentToSlot`, `checkOverlayDup`).

- [ ] **Step 1: selfcheck-3-8-3-9.mjs.**

Создать файл по образцу `selfcheck-3-5-3-7.mjs`. Порт: `5216` (или `process.env.SELFCHECK_BASE_URL`). Screen: `?screen=connections`. Скриншоты в `selfcheck-screenshots/3-8-3-9-*.png`.

`dragInstrumentToSlot` — идентична эталону (verbatim), но selector draggable включает `lab-resistor`:
```js
const draggableSel = [
  `${cardSel} > lab-power-source`,
  `${cardSel} > lab-voltmeter`,
  `${cardSel} > lab-ammeter`,
  `${cardSel} > lab-resistor`,
  `${cardSel} > lab-key`,
].join(', ');
```

`getSlotRect` вызывает `board.getSlotRect(slotId)` где `board = document.querySelector('#circuit-board')`.

**Сборка базовой цепи для задачи A (серия):**
```js
async function assembleSeriesBase(page, label) {
  const plan = [
    { slot: 'source',  eq: 'power-source' },
    { slot: 'key',     eq: 'key' },
    { slot: 'ammeter', eq: 'ammeter' },
    { slot: 'r1',      eq: 'r1' },
    { slot: 'r2',      eq: 'r2' },
  ];
  let placed = 0;
  for (const { slot, eq } of plan) {
    const ok = await dragInstrumentToSlot(page, eq, slot);
    if (ok) { placed++; pass(`${label}: D&D ${eq} → ${slot}`); }
    else fail(`${label}: D&D ${eq} → ${slot}`, 'дроп не сел');
  }
  return placed;
}
```

**Задача A (3.8 серия) — `runTaskA(page)`:**
1. `page.evaluate(() => window.connectionsExperiment?.setActiveTask('A-series'))`.
2. `assembleSeriesBase(page, '3.8-A')` → assert placed=5.
3. Замкнуть ключ: `page.evaluate(() => window.connectionsExperiment?.setKeyClosed(true))`.
4. Установить U=4.5: `page.evaluate(() => window.connectionsExperiment?.setVoltage(4.5))`.
5. Для каждой позиции `['v-pos-r1','v-pos-r2','v-pos-total']`:
   - mouse-D&D вольтметра в позицию: `await dragInstrumentToSlot(page, 'voltmeter', pos)`.
   - Запись: `await page.locator('#record-pending-btn').click()` (или API).
   - Прочитать `exp.measurements.at(-1).value`.
6. Assert: `U1 + U2 ≈ U_total` (допуск 2%), `U1 < U_total`, `U2 < U_total`.
7. Assert LCD вольтметра при U=6: `setVoltage(6.0)` → voltmeter `.lcd-text` ≈ '6,00'.
8. Overlay-dup=0.

**Задача B (3.9 параллель) — `runTaskB(page)`:**
1. `reset(true)` + `setActiveTask('B-parallel')`.
2. Собрать базу: `['source','key','voltmeter','r1','r2']` мышью.
3. Замкнуть ключ, U=4.5.
4. Для каждой позиции `['a-pos-r1','a-pos-r2','a-pos-main']`: mouse-D&D амперметра, запись.
5. Assert: `I1 + I2 ≈ I_total` (допуск 2%).
6. Assert LCD амперметра при I_total (U=4.5, R1=4.7, R2=5.7): ammeter `.lcd-text` ≈ I_total ± 5%.
7. Overlay-dup=0.

**Проверка слайдера:** `sliderMax = page.evaluate(() => document.querySelector('#voltage-input')?.getAttribute('max'))` → assert `'6.0'` или `'6'`.

Итог: `PASS n/0/0`.

- [ ] **Step 2: Запустить selfcheck** (dev в фоне на порту 5216).

```bash
# В одном терминале (фоновый процесс):
npm --workspace=@labosfera/kit-3-circuits run dev -- --host 127.0.0.1 --port 5216 --strictPort &
# Дождаться готовности, затем:
node experiments/kit-3-circuits/selfcheck-3-8-3-9.mjs
```

Добиться `STATUS: PASS`.

- [ ] **Step 3: Каталог** — в `experiments/home/src/data/kits.ts` выставить опытам `fipiTask:'3.8'` и `fipiTask:'3.9'` `done: true`. Kit-3 счётчик done: 7→9.

Run: `npm --workspace=@labosfera/home run test` — если падает пин числа done, обновить ожидаемое на 9.

- [ ] **Step 4: Полные гейты из корня** (Git Bash):

```bash
npm run typecheck && npm run lint && npm run test && npm run build
```

Ожидаемо: 0 ошибок, счёт тестов вырос (новые connections + specs + CircuitModel + home).

- [ ] **Step 5: Commit.**

```bash
git add -f experiments/kit-3-circuits/selfcheck-3-8-3-9.mjs
git add experiments/home/src/data/kits.ts
git commit -m "test(kit-3): selfcheck 3.8–3.9 + каталог done (kit-3 9/9) + гейты

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 6: Reality-check (контроллер, Playwright MCP)** — навигировать `?screen=connections`, задача A-series: собрать цепь, переставить вольтметр в 3 позиции, снять 3 показания; проверить result-panel содержит «правило напряжений» + ✓; скрин. Задача B-parallel: аналогично для амперметра + «правило токов». Проверить LCD приборов не замёрз (range=6/range=3). Расхождения → фикс-итерация. (Делает контроллер, не субагент.)

---

## Self-Review

### 1. Spec coverage (3.8/3.9)

| Требование | Task | Статус |
|---|---|---|
| 3.8 правило U=U1+U2, серия | T1 (физика) + T4 (экран) + T5 (selfcheck) | ✓ |
| 3.9 правило I=I1+I2, параллель | T1 (физика) + T4 (экран) + T5 (selfcheck) | ✓ |
| `seriesResistance` / `parallelResistance` с фаззингом | T1 | ✓ |
| `SERIES_VOLTAGE_SPEC` / `PARALLEL_CURRENT_SPEC` в `ALL_SPECS` | T2 | ✓ |
| `lab-connection-board` с topology series/parallel + 8 слотов | T3 | ✓ |
| Подвижный прибор в 3 позиции; при перемещении убрать из прочих | T4 (ConnectionsExperiment) | ✓ |
| Фиксированный прибор в своём слоте (ammeter для series, voltmeter для parallel) | T4 | ✓ |
| result-panel после 3 точек с допуском ~2% + aria-live | T4 | ✓ |
| Вольтметр range=6, слайдер max=6.0, clamp [1.0;6.0] (урок Фаза C) | T4 | ✓ |
| Амперметр range=3 (урок Фаза C) | T4 | ✓ |
| DRAG-зоны с префиксом `circuit-slot-` (урок Фазы C/D) | T4 | ✓ |
| `reset(false)` сбрасывает сигнатуру и вывод | T4 | ✓ |
| key-btn динамический aria-label | T4 | ✓ |
| Фильтр оборудования на section.hidden | T4 | ✓ |
| R1=4.7 Ом, R2=5.7 Ом (разные номиналы) | T4 | ✓ |
| selfcheck mouse-D&D, card[data-placed], инварианты | T5 | ✓ |
| Каталог 3.8/3.9 done=true (kit-3 9/9) | T5 | ✓ |
| Полные гейты из корня | T5 | ✓ |

### 2. Placeholder scan

Проверено — в плане нет «TBD», «TODO», «аналогично Task N». Физические формулы с конкретными числами (R1=4.7, R2=5.7). Код функций `seriesResistance`/`parallelResistance` полный. Код `recordMeasurement`, `#movablePosition`, `#refreshResultPanel`, `#isCircuitReady` полный. Тесты полные с реальными assert-значениями. Selfcheck содержит конкретный сценарий по шагам.

SVG-геометрия скелетов `lab-connection-board` намеренно оставлена на усмотрение реализатора (структурные требования + приёмка через тесты слотов и `getSlotRect`). Это не placeholder — это осознанное разделение: «что рисуется» специфицировано, «где именно» — craft реализатора.

### 3. Type consistency

| Имя | Определено в | Использовано в |
|---|---|---|
| `seriesResistance(...R_Ohm: number[]): number` | T1 CircuitModel.ts | T4 ConnectionsExperiment.ts |
| `parallelResistance(...R_Ohm: number[]): number` | T1 CircuitModel.ts | T4 ConnectionsExperiment.ts |
| `ConnectionTaskId = 'A-series' \| 'B-parallel'` | T4 ConnectionsExperiment.ts | T4 тест, T5 selfcheck |
| `ConnectionSlotId` (объединение всех слотов) | T4 ConnectionsExperiment.ts | T4 placeInSlot тест |
| `ConnectionEquipmentId` | T4 ConnectionsExperiment.ts | T4 тест |
| `ConnectionMeasurement.point: 'R1'\|'R2'\|'total'` | T4 | T4 result-panel, T5 selfcheck |
| `ConnectionMeasurement.value: number` | T4 | T4 recordMeasurement, T4 тест |
| `SERIES_VOLTAGE_SPEC` columns: idx/point/U_V | T2 specs.ts | T4 buildJournalRow (U_V=m.value для series) |
| `PARALLEL_CURRENT_SPEC` columns: idx/point/I_A | T2 specs.ts | T4 buildJournalRow (I_A=m.value для parallel) |
| slot-id контракт board↔screen: `v-pos-r1`, `v-pos-r2`, `v-pos-total`, `a-pos-r1`, `a-pos-r2`, `a-pos-main` | T3 lab-connection-board.ts | T4 SLOTS_SERIES/SLOTS_PARALLEL, T4 #movablePosition, T5 selfcheck |
| `getSlotRect(id: string): DOMRect` | T3 lab-connection-board.ts | T4 CircuitAssembly (через board API) |
| `setCurrentAnimating(on: boolean): void` | T3 lab-connection-board.ts | T4 CircuitAssembly.#refreshAnimation |

**Потенциальная неоднозначность для контроллера:** В `buildJournalRow` нужно класть `U_V` для series и `I_A` для parallel в `values`. Поскольку `ConnectionMeasurement.value` — универсальное поле, реализатор должен маппировать: `series → { idx, point: m.point, U_V: m.value }`, `parallel → { idx, point: m.point, I_A: m.value }`. Это явно описано в Task 4, но реализатор должен быть внимателен.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-27-kit-3-phase-E.md`. Two execution options:

**1. Subagent-Driven (recommended)** — dispatch fresh subagent per task via `superpowers:subagent-driven-development`, agent type `inter-oge-experiment`, review between tasks.

**2. Inline Execution** — execute tasks in this session via `superpowers:executing-plans`.

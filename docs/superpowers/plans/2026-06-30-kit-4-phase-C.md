# Кит-4 «Оптика» Фаза C — опыт 4.4 «Свойства изображения собирающей линзой» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить опыт 4.4 (исследование свойств изображения собирающей линзы по 5 зонам положения предмета) на экран `lens-bench` как задачу **C** через task-switcher, не сломав опыты 4.1 (A) и 4.2 (B).

**Architecture:** Расширяем общий журнал v2 источником колонки `'choice'` (select + категориальный грейд точным совпадением) — единый `renderJournalTable`, не кастомный журнал. Категориальный эталон инлайнится в `IMAGE_PROPERTIES_SPEC` теми же формулами, что и `LensModel.imageProperties` (канон specs.ts «физика инлайнится»), с кросс-чек-тестом против неё. Ученик двигает предмет слайдером с 5 метками зон; живой ридаут зоны и грейдинг идут из ОДНОГО `imageProperties(F,d)` при точном d (нулевой трап рассинхрона). В зонах d≤F (мнимое/бесконечность) `lab-optical-bench` рисует «через линзу» — увеличенную прямую призрачную стрелку у линзы (d<F) либо note «в бесконечности» (d=F).

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components (Shadow DOM), npm workspaces, Vitest (happy-dom) + property-фаззинг, Playwright + @axe-core. Дев-копия `C:\dev\Inter_OGE`. Ветка `feat/fipi-full-coverage`.

## Global Constraints (спека §8 — действуют на КАЖДУЮ задачу)

1. **ФИПИ-якорь** — docstring опыта с дословной цитатой сноски (4) компл.№4 (стр.19): «…исследование свойства изображения, полученного с помощью собирающей линзы…».
2. **Journal v2 (§21)** — только `renderJournalTable` + `JournalSpec` + `verifyRow` + `parseRu`; 3 режима записи через `record-mode` ключ `kit-4` (`inter-oge.record-mode.kit-4`). Кастомный журнал ЗАПРЕЩЁН — расширяем общую инфру.
3. **Чистая физика** — pure DOM-free в `physics/optics/`, Vitest + фаззинг. Категориальный эталон инлайнится в specs.ts (НЕ импорт из kit-4), с кросс-чек-тестом против `LensModel.imageProperties`.
4. **idx-first конвенция (универсальна)** — ПЕРВАЯ колонка ЛЮБОГО spec'а = `{ key:'idx', label:'№', source:'meta', format:'int' }`. Enforced: `experiments/kit-1-hydrostatics/src/__tests__/shared-journal.test.ts` (`it.each(ALL_SPECS)`).
5. **D&D через mouse** — selfcheck тащит приборы только `page.mouse.move/down/up`; REST-state (подсветки гнёзд скрыты в покое); дроп подтверждать через `card[data-placed]`. Единый префикс зон `bench-slot-*`.
6. **A11y** — axe 0; роли/aria-live; клавиатура; `prefers-reduced-motion`; **SR/live-region/result-panel НЕ палят правильную категорию** в semi-auto/fully-manual (только в fully-auto).
7. **SVG-приборы (§27/§28)** — `lab-*`, shadow DOM, `svg[hidden]{display:none}` и `[hidden]{display:none}` для ВНУТРЕННИХ групп (НЕ полагаться на IDL `.hidden`); функц-деталь ≥15% viewBox.
8. **Согласованность зоны и грейда** — живой ридаут зоны и эталон категорий обязаны исходить из ОДНОЙ классификации при точном текущем d (избегаем трапа d=201 «≈2F» vs грейд «уменьшенное»).
9. **Гейты зелёные** — корневой `npm test` (ВЕСЬ монорепо), `typecheck`/`lint`(0)/`build` + `selfcheck-4-4.mjs` PASS + reality-check скрином. Merge-gate — из корня, не пер-воркспейс (урок Фазы B).
10. **Технологии** — TS strict, Vite 6, vanilla Web Components, npm workspaces. Коммиты — только явными путями (никогда `git add -A`); `dist/`/`*.tsbuildinfo`/`package-lock.json` не коммитить; `selfcheck-*.mjs` gitignored → `git add -f`. Сообщения коммитов на русском, императив, в конце `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. npm — только из Git Bash.

**Команды (Git Bash, из `C:\dev\Inter_OGE`):**
- Юнит весь монорепо: `npm test`
- Юнит один воркспейс: `npm test --workspace=@labosfera/kit-4-optics` / `--workspace=@labosfera/shared-spa`
- Typecheck/lint/build: `npm run typecheck` / `npm run lint` / `npm run build`
- Дев для selfcheck: `npm run dev --workspace=@labosfera/kit-4-optics -- --host 127.0.0.1 --port 5191 --strictPort`

---

## Карта файлов (что и зачем меняем)

**Создаём:**
- `experiments/kit-4-optics/selfcheck-4-4.mjs` — Playwright self-check опыта 4.4 (gitignored, `git add -f`).

**Модифицируем (общая инфра журнала):**
- `experiments/_shared-spa/src/lib/journal/types.ts` — `ColumnSource` += `'choice'`; `ColumnSpec` += `options?`, `expectedChoiceFromRow?`.
- `experiments/_shared-spa/src/lib/journal/verify.ts` — `verifyChoiceValue` + ветка в `verifyRow`.
- `experiments/_shared-spa/src/lib/journal/render.ts` — рендер `<select>` для choice + `onChoiceInput` + ✓ по gradable (derived|choice).
- `experiments/_shared-spa/src/lib/journal/specs.ts` — `IMAGE_PROPERTIES_SPEC` (4.4) + регистрация в `ALL_SPECS`.

**Модифицируем (kit-4):**
- `experiments/kit-4-optics/src/physics/optics/LensModel.ts` — `ObjectZone`, `objectZone(F,d)`, `zoneLabelRu(zone)`.
- `experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts` — «через линзу»: `#virtual-image-group` + `#infinity-note-group`.
- `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts` — задача `C-image`: state, drafts строковые, слайдер-диапазон, зона-ридаут, журнал-строки C, запись/режимы.
- `experiments/kit-4-optics/src/screens/lens-bench/template.html` — шаг C + `#object-zone-readout` + легенда зон.
- `experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts` — ref `objectZoneReadout`.
- `experiments/kit-4-optics/src/styles/lens-bench-experiment.css` — стиль зона-чипа/легенды.
- `experiments/home/src/data/kits.ts` — опыт 4.4 → `done: true`, screen `lens-bench`.

**Тесты:**
- `experiments/kit-1-hydrostatics/src/__tests__/shared-journal.test.ts` — рендер/верификация choice (там живут тесты render/verify shared-инфры).
- `experiments/_shared-spa/__tests__/lens-specs.test.ts` — `IMAGE_PROPERTIES_SPEC` + категориальный эталон.
- `experiments/_shared-spa/__tests__/specs.test.ts` — обновить whitelist источников, если он есть.
- `experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts` — `objectZone` + фаззинг + кросс-чек.
- `experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts` — virtual/infinity рендер.
- `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts` — задача C state-machine.

---

## Task 1: Журнал v2 — источник колонки `'choice'` (types + verify + render)

**Files:**
- Modify: `experiments/_shared-spa/src/lib/journal/types.ts`
- Modify: `experiments/_shared-spa/src/lib/journal/verify.ts`
- Modify: `experiments/_shared-spa/src/lib/journal/render.ts`
- Test: `experiments/kit-1-hydrostatics/src/__tests__/shared-journal.test.ts`
- Check: `experiments/_shared-spa/__tests__/specs.test.ts` (whitelist источников)

**Interfaces:**
- Produces:
  - `ColumnSource = 'meta' | 'direct' | 'derived' | 'choice'`
  - `ColumnSpec.options?: ReadonlyArray<{ readonly value: string; readonly label: string }>`
  - `ColumnSpec.expectedChoiceFromRow?: (row: Readonly<Record<string, number>>) => string`
  - `verifyChoiceValue(spec: ColumnSpec, row: JournalRow, value: string | null): JournalVerdict`
  - `RenderJournalOptions.onChoiceInput?(rowIdx: number, key: string, value: string | null): void`

- [ ] **Step 1: Падающие тесты — verify choice (verify.ts)**

В `experiments/kit-1-hydrostatics/src/__tests__/shared-journal.test.ts` добавить блок (импорты `verifyChoiceValue`, `verifyRow` из `@labosfera/shared-spa/lib/journal/verify`, тип `ColumnSpec`/`JournalRow` из `.../types`):

```ts
import { verifyChoiceValue } from '@labosfera/shared-spa/lib/journal/verify';
import type { ColumnSpec, JournalRow } from '@labosfera/shared-spa/lib/journal/types';

describe('verifyChoiceValue (источник choice)', () => {
  const kindCol: ColumnSpec = {
    key: 'kind', label: 'Вид', source: 'choice',
    options: [{ value: 'real', label: 'действительное' }, { value: 'virtual', label: 'мнимое' }],
    expectedChoiceFromRow: (r) => ((r.d_mm ?? 0) > (r.F_mm ?? 0) ? 'real' : 'virtual'),
  };
  const row = (vals: Record<string, number | string | null>): JournalRow => ({ idx: 1, timestamp: 1, values: vals });

  it('точное совпадение → ok', () => {
    expect(verifyChoiceValue(kindCol, row({ d_mm: 300, F_mm: 100 }), 'real')).toBe('ok');
  });
  it('неверный выбор → wrong', () => {
    expect(verifyChoiceValue(kindCol, row({ d_mm: 300, F_mm: 100 }), 'virtual')).toBe('wrong');
  });
  it('пусто (null / "") → empty', () => {
    expect(verifyChoiceValue(kindCol, row({ d_mm: 300, F_mm: 100 }), null)).toBe('empty');
    expect(verifyChoiceValue(kindCol, row({ d_mm: 300, F_mm: 100 }), '')).toBe('empty');
  });
  it('verifyRow грейдит choice-колонку по строковому значению', () => {
    const v = verifyRow([kindCol], row({ d_mm: 300, F_mm: 100, kind: 'real' }));
    expect(v['kind']).toBe('ok');
  });
  it('verifyRow: derived и choice вместе', () => {
    const gammaCol: ColumnSpec = {
      key: 'g', label: 'Γ', source: 'derived', format: 'fixed2', tolerance: 0.1,
      expectedFromRow: (r) => -(r.d_mm ?? 0) / 100,
    };
    const v = verifyRow([kindCol, gammaCol], row({ d_mm: 300, F_mm: 100, kind: 'real', g: -3 }));
    expect(v['kind']).toBe('ok');
    expect(v['g']).toBe('ok');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: FAIL — `verifyChoiceValue is not a function` / тип `'choice'` не в `ColumnSource`.

- [ ] **Step 3: types.ts — расширить ColumnSource и ColumnSpec**

В `experiments/_shared-spa/src/lib/journal/types.ts`:

```ts
export type ColumnSource = 'meta' | 'direct' | 'derived' | 'choice';
```

В интерфейс `ColumnSpec` (после `ariaLabel?`) добавить:

```ts
  /**
   * Только для `choice`: варианты выпадающего списка (value хранится в row.values
   * как строка, label показывается ученику).
   */
  readonly options?: ReadonlyArray<{ readonly value: string; readonly label: string }>;
  /**
   * Только для `choice`: эталонная категория (value одного из options) по числовому
   * контексту строки. Грейд = точное совпадение выбранного value с этим.
   */
  readonly expectedChoiceFromRow?: (row: Readonly<Record<string, number>>) => string;
```

Обновить шапку-docstring `ColumnSource`: добавить строку `*   - 'choice'  — категориальный выбор (select); ученик выбирает, грейд = точное совпадение с expectedChoiceFromRow.`

- [ ] **Step 4: verify.ts — verifyChoiceValue + ветка в verifyRow**

В `experiments/_shared-spa/src/lib/journal/verify.ts` добавить (после `verifyDerivedValue`):

```ts
/**
 * Грейд категориального (choice) значения: точное совпадение с эталоном.
 *   - `empty`  — value === null или ''
 *   - `ok`     — value === expectedChoiceFromRow(context)
 *   - `wrong`  — иначе, либо expected не определён
 */
export function verifyChoiceValue(
  spec: ColumnSpec,
  row: JournalRow,
  value: string | null,
): JournalVerdict {
  if (value === null || value === '') return 'empty';
  if (spec.source !== 'choice') return 'wrong';
  if (typeof spec.expectedChoiceFromRow !== 'function') return 'wrong';

  const context: Record<string, number> = {};
  for (const [k, v] of Object.entries(row.values)) {
    if (typeof v === 'number' && Number.isFinite(v)) context[k] = v;
  }
  let expected: string;
  try {
    expected = spec.expectedChoiceFromRow(context);
  } catch {
    return 'wrong';
  }
  return value === expected ? 'ok' : 'wrong';
}
```

В `verifyRow` заменить цикл, чтобы покрыть choice:

```ts
export function verifyRow(
  columns: ReadonlyArray<ColumnSpec>,
  row: JournalRow,
): Record<string, JournalVerdict> {
  const verdicts: Record<string, JournalVerdict> = {};
  for (const col of columns) {
    if (col.source === 'derived') {
      const v = row.values[col.key];
      const num = typeof v === 'number' ? v : null;
      verdicts[col.key] = verifyDerivedValue(col, row, num);
    } else if (col.source === 'choice') {
      const v = row.values[col.key];
      const str = typeof v === 'string' ? v : null;
      verdicts[col.key] = verifyChoiceValue(col, row, str);
    }
  }
  return verdicts;
}
```

- [ ] **Step 5: Запустить verify-тесты — зелёные**

Run: `npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: блок `verifyChoiceValue` PASS. (Рендер-тесты ещё не добавлены.)

- [ ] **Step 6: Падающие тесты — рендер choice (render.ts)**

В тот же `shared-journal.test.ts` добавить (импорт `renderJournalTable` из `.../journal/render`, `JournalSpec` из `.../types`):

```ts
import { renderJournalTable } from '@labosfera/shared-spa/lib/journal/render';

describe('renderJournalTable — choice-колонка', () => {
  const spec: JournalSpec = {
    experimentId: '4.4', kitId: 'kit-4',
    columns: [
      { key: 'idx', label: '№', source: 'meta', format: 'int' },
      {
        key: 'kind', label: 'Вид', source: 'choice',
        options: [{ value: 'real', label: 'действительное' }, { value: 'virtual', label: 'мнимое' }],
        expectedChoiceFromRow: (r) => ((r.d_mm ?? 0) > (r.F_mm ?? 0) ? 'real' : 'virtual'),
      },
    ],
  };
  const rows = [{ idx: 1, timestamp: 1, values: { idx: 1, kind: null as string | null, d_mm: 300, F_mm: 100 } }];

  it('semi-auto: choice рендерится как <select> с опциями', () => {
    const host = document.createElement('div');
    renderJournalTable(host, spec, rows, { mode: 'semi-auto', onCellInput: () => {}, onVerify: () => {}, onChoiceInput: () => {} });
    const sel = host.querySelector<HTMLSelectElement>('select[data-key="kind"]');
    expect(sel).not.toBeNull();
    const optionValues = Array.from(sel!.options).map((o) => o.value);
    expect(optionValues).toContain('real');
    expect(optionValues).toContain('virtual');
  });

  it('semi-auto: onChoiceInput вызывается при change со значением value', () => {
    const host = document.createElement('div');
    let captured: [number, string, string | null] | null = null;
    renderJournalTable(host, spec, rows, {
      mode: 'semi-auto', onCellInput: () => {}, onVerify: () => {},
      onChoiceInput: (rowIdx, key, value) => { captured = [rowIdx, key, value]; },
    });
    const sel = host.querySelector<HTMLSelectElement>('select[data-key="kind"]')!;
    sel.value = 'real';
    sel.dispatchEvent(new Event('change'));
    expect(captured).toEqual([1, 'kind', 'real']);
  });

  it('fully-auto: choice — readonly текст-метка (label), без select', () => {
    const host = document.createElement('div');
    const filled = [{ idx: 1, timestamp: 1, values: { idx: 1, kind: 'real', d_mm: 300, F_mm: 100 } }];
    renderJournalTable(host, spec, filled, { mode: 'fully-auto', onCellInput: () => {}, onVerify: () => {} });
    expect(host.querySelector('select[data-key="kind"]')).toBeNull();
    const td = host.querySelector('td[data-key="kind"]')!;
    expect(td.textContent).toContain('действительное');
  });

  it('fully-manual: choice — <select> (НЕ <input type=text> через ветку direct)', () => {
    const host = document.createElement('div');
    renderJournalTable(host, spec, rows, { mode: 'fully-manual', onCellInput: () => {}, onVerify: () => {}, onChoiceInput: () => {} });
    expect(host.querySelector('select[data-key="kind"]')).not.toBeNull();
    expect(host.querySelector('input[data-key="kind"]')).toBeNull();
  });
});
```

- [ ] **Step 7: Запустить — убедиться, что падает**

Run: `npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: FAIL — нет `<select>`; `onChoiceInput` не в типе опций.

- [ ] **Step 8: render.ts — onChoiceInput, рендер select, ✓ по gradable**

В `experiments/_shared-spa/src/lib/journal/render.ts`:

(а) В `RenderJournalOptions` добавить:

```ts
  /** Колбэк выбора категории в choice-колонке. value: option.value | null. */
  onChoiceInput?(rowIdx: number, key: string, value: string | null): void;
```

(б) **Dual-site (обе декларации, дословно):** в файле ДВЕ независимые `const hasDerived` — заменить ОБЕ:
- в `renderJournalTable` (~строка 93, управляет `<th>✓`): `const hasDerived = spec.columns.some((c) => c.source === 'derived');` → `const hasGradable = spec.columns.some((c) => c.source === 'derived' || c.source === 'choice');` и ниже `if (hasDerived && opts.mode === 'semi-auto')` → `if (hasGradable && opts.mode === 'semi-auto')`.
- в `renderRow` (~строка 160, управляет `<button>✓`): тот же `const hasDerived = ...` → `const hasGradable = ...` и `if (hasDerived && ...)` → `if (hasGradable && ...)`.

**Верификация (обязательна):** `grep -n 'hasDerived' experiments/_shared-spa/src/lib/journal/render.ts` → 0 совпадений. Иначе ✓-колонка рассинхронизируется между заголовком и строками → selfcheck Step 7 сломается.

(в) В `renderCell` — добавить ветку choice ПЕРЕД общим writable-расчётом. После строки `const isEditing = opts.editingRowIdx === row.idx;` вставить:

```ts
  if (col.source === 'choice') {
    const choiceVal = typeof value === 'string' ? value : null;
    const writableChoice = opts.mode !== 'fully-auto' || isEditing;
    if (writableChoice) {
      const select = document.createElement('select');
      select.className = 'j-input j-input--choice';
      select.dataset['row'] = String(row.idx);
      select.dataset['key'] = col.key;
      if (verdict && opts.mode === 'semi-auto') select.dataset['verdict'] = verdict;
      select.setAttribute('aria-label', col.ariaLabel ?? col.label);
      const ph = document.createElement('option');
      ph.value = '';
      ph.textContent = '—';
      select.appendChild(ph);
      for (const opt of col.options ?? []) {
        const o = document.createElement('option');
        o.value = opt.value;
        o.textContent = opt.label;
        select.appendChild(o);
      }
      select.value = choiceVal ?? '';
      select.addEventListener('change', () => {
        opts.onChoiceInput?.(row.idx, col.key, select.value === '' ? null : select.value);
      });
      td.appendChild(select);
    } else {
      const label = col.options?.find((o) => o.value === choiceVal)?.label ?? '—';
      td.textContent = label;
    }
    return td;
  }
```

(Блок verdict-data в начале `renderCell` уже ставит `td.dataset['verdict']` и CSS-классы для любого источника — choice их наследует.)

- [ ] **Step 9: Запустить рендер-тесты — зелёные**

Run: `npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: PASS — все choice-тесты зелёные.

- [ ] **Step 10: Проверить whitelist источников в specs.test.ts**

Прочитать `experiments/_shared-spa/__tests__/specs.test.ts`. Если есть assertion вида «source ∈ {meta,direct,derived}» (перечисление допустимых) — добавить `'choice'`. Если такого нет — ничего не менять.

- [ ] **Step 11: Гейты воркспейса shared-spa + потребители**

Run: `npm run typecheck && npm test --workspace=@labosfera/shared-spa && npm test --workspace=@labosfera/kit-1-hydrostatics`
Expected: PASS (расширение `ColumnSource` обратно совместимо — новые поля опциональны, `onChoiceInput` опционален).

- [ ] **Step 12: Commit**

```bash
git add experiments/_shared-spa/src/lib/journal/types.ts \
        experiments/_shared-spa/src/lib/journal/verify.ts \
        experiments/_shared-spa/src/lib/journal/render.ts \
        experiments/kit-1-hydrostatics/src/__tests__/shared-journal.test.ts
# specs.test.ts — добавить, ТОЛЬКО если правился в Step 10
git commit -m "$(cat <<'EOF'
feat(journal): источник колонки choice (select + категориальный грейд)

Расширяет журнал v2 категориальными колонками для опыта 4.4: ColumnSource
+= choice, options + expectedChoiceFromRow, verifyChoiceValue (точное
совпадение), рендер <select> + onChoiceInput, ✓ по gradable (derived|choice).
Обратно совместимо: новые поля и колбэк опциональны.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: LensModel — `objectZone(F,d)` (зона из imageProperties) + фаззинг

**Files:**
- Modify: `experiments/kit-4-optics/src/physics/optics/LensModel.ts`
- Test: `experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts`

**Interfaces:**
- Consumes: `imageProperties(F_mm, d_mm)` (есть).
- Produces:
  - `type ObjectZone = 'gt2F' | 'eq2F' | 'F_2F' | 'eqF' | 'ltF'`
  - `objectZone(F_mm: number, d_mm: number): ObjectZone`
  - `zoneLabelRu(zone: ObjectZone): string`

- [ ] **Step 1: Падающие тесты**

В `experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts` добавить (импорт `objectZone`, `zoneLabelRu`, `imageProperties`):

```ts
import { objectZone, zoneLabelRu, imageProperties } from '../LensModel';

describe('objectZone (зона по положению предмета относительно F и 2F)', () => {
  const F = 100;
  it('d > 2F → gt2F (уменьшенное действительное)', () => {
    expect(objectZone(F, 300)).toBe('gt2F');
  });
  it('d = 2F → eq2F (равное)', () => {
    expect(objectZone(F, 200)).toBe('eq2F');
  });
  it('F < d < 2F → F_2F (увеличенное действительное)', () => {
    expect(objectZone(F, 150)).toBe('F_2F');
  });
  it('d = F → eqF (изображение в бесконечности)', () => {
    expect(objectZone(F, 100)).toBe('eqF');
  });
  it('d < F → ltF (мнимое прямое увеличенное)', () => {
    expect(objectZone(F, 60)).toBe('ltF');
  });
  it('zoneLabelRu — человекочитаемые подписи', () => {
    expect(zoneLabelRu('gt2F')).toBe('d > 2F');
    expect(zoneLabelRu('eq2F')).toBe('d = 2F');
    expect(zoneLabelRu('F_2F')).toBe('F < d < 2F');
    expect(zoneLabelRu('eqF')).toBe('d = F');
    expect(zoneLabelRu('ltF')).toBe('d < F');
  });

  // Кросс-чек: зона СОГЛАСОВАНА с imageProperties (нулевой трап рассинхрона).
  it('фаззинг: objectZone бийективен с imageProperties.{kind,size} (50k)', () => {
    for (let i = 0; i < 50000; i++) {
      const Ff = 30 + (i % 170);            // 30..199 мм
      const d = 10 + ((i * 7) % 400);       // 10..409 мм
      if (d === Ff) continue;               // вырожденную d=F проверяем отдельно
      const p = imageProperties(Ff, d);
      const z = objectZone(Ff, d);
      if (p.kind === 'virtual' && Number.isFinite(p.gamma)) {
        expect(z).toBe('ltF');
      } else if (p.kind === 'real') {
        if (p.size === 'reduced') expect(z).toBe('gt2F');
        else if (p.size === 'equal') expect(z).toBe('eq2F');
        else expect(z).toBe('F_2F');
      }
    }
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test --workspace=@labosfera/kit-4-optics -- LensModel`
Expected: FAIL — `objectZone is not a function`.

- [ ] **Step 3: Реализация в LensModel.ts**

В конец `experiments/kit-4-optics/src/physics/optics/LensModel.ts` добавить:

```ts
/** Зона положения предмета относительно фокуса. */
export type ObjectZone = 'gt2F' | 'eq2F' | 'F_2F' | 'eqF' | 'ltF';

/**
 * Классифицировать положение предмета по зонам, СОГЛАСОВАННО с imageProperties:
 *   real+reduced  → gt2F   |  real+equal → eq2F  |  real+enlarged → F_2F
 *   virtual (∞)   → eqF    |  virtual (конечн.) → ltF
 * Единый источник классификации — imageProperties (нет второго порога → нет трапа).
 */
export function objectZone(F_mm: number, d_mm: number): ObjectZone {
  guard(F_mm, d_mm);
  const p = imageProperties(F_mm, d_mm);
  if (p.kind === 'virtual') {
    return Number.isFinite(p.gamma) ? 'ltF' : 'eqF';
  }
  if (p.size === 'reduced') return 'gt2F';
  if (p.size === 'equal') return 'eq2F';
  return 'F_2F';
}

/** Человекочитаемая подпись зоны (RU). */
export function zoneLabelRu(zone: ObjectZone): string {
  const map: Record<ObjectZone, string> = {
    gt2F: 'd > 2F',
    eq2F: 'd = 2F',
    F_2F: 'F < d < 2F',
    eqF: 'd = F',
    ltF: 'd < F',
  };
  return map[zone];
}
```

- [ ] **Step 4: Запустить — зелёные**

Run: `npm test --workspace=@labosfera/kit-4-optics -- LensModel`
Expected: PASS (включая 50k фаззинг).

- [ ] **Step 5: Commit**

```bash
git add experiments/kit-4-optics/src/physics/optics/LensModel.ts \
        experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts
git commit -m "$(cat <<'EOF'
feat(optics): objectZone(F,d) — зона предмета, согласованная с imageProperties

Зона выводится из imageProperties (единый источник) → исключает трап
рассинхрона зоны и категориального грейда. Фаззинг 50k подтверждает
бийекцию зона↔{kind,size}.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `IMAGE_PROPERTIES_SPEC` (журнал 4.4) — категориальный эталон (инлайн) + регистрация

**Files:**
- Modify: `experiments/_shared-spa/src/lib/journal/specs.ts`
- Test: `experiments/_shared-spa/__tests__/lens-specs.test.ts`
- Test (кросс-чек физики): `experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts`

**Interfaces:**
- Consumes: `ColumnSpec` (`choice` + `options` + `expectedChoiceFromRow` из Task 1).
- Produces: `IMAGE_PROPERTIES_SPEC: JournalSpec` (experimentId `'4.4'`). Колонки: `idx`(meta), `zone`(meta,string), `kind`(choice), `orientation`(choice), `size`(choice), `gamma`(derived). Контекст грейда — числовые `d_mm`,`F_mm` в `row.values` (НЕ колонки).

- [ ] **Step 1: Падающие тесты**

В `experiments/_shared-spa/__tests__/lens-specs.test.ts` добавить (импорт расширить `IMAGE_PROPERTIES_SPEC`, `verifyRow` уже импортирован):

```ts
import { IMAGE_PROPERTIES_SPEC } from '../src/lib/journal/specs';

describe('IMAGE_PROPERTIES_SPEC (опыт 4.4)', () => {
  const col = (k: string) => IMAGE_PROPERTIES_SPEC.columns.find((c) => c.key === k)!;

  it('experimentId=4.4, kitId=kit-4', () => {
    expect(IMAGE_PROPERTIES_SPEC.experimentId).toBe('4.4');
    expect(IMAGE_PROPERTIES_SPEC.kitId).toBe('kit-4');
  });

  it('порядок колонок: idx, zone, kind, orientation, size, gamma', () => {
    expect(IMAGE_PROPERTIES_SPEC.columns.map((c) => c.key))
      .toEqual(['idx', 'zone', 'kind', 'orientation', 'size', 'gamma']);
  });

  it('kind/orientation/size — choice с options', () => {
    for (const k of ['kind', 'orientation', 'size']) {
      expect(col(k).source).toBe('choice');
      expect((col(k).options?.length ?? 0)).toBeGreaterThanOrEqual(2);
    }
    expect(col('gamma').source).toBe('derived');
    expect(col('zone').source).toBe('meta');
  });

  it('expectedChoiceFromRow — 5 зон (F=100)', () => {
    const cases: Array<[number, string, string, string]> = [
      // d,     kind,      orientation,   size
      [300, 'real', 'inverted', 'reduced'],
      [200, 'real', 'inverted', 'equal'],
      [150, 'real', 'inverted', 'enlarged'],
      [100, 'virtual', 'upright', 'enlarged'],  // d=F (контракт)
      [60,  'virtual', 'upright', 'enlarged'],
    ];
    for (const [d, kind, orient, size] of cases) {
      const ctx = { d_mm: d, F_mm: 100 };
      expect(col('kind').expectedChoiceFromRow!(ctx)).toBe(kind);
      expect(col('orientation').expectedChoiceFromRow!(ctx)).toBe(orient);
      expect(col('size').expectedChoiceFromRow!(ctx)).toBe(size);
    }
  });

  it('gamma.expectedFromRow — Γ=−f/d: действит. d=300→−0,5; d=150→−2; мнимое d=60→+2,5 (знак!)', () => {
    expect(col('gamma').expectedFromRow!({ d_mm: 300, F_mm: 100 })).toBeCloseTo(-0.5, 3);
    expect(col('gamma').expectedFromRow!({ d_mm: 150, F_mm: 100 })).toBeCloseTo(-2, 3);
    // d<F (мнимое): Γ=F/(F−d)=100/40=+2,5 ПОЛОЖИТЕЛЬНОЕ (прямое) — ловит знаковую ошибку
    expect(col('gamma').expectedFromRow!({ d_mm: 60, F_mm: 100 })).toBeCloseTo(2.5, 2);
  });

  it('verifyRow: верный выбор категорий → ok; неверный → wrong (d=300,F=100)', () => {
    const okV = verifyRow(IMAGE_PROPERTIES_SPEC.columns, {
      idx: 1, timestamp: 1,
      values: { idx: 1, zone: 'd > 2F', d_mm: 300, F_mm: 100, kind: 'real', orientation: 'inverted', size: 'reduced', gamma: -0.5 },
    });
    expect(okV['kind']).toBe('ok');
    expect(okV['orientation']).toBe('ok');
    expect(okV['size']).toBe('ok');
    expect(okV['gamma']).toBe('ok');
    const badV = verifyRow(IMAGE_PROPERTIES_SPEC.columns, {
      idx: 1, timestamp: 1,
      values: { idx: 1, zone: 'd > 2F', d_mm: 300, F_mm: 100, kind: 'virtual', orientation: 'inverted', size: 'reduced', gamma: -0.5 },
    });
    expect(badV['kind']).toBe('wrong');
  });

  it('d=F: gamma пустой → empty (∞), любой ввод → wrong; категории по контракту', () => {
    const base = (gamma: number | null) => ({
      idx: 1, timestamp: 1,
      values: { idx: 1, zone: 'd = F', d_mm: 100, F_mm: 100, kind: 'virtual', orientation: 'upright', size: 'enlarged', gamma },
    });
    const vEmpty = verifyRow(IMAGE_PROPERTIES_SPEC.columns, base(null));
    expect(vEmpty['kind']).toBe('ok');
    expect(vEmpty['gamma']).toBe('empty');
    // ввод любого числа при d=F (эталон ∞) → wrong (verify.ts: !Number.isFinite(expected))
    const vNum = verifyRow(IMAGE_PROPERTIES_SPEC.columns, base(999));
    expect(vNum['gamma']).toBe('wrong');
  });

  it('зарегистрирован в ALL_SPECS / getSpecByExperimentId', () => {
    expect(getSpecByExperimentId('4.4')).toBe(IMAGE_PROPERTIES_SPEC);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test --workspace=@labosfera/shared-spa -- lens-specs`
Expected: FAIL — `IMAGE_PROPERTIES_SPEC` не экспортирован.

- [ ] **Step 3: specs.ts — добавить IMAGE_PROPERTIES_SPEC**

В `experiments/_shared-spa/src/lib/journal/specs.ts` ПОСЛЕ `FOCAL_2F_SPEC` добавить:

```ts
/**
 * Опыт 4.4 «Исследование свойств изображения, полученного с помощью собирающей линзы».
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№4 (стр.19), сноска (4):
 * «...исследование свойства изображения, полученного с помощью собирающей линзы...». КОДИФ §1.29.
 *
 * Метод: двигать предмет по 5 зонам (>2F, =2F, F<d<2F, =F, <F) → классифицировать
 *   изображение: вид (действит./мнимое), ориентация (прямое/перев.), размер
 *   (увелич./уменьш./равное) + числовое Γ = −f/d.
 *
 * Грейд категорий — точное совпадение с эталоном; gamma — числовой (tolerance).
 * Эталон ИНЛАЙНИТСЯ теми же формулами, что LensModel.imageProperties (канон §21.A.4;
 * кросс-чек-тест в kit-4 подтверждает совпадение). Контекст грейда — числовые d_mm,F_mm
 * в row.values (НЕ колонки: render их не показывает, verify читает). F скрыт от ученика.
 *
 * d=F: изображение в бесконечности → Γ=∞ (ученик оставляет gamma пустым → empty);
 *   категории по контракту imageProperties: virtual / upright / enlarged.
 */
const IMG_EPS = 0.001;

function imgFocalF(d: number, F: number): number {
  const denom = d - F;
  return Math.abs(denom) < 1e-9 ? Infinity : (d * F) / denom;
}

export const IMAGE_PROPERTIES_SPEC: JournalSpec = {
  experimentId: '4.4',
  kitId: 'kit-4',
  columns: [
    { key: 'idx', label: '№', source: 'meta', format: 'int' },
    { key: 'zone', label: 'Зона', source: 'meta' },
    {
      key: 'kind',
      label: 'Вид',
      source: 'choice',
      options: [
        { value: 'real', label: 'действительное' },
        { value: 'virtual', label: 'мнимое' },
      ],
      expectedChoiceFromRow: (r) => {
        const d = r.d_mm ?? 0;
        const F = r.F_mm ?? 0;
        if (Math.abs(d - F) < 1e-9) return 'virtual'; // d=F контракт
        return imgFocalF(d, F) > 0 ? 'real' : 'virtual';
      },
    },
    {
      key: 'orientation',
      label: 'Ориентация',
      source: 'choice',
      options: [
        { value: 'inverted', label: 'перевёрнутое' },
        { value: 'upright', label: 'прямое' },
      ],
      expectedChoiceFromRow: (r) => {
        const d = r.d_mm ?? 0;
        const F = r.F_mm ?? 0;
        if (Math.abs(d - F) < 1e-9) return 'upright';
        const f = imgFocalF(d, F);
        return -f / d < 0 ? 'inverted' : 'upright';
      },
    },
    {
      key: 'size',
      label: 'Размер',
      source: 'choice',
      options: [
        { value: 'enlarged', label: 'увеличенное' },
        { value: 'reduced', label: 'уменьшенное' },
        { value: 'equal', label: 'равное' },
      ],
      expectedChoiceFromRow: (r) => {
        const d = r.d_mm ?? 0;
        const F = r.F_mm ?? 0;
        if (Math.abs(d - F) < 1e-9) return 'enlarged'; // d=F контракт
        const g = Math.abs(-imgFocalF(d, F) / d);
        if (g > 1 + IMG_EPS) return 'enlarged';
        if (g < 1 - IMG_EPS) return 'reduced';
        return 'equal';
      },
    },
    {
      key: 'gamma',
      label: 'Γ',
      source: 'derived',
      format: 'fixed2',
      tolerance: 0.10,
      // Γ = −f/d; d=F → ∞ (verify вернёт wrong на ввод, empty на пусто).
      expectedFromRow: (r) => {
        const d = r.d_mm ?? 0;
        const F = r.F_mm ?? 0;
        if (d <= 0 || F <= 0) return 0;
        const f = imgFocalF(d, F);
        return Number.isFinite(f) ? -f / d : Infinity;
      },
    },
  ],
};
```

- [ ] **Step 4: Зарегистрировать в ALL_SPECS**

В массив `ALL_SPECS` (после `FOCAL_2F_SPEC,`) добавить `IMAGE_PROPERTIES_SPEC,`.

- [ ] **Step 5: Запустить spec-тесты + idx-first**

Run: `npm test --workspace=@labosfera/shared-spa -- lens-specs && npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: PASS — включая `it.each(ALL_SPECS)` «первая колонка idx» для 4.4.

- [ ] **Step 5b: Кросс-чек физики — инлайн спеки == LensModel.imageProperties (mustFix verify)**

Инлайн-формулы в `specs.ts` — ВТОРОЙ источник физики; обязан совпадать с каноном `LensModel.imageProperties` (Constraint 8). Кросс-чек живёт в kit-4 (kit-4 зависит от shared-spa; импорт `IMAGE_PROPERTIES_SPEC` из `@labosfera/shared-spa` допустим, обратное — нет). В `experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts` добавить:

```ts
import { IMAGE_PROPERTIES_SPEC } from '@labosfera/shared-spa/lib/journal/specs';

describe('IMAGE_PROPERTIES_SPEC инлайн ↔ LensModel.imageProperties (кросс-чек)', () => {
  const col = (k: string) => IMAGE_PROPERTIES_SPEC.columns.find((c) => c.key === k)!;
  it('фаззинг 50k: choice-эталоны спеки == imageProperties.{kind,orientation,size}', () => {
    for (let i = 0; i < 50000; i++) {
      const F = 30 + (i % 170);          // 30..199
      const d = 10 + ((i * 7) % 400);    // 10..409
      if (Math.abs(d - F) < 1e-9) continue;
      const ctx = { d_mm: d, F_mm: F };
      const p = imageProperties(F, d);
      expect(col('kind').expectedChoiceFromRow!(ctx)).toBe(p.kind);
      expect(col('orientation').expectedChoiceFromRow!(ctx)).toBe(p.orientation);
      expect(col('size').expectedChoiceFromRow!(ctx)).toBe(p.size);
    }
  });
});
```

Run: `npm test --workspace=@labosfera/kit-4-optics -- LensModel`
Expected: PASS (расхождение инлайна и канона упадёт здесь).

- [ ] **Step 6: Commit**

```bash
git add experiments/_shared-spa/src/lib/journal/specs.ts \
        experiments/_shared-spa/__tests__/lens-specs.test.ts \
        experiments/kit-4-optics/src/physics/optics/__tests__/LensModel.test.ts
git commit -m "$(cat <<'EOF'
feat(journal): IMAGE_PROPERTIES_SPEC (4.4) — категориальные колонки изображения

idx + zone(meta) + kind/orientation/size(choice) + gamma(derived). Эталон
категорий инлайнится формулами imageProperties; контекст d_mm/F_mm — скрытые
числовые поля row.values. d=F: gamma пустой (∞), категории по контракту.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `lab-optical-bench` — «через линзу» (мнимое d<F + бесконечность d=F)

**Files:**
- Modify: `experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts`
- Test: `experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts`

**Interfaces:**
- Consumes: внутренний `computeImageDistance(F,d)`; `setObjectDistanceMm/setLensFocalMm/setScreenDistanceMm` (есть).
- Produces (DOM в shadowRoot, наблюдаемо тестами):
  - `#virtual-image-group` (g, `class="virtual-image"`) — призрачная ПРЯМАЯ увеличенная стрелка у линзы; виден при d<F.
  - `#infinity-note-group` (g) — текст «изображение в бесконечности» + 2 параллельных луча; виден при d=F.
  - Реальное `#projected-image-group` — виден ТОЛЬКО при действительном (d>F), как сейчас.

- [ ] **Step 1: Падающие тесты**

В `experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts` добавить блок (паттерн существующих тестов; элемент создаётся `document.createElement('lab-optical-bench')`, в DOM, `connectedCallback` отрабатывает; F по умолчанию 100):

```ts
describe('lab-optical-bench — рендер изображения по зонам (опыт 4.4)', () => {
  function makeBench(): HTMLElement & {
    setObjectDistanceMm(d: number): void;
    setLensFocalMm(F: number): void;
    setScreenDistanceMm(f: number): void;
  } {
    const el = document.createElement('lab-optical-bench') as never;
    document.body.appendChild(el);
    return el;
  }
  const isVisible = (root: ShadowRoot, sel: string) => {
    const e = root.querySelector(sel);
    return !!e && !e.hasAttribute('hidden');
  };

  it('d > F (действительное): проецируемое видно, virtual/infinity скрыты', () => {
    const el = makeBench();
    el.setLensFocalMm(100);
    el.setObjectDistanceMm(300);
    el.setScreenDistanceMm(150);
    const sr = (el as unknown as { shadowRoot: ShadowRoot }).shadowRoot;
    expect(isVisible(sr, '#projected-image-group')).toBe(true);
    expect(isVisible(sr, '#virtual-image-group')).toBe(false);
    expect(isVisible(sr, '#infinity-note-group')).toBe(false);
    el.remove();
  });

  it('d < F (мнимое): virtual видно ПРЯМОЕ (не инвертировано), проецируемое скрыто', () => {
    const el = makeBench();
    el.setLensFocalMm(100);
    el.setObjectDistanceMm(60);
    const sr = (el as unknown as { shadowRoot: ShadowRoot }).shadowRoot;
    expect(isVisible(sr, '#virtual-image-group')).toBe(true);
    expect(isVisible(sr, '#projected-image-group')).toBe(false);
    expect(isVisible(sr, '#infinity-note-group')).toBe(false);
    // прямое: трансформ НЕ содержит инверсии scale(1,-..)
    const inner = sr.querySelector('.virtual-image')!;
    expect(inner.getAttribute('transform') ?? '').not.toMatch(/scale\(1,\s*-/);
    el.remove();
  });

  it('d = F (бесконечность): infinity note видно, остальные скрыты', () => {
    const el = makeBench();
    el.setLensFocalMm(100);
    el.setObjectDistanceMm(100);
    const sr = (el as unknown as { shadowRoot: ShadowRoot }).shadowRoot;
    expect(isVisible(sr, '#infinity-note-group')).toBe(true);
    expect(isVisible(sr, '#projected-image-group')).toBe(false);
    expect(isVisible(sr, '#virtual-image-group')).toBe(false);
    el.remove();
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test --workspace=@labosfera/kit-4-optics -- lab-optical-bench`
Expected: FAIL — нет `#virtual-image-group` / `#infinity-note-group`.

- [ ] **Step 3: Шаблон — добавить две группы**

В `experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts`, в SVG-шаблоне ПОСЛЕ группы `#object-arrow` (перед оверлеем лучей) добавить:

```html
  <!-- ════ МНИМОЕ ИЗОБРАЖЕНИЕ «через линзу» (d<F): прямое, увеличенное, призрачное ════ -->
  <g id="virtual-image-group" hidden="">
    <g class="virtual-image"
       transform="translate(${mmToPx(DEFAULT_LENS_MM)}, ${BENCH_Y - BENCH_HEIGHT / 2 - SLOT_H / 2})">
      <!-- прямая стрелка (вверх), БЕЗ инверсии; масштаб задаётся в #updateImageRendering -->
      <line x1="0" y1="0" x2="0" y2="${-ARROW_H}"
            stroke="#9d8cff" stroke-width="3" stroke-linecap="round"
            stroke-dasharray="4 3" opacity="0.85"/>
      <polygon points="0,${-ARROW_H - 8} -6,${-ARROW_H + 2} 6,${-ARROW_H + 2}"
               fill="#9d8cff" opacity="0.85"/>
      <ellipse cx="0" cy="${-ARROW_H / 2}" rx="10" ry="${ARROW_H / 2 + 4}"
               fill="rgba(157,140,255,0.08)"/>
    </g>
  </g>

  <!-- ════ ИЗОБРАЖЕНИЕ В БЕСКОНЕЧНОСТИ (d=F): note + 2 параллельных луча ════ -->
  <g id="infinity-note-group" hidden="">
    <line class="ray-line ray-parallel" x1="${mmToPx(DEFAULT_LENS_MM)}" y1="${BENCH_Y - ARROW_H}"
          x2="${BENCH_RIGHT_X}" y2="${BENCH_Y - ARROW_H}" opacity="0.7"/>
    <line class="ray-line ray-parallel" x1="${mmToPx(DEFAULT_LENS_MM)}" y1="${BENCH_Y}"
          x2="${BENCH_RIGHT_X}" y2="${BENCH_Y}" opacity="0.7"/>
    <text class="focal-marker" x="${mmToPx(DEFAULT_LENS_MM + 220)}" y="${BENCH_Y - ARROW_H - 8}">
      изображение в бесконечности
    </text>
  </g>
```

- [ ] **Step 4: Ссылки и логика видимости**

(а) В класс `LabOpticalBench` добавить поля:

```ts
  #virtualImageGroup: SVGGElement | null = null;
  #virtualImageInner: SVGGElement | null = null;
  #infinityNoteGroup: SVGGElement | null = null;
```

(б) В конструкторе (рядом с другими `shadow.querySelector`):

```ts
    this.#virtualImageGroup = shadow.querySelector<SVGGElement>('#virtual-image-group');
    this.#virtualImageInner = shadow.querySelector<SVGGElement>('.virtual-image');
    this.#infinityNoteGroup = shadow.querySelector<SVGGElement>('#infinity-note-group');
```

(в) Переписать `#updateProjectedImage` → единый `#updateImageRendering`, управляющий тремя группами. Заменить тело метода:

```ts
  #updateProjectedImage(): void {
    const imagePlaneMm = computeImageDistance(this.#lensFocalMm, this.#objectDistanceMm);
    const isReal = isFinite(imagePlaneMm) && imagePlaneMm > 0;
    const isInfinity = !isFinite(imagePlaneMm); // d=F
    const isVirtual = isFinite(imagePlaneMm) && imagePlaneMm < 0; // d<F

    this.#toggleHidden(this.#projectedImageGroup, !isReal);
    this.#toggleHidden(this.#virtualImageGroup, !isVirtual);
    this.#toggleHidden(this.#infinityNoteGroup, !isInfinity);

    if (isVirtual) this.#updateVirtualImage();

    let sharpness: number;
    if (!isReal) {
      sharpness = 0;
    } else {
      const delta = Math.abs(this.#screenDistanceMm - imagePlaneMm);
      sharpness = 1 / (1 + delta / BLUR_BAND_MM);
    }
    this.#applyBlur(sharpness);
  }

  /** Призрачная ПРЯМАЯ увеличенная стрелка у линзы (мнимое, |Γ|=F/(F−d), кламп [1,3]). */
  #updateVirtualImage(): void {
    if (!this.#virtualImageInner) return;
    const F = this.#lensFocalMm;
    const d = this.#objectDistanceMm;
    const denom = F - d;
    const g = denom > 0.001 ? F / denom : 3;
    const sy = Math.max(1, Math.min(3, g));
    const x = mmToPx(this.#lensPosMm);
    const y = BENCH_Y - BENCH_HEIGHT / 2 - SLOT_H / 2;
    // ПРЯМОЕ: scale(1, sy) БЕЗ минуса (не инвертировано); базовая стрелка вверх.
    this.#virtualImageInner.setAttribute('transform', `translate(${x}, ${y}) scale(1,${sy.toFixed(2)})`);
  }

  /** Установить/снять hidden c явным CSS [hidden]{display:none} (SVG-ловушка §7). */
  #toggleHidden(el: Element | null, hidden: boolean): void {
    if (!el) return;
    if (hidden) el.setAttribute('hidden', '');
    else el.removeAttribute('hidden');
  }
```

(Удалить прежний блок `if (this.#projectedImageGroup) {...}` внутри старого `#updateProjectedImage` — теперь это делает `#toggleHidden`.)

- [ ] **Step 5: Обновить title/docstring компонента**

В docstring шапки и в `<title id="bench-title">` уточнить, что скамья обслуживает опыты 4.1/4.2/4.4 (рендер действительного/мнимого/бесконечного изображения). Текст title: `Оптическая скамья — опыты 4.1/4.2/4.4 (собирающая линза)`.

- [ ] **Step 6: Запустить — зелёные (компонент + регрессия A/B)**

Run: `npm test --workspace=@labosfera/kit-4-optics -- lab-optical-bench`
Expected: PASS — новые тесты + прежние (d>F поведение не изменилось).

> Проверки (verify shouldFix): скрытие `#projected-image-group` при d<F идёт через `#toggleHidden` (НЕ bare `setAttribute`), чтобы сработал `[hidden]{display:none}`. Существующий тест transform-клампа `.projected-image` (текущие строки ~245–253) корректен — НЕ менять; пробел видимости закрывает новый тест Step 1 (`isVisible('#projected-image-group') === false` при d<F).

- [ ] **Step 7: Commit**

```bash
git add experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts \
        experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(optics): lab-optical-bench «через линзу» — мнимое (d<F) и бесконечность (d=F)

d<F → призрачная прямая увеличенная стрелка у линзы (|Γ|=F/(F−d), кламп);
d=F → note «в бесконечности» + параллельные лучи; d>F → действительное как было.
В опытах 4.1/4.2 (d>F всегда) не триггерится — регрессии нет.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `LensBenchExperiment` — задача `C-image` (state, drafts, слайдер, зона, запись, режимы)

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts`
- Test: `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts`

**Interfaces:**
- Consumes: `IMAGE_PROPERTIES_SPEC` (Task 3), `objectZone`/`zoneLabelRu` (Task 2), `imageProperties`/`magnification` (LensModel), `verifyRow` (Task 1, ветка choice), `onChoiceInput` (Task 1).
- Produces:
  - `LensTaskId = 'A-power' | 'B-focal2f' | 'C-image'`
  - `ExperimentRefs.objectZoneReadout?: HTMLElement | undefined`
  - Запись строк опыта 4.4 (по зоне), категориальные drafts (string), грейд через ✓/авто.

- [ ] **Step 1: Падающие тесты (state-machine, happy-dom)**

В `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts` добавить (использовать существующий тест-харнесс DOM + `placeInSlot` + установку record-mode через `localStorage.setItem('inter-oge.record-mode.kit-4', ...)`; если в файле есть фабрика harness — переиспользовать её, добавив в refs `objectZoneReadout`):

```ts
describe('LensBenchExperiment — задача C (опыт 4.4 свойства изображения)', () => {
  it('setActiveTask("C-image") активирует задачу C и показывает слайдер предмета', () => {
    const { exp, refs } = makeExperiment(); // фабрика харнесса файла
    exp.setActiveTask('C-image');
    expect(exp.activeTask).toBe('C-image');
    expect(refs.objectSliderRow!.hidden).toBe(false);
  });

  it('диапазон слайдера предмета в C достаёт зоны <F и >2F (min≤60, max≥300)', () => {
    const { exp, refs } = makeExperiment();
    exp.setActiveTask('C-image');
    expect(Number(refs.objectSlider.min)).toBeLessThanOrEqual(60);
    expect(Number(refs.objectSlider.max)).toBeGreaterThanOrEqual(300);
  });

  it('зона-ридаут обновляется из imageProperties при движении предмета (F=100)', () => {
    const { exp, refs } = makeExperiment();
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    expect(refs.objectZoneReadout!.textContent).toContain('d > 2F');
    exp.setObjectDistanceMm(60);
    expect(refs.objectZoneReadout!.textContent).toContain('d < F');
  });

  it('запись строки C: журнал-строка несёт zone(label) + скрытые d_mm,F_mm; gamma/kind пустые в semi-auto', () => {
    const { exp, refs } = makeExperiment();
    localStorage.setItem('inter-oge.record-mode.kit-4', 'semi-auto');
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    exp.recordMeasurement();
    const sel = refs.journalHost!.querySelector('select[data-key="kind"]');
    expect(sel).not.toBeNull(); // semi-auto → ученик выбирает
    expect(exp.measurements.filter((m) => m.task === 'C-image').length).toBe(1);
  });

  it('fully-auto: запись авто-заполняет категории (kind=действительное при d>2F)', () => {
    const { exp, refs } = makeExperiment();
    localStorage.setItem('inter-oge.record-mode.kit-4', 'fully-auto');
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    exp.recordMeasurement();
    const td = refs.journalHost!.querySelector('td[data-key="kind"]')!;
    expect(td.textContent).toContain('действительное');
  });

  it('a11y: в semi-auto live-region и result-panel НЕ содержат правильную категорию', () => {
    const { exp, refs } = makeExperiment();
    localStorage.setItem('inter-oge.record-mode.kit-4', 'semi-auto');
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    exp.recordMeasurement();
    for (const node of [refs.liveRegion, refs.resultPanel]) {
      const t = node.textContent ?? '';
      expect(t).not.toContain('уменьшенное');
      expect(t).not.toContain('перевёрнутое');
      expect(t).not.toContain('действительное');
    }
  });

  it('изоляция задач: измерения C не видны в журнале A', () => {
    const { exp } = makeExperiment();
    localStorage.setItem('inter-oge.record-mode.kit-4', 'fully-auto');
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    exp.recordMeasurement();
    exp.setActiveTask('A-power');
    expect(exp.measurements.filter((m) => m.task === 'A-power').length).toBe(0);
  });

  afterEach(() => localStorage.removeItem('inter-oge.record-mode.kit-4'));
});
```

> **ВАЖНО (verify shouldFix): харнесс `makeExperiment()` в kit-4 НЕ существует** (существующие describe используют class-level `let host/experiment` + `beforeEach`; `grep makeExperiment` = 0). Перед тестами C ОБЯЗАТЕЛЬНО три правки, иначе тесты падают ReferenceError/TypeError:
> 1. Добавить `objectZoneReadout?: HTMLElement | undefined` в `ExperimentRefs` (это Step 3д — сделать ДО тестов).
> 2. Завести фабрику `function makeExperiment(): { exp: LensBenchExperiment; refs: ExperimentRefs; host: HTMLElement }` в тест-файле: строит host со ВСЕМИ слотами template'а (включая `<output id="object-zone-readout">` внутри `#stage`, `#steps` с li `data-task="C-image"`, `#object-slider`/`#object-slider-row`), собирает refs, чистит в `afterEach`. Переиспользовать существующий DOM-билдер харнесса, если он есть — не дублировать строки.
> 3. `<output id="object-zone-readout">` появляется в РЕАЛЬНОМ template.html в Task 6 — но для unit-тестов (happy-dom) его строит харнесс здесь.
> Если предпочитаешь существующий beforeEach-паттерн вместо фабрики — адаптируй тест-код выше под него (добавив readout в общий host); НЕ оставляй вызовы `makeExperiment()`, если фабрику не создал.

- [ ] **Step 2: Запустить — падает**

Run: `npm test --workspace=@labosfera/kit-4-optics -- lens-bench`
Expected: FAIL — нет `'C-image'` в `LensTaskId`, нет `objectZoneReadout`, нет ветки записи C.

- [ ] **Step 3: Типы и константы C**

> **АТОМАРНОСТЬ (verify mustFix):** подшаги (б) расширение `LensTaskId` и (г) дополнение `TASK_DEFAULTS`/`SPEC_BY_TASK`/`OBJECT_SLIDER_RANGE` ключом `'C-image'` делать ОДНИМ заходом (заменить блок строк ~46–104 целиком). Промежуточное состояние (тип расширен, но `Record<LensTaskId, …>` без ключа `'C-image'`) даёт TS2741 на оба Record — не коммитить/не запускать между ними.

В `LensBenchExperiment.ts`:

(а) Импорты — добавить:
```ts
import { focalFromDistances, opticalPower, imageDistance, magnification, imageProperties, objectZone, zoneLabelRu } from '@physics/optics/LensModel';
import { LENS_POWER_SPEC, FOCAL_2F_SPEC, IMAGE_PROPERTIES_SPEC } from '@labosfera/shared-spa/lib/journal/specs';
```

(б) `LensTaskId`:
```ts
export type LensTaskId = 'A-power' | 'B-focal2f' | 'C-image';
```

(в) `BenchMeasurement` — добавить опциональные поля C:
```ts
  readonly twoF_mm?: number;
  readonly zoneLabel?: string;   // опыт 4.4: подпись зоны
```

(г) `TASK_DEFAULTS` + `SPEC_BY_TASK`:
```ts
const TASK_DEFAULTS: Record<LensTaskId, { objectMm: number; screenMm: number }> = {
  'A-power':   { objectMm: 200, screenMm: 200 },
  'B-focal2f': { objectMm: 150, screenMm: 250 },
  'C-image':   { objectMm: 300, screenMm: 150 },
};
const SPEC_BY_TASK: Record<LensTaskId, JournalSpec> = {
  'A-power':   LENS_POWER_SPEC,
  'B-focal2f': FOCAL_2F_SPEC,
  'C-image':   IMAGE_PROPERTIES_SPEC,
};
/** Диапазон слайдера предмета по задаче (мм). C достаёт зоны <F и >2F. */
const OBJECT_SLIDER_RANGE: Record<LensTaskId, { min: number; max: number }> = {
  'A-power':   { min: 110, max: 290 },
  'B-focal2f': { min: 110, max: 290 },
  'C-image':   { min: 50,  max: 350 },
};
```

(д) `ExperimentRefs` — добавить поле:
```ts
  objectZoneReadout?: HTMLElement | undefined;
```

(е) Drafts — строковые тоже:
```ts
  #journalDrafts = new Map<number, Record<string, number | string>>();
```

- [ ] **Step 4: Слайдер-диапазон, зона-ридаут, видимость**

(а) Новый метод применения диапазона + обновить видимость:
```ts
  #applyObjectSliderRange(task: LensTaskId): void {
    const r = OBJECT_SLIDER_RANGE[task];
    this.#refs.objectSlider.min = String(r.min);
    this.#refs.objectSlider.max = String(r.max);
  }

  #refreshObjectSliderVisibility(): void {
    // Слайдер предмета нужен в задачах B и C (в A предмет фиксирован).
    const task = this.#store.get().activeTask;
    if (this.#refs.objectSliderRow) {
      this.#refs.objectSliderRow.hidden = task === 'A-power';
    }
  }

  #updateZoneReadout(): void {
    if (!this.#refs.objectZoneReadout) return;
    const st = this.#store.get();
    if (st.activeTask !== 'C-image') { this.#refs.objectZoneReadout.textContent = ''; return; }
    const zone = objectZone(st.lensF_mm, st.objectDistanceMm);
    this.#refs.objectZoneReadout.textContent = `Зона: ${zoneLabelRu(zone)}`;
  }
```

(б) Вызвать `#applyObjectSliderRange(task)` в `setActiveTask` (перед `#syncObjectSlider`) и в конструкторе (перед `#refreshObjectSliderVisibility`). Вызвать `#updateZoneReadout()` в `#afterBenchChange` (в конце), `setActiveTask`, `setObjectDistanceMm`, `reset`.

(в) **Ветка C в `HintEngine.update`** (иначе для C показывается чужой хинт «двигайте экран до резкости»). В классе `HintEngine` сразу ПОСЛЕ блока проверок отсутствующих приборов (перед `const d = st.objectDistanceMm;`) добавить:
```ts
    if (st.activeTask === 'C-image') {
      const zone = objectZone(st.lensF_mm, st.objectDistanceMm);
      this.#set(`Двигайте предмет по зонам. Сейчас: ${zoneLabelRu(zone)}. Запишите наблюдение и классифицируйте изображение.`);
      return;
    }
```
`HintEngine` использует `objectZone`/`zoneLabelRu` — они уже импортированы из `@physics/optics/LensModel` (Step 3а). Хинт-бар не aria-live для категорий (a11y ок: зона ≠ категория-ответ).

- [ ] **Step 5: onChoiceInput + запись строки C**

(а) В `#refreshUi`, в вызове `renderJournalTable`, добавить колбэк `onChoiceInput` рядом с `onCellInput`:
```ts
        onChoiceInput: (rowIdx, key, value) => {
          const m = taskMeasurements[rowIdx - 1];
          if (!m) return;
          const drafts = this.#journalDrafts.get(m.timestamp) ?? {};
          if (value !== null) drafts[key] = value; else delete drafts[key];
          this.#journalDrafts.set(m.timestamp, drafts);
        },
```

(б) В `recordMeasurement` добавить ветку C ПЕРЕД веткой B (или внутри if-цепочки). Для C запись допустима при собранной топологии (резкость НЕ требуется — классификация не зависит от экрана):
```ts
    if (st.activeTask === 'C-image') {
      const d = st.objectDistanceMm;
      const F = st.lensF_mm;
      const g = magnification(F, d);
      const zone = zoneLabelRu(objectZone(F, d));
      const measurement: BenchMeasurement = {
        id: `m-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`,
        timestamp: Date.now(),
        task: 'C-image',
        d_mm: d,
        f_mm: st.screenDistanceMm,
        F_mm: F,
        D_dptr: 0,
        zoneLabel: zone,
      };
      this.#store.update((s) => ({ measurements: [...s.measurements, measurement] }));
      this.#lastRecordedSignature = this.#pendingSignature();
      this.#refreshUi();
      // a11y: НЕ озвучивать категорию в semi/manual.
      this.#hints.announce(
        this.#recordMode() === 'fully-auto'
          ? `Записана зона ${zone}.`
          : `Записана зона ${zone}. Классифицируйте изображение в журнале и проверьте.`,
      );
      void g; // gamma считается эталоном в spec; здесь не печатаем (a11y)
      return;
    }
```
(Существующие ветки B и A оставить как есть, ниже.)

(в) `#buildJournalRow` — ветка C (ПЕРЕД веткой B):
```ts
    if (m.task === 'C-image') {
      const props = imageProperties(m.F_mm, m.d_mm);
      const auto = isFullyAuto;
      return {
        idx,
        timestamp: m.timestamp,
        values: {
          idx,
          zone: m.zoneLabel ?? '',
          // скрытый числовой контекст для грейда (render не покажет — нет колонок d_mm/F_mm)
          d_mm: m.d_mm,
          F_mm: m.F_mm,
          kind: auto ? props.kind : null,
          orientation: auto ? props.orientation : null,
          size: auto ? props.size : null,
          gamma: auto ? (Number.isFinite(props.gamma) ? props.gamma : null) : null,
        },
      };
    }
```

- [ ] **Step 6: Гейтинг записи/режимов для C (без авто-флуда)**

(а) В `#afterBenchChange` — авто-запись (fully-auto) ТОЛЬКО для A/B (для C запись по явному действию, иначе флуд при движении слайдера):
```ts
      const canAutoRecord =
        st.activeTask === 'C-image'
          ? false
          : st.activeTask === 'B-focal2f'
            ? (this.isSharp && this.isSizesEqual)
            : this.isSharp;
```

**Dual-site (verify mustFix):** ТО ЖЕ самое в `#handleRecordModeChange` (~строка 666) — иначе переключение record-mode на fully-auto при собранной топологии C вызовет авто-запись (флуд). Заменить дословно:
```ts
    const canAuto =
      st.activeTask === 'C-image'
        ? false
        : st.activeTask === 'B-focal2f'
          ? (this.isSharp && this.isSizesEqual)
          : this.isSharp;
```
Добавить тест в Step 1: `'fully-auto: переключение режима при задаче C НЕ вызывает авто-запись'` —
```ts
  it('fully-auto: смена режима при задаче C НЕ авто-записывает (антифлуд)', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('C-image');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('screen', 'screen');
    exp.setObjectDistanceMm(300);
    localStorage.setItem('inter-oge.record-mode.kit-4', 'fully-auto');
    (exp as unknown as { '#handleRecordModeChange'?: () => void });
    // триггерим обработчик смены режима тем же путём, что UI (через публичный метод/событие тулбара)
    window.dispatchEvent(new Event('storage')); // или вызвать публичный re-sync, если он есть
    expect(exp.measurements.filter((m) => m.task === 'C-image').length).toBe(0);
  });
```
> Замечание: если `#handleRecordModeChange` нельзя дёрнуть из теста публично — проверь через тот же канал, что и реальный record-mode toggle (его `onChange`), либо вынеси минимальный публичный хук. Тест должен доказать ОТСУТСТВИЕ авто-записи именно на пути смены режима, не только на пути слайдера (тест M2 этого не ловит).

(б) `#refreshUi` — pending-плашка: для C показывать кнопку «Записать зону» при собранной топологии и новой зоне (в ЛЮБОМ режиме, т.к. авто-записи нет); для A/B оставить semi-auto-only:
```ts
    const canRecordNow =
      st.activeTask === 'C-image' ? ok
      : st.activeTask === 'B-focal2f' ? (ok && this.isSharp && this.isSizesEqual)
      : (ok && this.isSharp);
    const isPending = canRecordNow && this.#pendingSignature() !== this.#lastRecordedSignature;
    const mode = this.#recordMode();
    if (this.#refs.recordPendingSlot) {
      const showPending = st.activeTask === 'C-image'
        ? isPending
        : (isPending && mode === 'semi-auto');
      this.#refs.recordPendingSlot.hidden = !showPending;
    }
    if (this.#refs.recordPendingSummary && isPending) {
      this.#refs.recordPendingSummary.textContent =
        st.activeTask === 'C-image' ? `Зона ${zoneLabelRu(objectZone(st.lensF_mm, st.objectDistanceMm))}`
        : st.activeTask === 'B-focal2f' ? `2F=${st.objectDistanceMm.toFixed(0)} мм`
        : `d=${st.objectDistanceMm.toFixed(0)} мм, f=${st.screenDistanceMm.toFixed(0)} мм`;
    }
```

(в) `#refreshUi` result-panel — ветка C (a11y: НЕ печатать категории в semi/manual):
```ts
      if (st.activeTask === 'C-image') {
        const last = taskMeasurements[taskMeasurements.length - 1]!;
        // zoneLabel опционально → пересчитать из физики (иначе литерал 'undefined' в HTML).
        const zLabel = last.zoneLabel ?? zoneLabelRu(objectZone(last.F_mm, last.d_mm));
        const isEqF = objectZone(last.F_mm, last.d_mm) === 'eqF';
        // a11y: при d=F подсказать оставить Γ пустым (категорию НЕ палим).
        const gammaNote = isEqF ? ' При d = F изображение в бесконечности — Γ не определено, оставьте поле Γ пустым.' : '';
        if (isFullyAuto) {
          const p = imageProperties(last.F_mm, last.d_mm);
          const ru = { real:'действительное', virtual:'мнимое', inverted:'перевёрнутое', upright:'прямое', enlarged:'увеличенное', reduced:'уменьшенное', equal:'равное' } as const;
          html =
            `<p class="result-line">Зона ${zLabel}: <strong>${ru[p.kind]}, ${ru[p.orientation]}, ${ru[p.size]}</strong></p>` +
            `<p class="result-note">Сравните с вашей классификацией в журнале.${gammaNote}</p>`;
        } else {
          html =
            `<p class="result-line">Зона ${zLabel} записана.</p>` +
            `<p class="result-note">Выберите вид, ориентацию и размер изображения в журнале и проверьте (✓).${gammaNote}</p>`;
        }
      } else if (st.activeTask === 'B-focal2f') {
        // ... существующая ветка B без изменений ...
```
(Вставить ветку C первой в существующую if/else-цепочку result-panel.)

- [ ] **Step 7: Запустить тесты задачи C + регрессия A/B**

Run: `npm test --workspace=@labosfera/kit-4-optics -- lens-bench`
Expected: PASS — задача C + прежние тесты A/B (изоляция, фильтр measurements).

- [ ] **Step 8: Гейты воркспейса kit-4**

Run: `npm run typecheck && npm test --workspace=@labosfera/kit-4-optics`
Expected: PASS.

- [ ] **Step 9: Commit**

```bash
git add experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts \
        experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(lens-bench): задача C — опыт 4.4 свойства изображения по 5 зонам

LensTaskId += C-image; слайдер предмета 50–350 (достаёт <F и >2F); живой
зона-ридаут из objectZone(imageProperties); запись по зоне без авто-флуда
(явная кнопка во всех режимах); категориальные drafts (string) + грейд через
✓; fully-auto авто-заполняет категории; a11y — категория не палится в semi/manual.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Шаблон + экран-обёртка + CSS + каталог

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/template.html`
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts`
- Modify: `experiments/kit-4-optics/src/styles/lens-bench-experiment.css`
- Modify: `experiments/home/src/data/kits.ts`

**Interfaces:**
- Consumes: `ExperimentRefs.objectZoneReadout` (Task 5).
- Produces: шаг C в `#steps`; `#object-zone-readout` + легенда зон; ref `objectZoneReadout`; каталог 4.4 `done`.

- [ ] **Step 1: template.html — шаг C, зона-ридаут, легенда зон**

(а) В `<ol id="steps">` после `<li data-task="B-focal2f">…</li>` добавить:
```html
        <li class="step" data-task="C-image" tabindex="0" role="button" aria-current="false">
          <span class="step-num">C</span>
          <span class="step-label">Свойства изображения <small>(4.4)</small></span>
        </li>
```

(б) В `#object-slider-row` после `<output id="object-slider-readout">` добавить зона-ридаут и легенду (легенда — статические метки 5 зон, гайд для слайдера; помечена `aria-hidden`, ридаут озвучивает зону):
```html
          <output id="object-zone-readout" class="object-zone-readout" for="object-slider" aria-live="polite"></output>
          <div class="zone-legend" aria-hidden="true">
            <span>&lt;F</span><span>=F</span><span>F&lt;d&lt;2F</span><span>=2F</span><span>&gt;2F</span>
          </div>
```

- [ ] **Step 2: LensBenchScreen.ts — ref objectZoneReadout**

В объект `refs` (после `objectSliderReadout`) добавить:
```ts
      objectZoneReadout: host.querySelector<HTMLElement>('#object-zone-readout') ?? undefined,
```

- [ ] **Step 3: CSS — зона-чип и легенда**

В `experiments/kit-4-optics/src/styles/lens-bench-experiment.css` добавить:
```css
.object-zone-readout {
  font-family: var(--font-mono, monospace);
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--color-brand-amber, #ffbe0b);
  margin-left: 0.5rem;
  min-width: 8ch;
}
.zone-legend {
  display: flex;
  justify-content: space-between;
  width: 100%;
  margin-top: 2px;
  font-size: 0.62rem;
  color: rgb(255 255 255 / 0.4);
  font-family: var(--font-mono, monospace);
}
.object-zone-readout:empty { display: none; }
```

- [ ] **Step 4: kits.ts — опыт 4.4 done**

В `experiments/home/src/data/kits.ts` найти kit-4, опыт с ФИПИ-id 4.4; выставить `id: 'lens-bench'`, `done: true` (по образцу 4.2 из Фазы B). Статус кита НЕ менять (остаётся `planned` до Фазы F).

> Замечание: deep-link `?task=` (4.1/4.2/4.4 делят слаг `lens-bench` → запуск 4.4 c главной открывает A) — известный долг, выносится в Фазу F. Здесь НЕ чинить.

- [ ] **Step 5: Гейты + сборка**

Run: `npm run typecheck && npm test --workspace=@labosfera/kit-4-optics && npm run build --workspace=@labosfera/kit-4-optics && npm run build --workspace=labosfera-home`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add experiments/kit-4-optics/src/screens/lens-bench/template.html \
        experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts \
        experiments/kit-4-optics/src/styles/lens-bench-experiment.css \
        experiments/home/src/data/kits.ts
git commit -m "$(cat <<'EOF'
feat(lens-bench): шаг C в свитчере + зона-ридаут/легенда + каталог 4.4 done

Шаг C (4.4) в task-switcher; #object-zone-readout (aria-live) + статическая
легенда 5 зон под слайдером предмета; kits.ts опыт 4.4 → done на экране
lens-bench. Статус кита остаётся planned (Фаза F). Deep-link ?task= — долг Фазы F.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `selfcheck-4-4.mjs` + axe + REFERENCE §30 + полные гейты

**Files:**
- Create: `experiments/kit-4-optics/selfcheck-4-4.mjs` (gitignored, `git add -f`)
- Modify: `experiments/2-1-spring/REFERENCE.md` (§30 статус-строка 4.4)

**Interfaces:**
- Consumes: всё предыдущее (запущенный дев-сервер kit-4, `window.lensBenchExperiment`).

- [ ] **Step 1: selfcheck-4-4.mjs — multi-state + REST + 3 режима + a11y + axe**

Создать `experiments/kit-4-optics/selfcheck-4-4.mjs` по образцу `selfcheck-4-2.mjs` (Playwright, `@axe-core/playwright`). Сценарий (каждый assert логирует PASS/FAIL, итог `N passed / M failed`):

1. Навигация на `http://127.0.0.1:5191/?screen=lens-bench`; дождаться `lab-optical-bench`.
2. REST-state: `.slot-rect` скрыты (opacity 0) до drag.
3. Переключить на задачу C (`[data-task="C-image"]` click); проверить `aria-current="true"`, слайдер предмета виден, диапазон min≤60/max≥300.
4. D&D реальной мышью (`page.mouse.move/down/up`) осветитель→`bench-slot-object`, линза→lens, экран→screen; подтвердить `card[data-placed]` ×3.
5. Сметнуть по 5 зонам слайдером предмета (set value + dispatch input через `window.lensBenchExperiment.setObjectDistanceMm(...)` ИЛИ реальный drag слайдера), на каждой проверить `#object-zone-readout` текст: 300→«d > 2F», 200→«d = 2F», 150→«F < d < 2F», 100→«d = F», 60→«d < F».
6. Визуальный инвариант (читать через `evaluate` shadowRoot): d=300 → `#projected-image-group` виден, `#virtual-image-group`/`#infinity-note-group` скрыты; d=60 → virtual виден и ПРЯМОЙ (transform без `scale(1,-`); d=100 → infinity note виден.
7. semi-auto: записать строку в зоне d=300 (кнопка `#record-pending-btn`); журнал рендерит `<select data-key="kind|orientation|size">`; выбрать ВЕРНО (real/inverted/reduced) + Γ=−0.5, нажать ✓ → `td[data-verdict="ok"]`; затем перезаписать с НЕВЕРНЫМ выбором (virtual) → `[data-verdict="wrong"]`.
8. a11y-утечка: `#live-region` и `#result-panel` НЕ содержат слов «действительное/перевёрнутое/уменьшенное» в semi-auto.
9. fully-auto (localStorage `inter-oge.record-mode.kit-4=fully-auto`, перезагрузка): запись зоны → `td[data-key="kind"]` текст «действительное» (авто-заполнено).
10. 3 режима записи различимы (semi-auto select-ввод / fully-manual без verdict-подсветки / fully-auto readonly-метка) — реальный ключ `inter-oge.record-mode.kit-4`.
11. Изоляция: переключить на A → журнал C-строк не показывает.
12. axe (`AxeBuilder`): 0 нарушений на экране с задачей C.

Запуск дев-сервера (Git Bash, фон):
`npm run dev --workspace=@labosfera/kit-4-optics -- --host 127.0.0.1 --port 5191 --strictPort`
Затем `node experiments/kit-4-optics/selfcheck-4-4.mjs`.

- [ ] **Step 2: Прогнать selfcheck до PASS**

Run (Git Bash): запустить дев на 5191, затем `node experiments/kit-4-optics/selfcheck-4-4.mjs`.
Expected: `N passed / 0 failed`. Чинить причину (не подгонять тест).

- [ ] **Step 3: REFERENCE §30 — статус 4.4**

В `experiments/2-1-spring/REFERENCE.md` §30 в блоке комплекта №4 пометить опыт 4.4 как реализованный (одна строка статуса, по образцу 4.1/4.2). Не разворачивать блок «Комплект №4» целиком — это Фаза F.

- [ ] **Step 4: Полные гейты монорепо (из корня — урок Фазы B)**

Run: `npm run typecheck && npm run lint && npm test && npm run build`
Expected: TYPECHECK_EXIT=0, LINT_EXIT=0, TEST_EXIT=0 (весь монорепо), BUILD_EXIT=0.

- [ ] **Step 5: Commit**

```bash
git add -f experiments/kit-4-optics/selfcheck-4-4.mjs
git add experiments/2-1-spring/REFERENCE.md
git commit -m "$(cat <<'EOF'
test(kit-4): selfcheck-4-4 (5 зон, через-линзу, choice-журнал, 3 режима, axe) + REFERENCE §30

Полный self-check опыта 4.4: REST, реальный D&D, зона-ридаут по 5 зонам,
визуальный инвариант (действит./мнимое/бесконечность), категориальный грейд
ok/wrong, a11y без утечки категории, 3 режима, изоляция задач, axe 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review (выполнено автором плана)

**1. Покрытие спеки §4.1.C / §7 / §13:**
- Категориальные колонки журнала → Task 1 (инфра `choice`) + Task 3 (`IMAGE_PROPERTIES_SPEC`). ✔ («по §17.1 поверх v2, не вместо» = расширение общей инфры, не кастомный журнал.)
- 5 зон + слайдер с метками → Task 5 (диапазон+зона-ридаут) + Task 6 (легенда). ✔
- «Через линзу» (мнимое/бесконечность) → Task 4. ✔
- Риск §13 «категориальные не вписываются» → решён расширением (Task 1). ✔
- Риск §13 «рендер резкости/изображения» → reality-check в Task 7 + unit Task 4. ✔
- idx-first (Global Constraint 4) → Task 3 (колонка idx первой; `it.each(ALL_SPECS)` зелёный в Step 5). ✔
- A11y без утечки (Constraint 6) → Task 5 (result/announce) + Task 7 (selfcheck assert). ✔
- DoD: ФИПИ-якорь (docstring spec+опыт), журнал 3 режима, физика+фаззинг (Task 2), D&D+REST (Task 7), axe 0 (Task 7), unit зелёные, selfcheck PASS, ручной проход/reality-check (после Task 7), kits.ts (Task 6), REFERENCE §30 (Task 7). ✔

**2. Плейсхолдеры:** код приведён в каждом шаге; «по образцу selfcheck-4-2» в Task 7 — допустимо (полный selfcheck-сценарий перечислен по пунктам, не «TODO»).

**3. Согласованность типов:** `LensTaskId` ('A-power'|'B-focal2f'|'C-image') — Task 5; `objectZone`/`zoneLabelRu` — Task 2 потребляются в Task 5; `IMAGE_PROPERTIES_SPEC` колонки idx/zone/kind/orientation/size/gamma — Task 3 ↔ ветки `#buildJournalRow`/`recordMeasurement` Task 5 (ключи совпадают: zone/d_mm/F_mm/kind/orientation/size/gamma); `onChoiceInput(rowIdx,key,value:string|null)` — Task 1 ↔ Task 5; drafts `Record<string, number|string>` — Task 5. Скрытые d_mm/F_mm в row.values читаются `expectedChoiceFromRow`/`expectedFromRow` (Task 3) через числовой контекст verify (Task 1). ✔

**Ключевые проектные решения (зафиксированы):**
- Контекст грейда d_mm/F_mm — НЕ колонки (render показывает только `spec.columns`), F скрыт от ученика.
- Зона-ридаут и грейд из ОДНОЙ `imageProperties` → нет трапа рассинхрона (Constraint 8).
- d=F: gamma пустой → 'empty' (не 'wrong'); категории по контракту imageProperties.
- Авто-запись fully-auto отключена для C (антифлуд, урок Фазы A) → явная кнопка «Записать зону» во всех режимах.

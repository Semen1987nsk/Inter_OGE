# Кит-3 Фаза D «Сопротивление проводника» (опыты 3.5/3.6/3.7) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Довести опыты ФИПИ **3.5 R(длина)**, **3.6 R(сечение)**, **3.7 R(ρ)** до полного канона на новом экране `wire-resistance` пакета `kit-3-circuits`.

**Architecture:** Новый экран `wire-resistance` по образцу `measurements` (мульти-таск task-switcher A-length/B-section/C-rho). Метод тот же, что 3.1 — R=U/I через сборку цепи (`CircuitAssembly` + `CircuitDragController` + `lab-circuit-board`). Исследуемый элемент — дискретный «набор проволочных резисторов» (новый компонент `lab-wire-resistor`, по 3 карточки на задачу, панель фильтруется по активной задаче). Физика — чистая `CircuitModel.wireResistance(ρ,l,S)=ρl/S`. Журнал v2 — три SPEC, зеркало `RESISTANCE_SPEC` (R_Ohm=U/I грейдится); теоретическое R=ρl/S и вывод о пропорциональности — в result-panel (НЕ грейдимая колонка, т.к. журнал v2 `derived` = ученик считает + tolerance).

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components, Vitest (happy-dom), Playwright, npm workspaces.

**Спека:** docs/superpowers/specs/2026-06-25-wave-1-kit-3-circuits.md (§6 экраны, §4 физика, §7 журнал). **Эпик:** docs/superpowers/specs/2026-06-24-fipi-full-coverage-epic.md.

## Global Constraints

(Копия из эпика §5 / спеки; verbatim.)
- **ФИПИ-якорь** в docstring каждого `*Experiment.ts`/SPEC: дословная цитата СПЕЦ Прил.2 компл.№3 (сноска 3) пп.5/6/7 + КОДИФ §1.29.
- **Journal v2 (§21)**: `renderJournalTable(host, SPEC, rows, {mode,onCellInput,onVerify})` + `JournalSpec` в `_shared-spa/.../journal/specs.ts` + `verifyRow` + `parseRu`; 3 режима через `record-mode` ключ `'kit-3'`.
- **Чистая физика** в `src/physics/circuit/`, Vitest + property-фаззинг; `RangeError` на невалидных входах (паттерн `current`/`lampCurrent`).
- **D&D через mouse** в selfcheck (`page.mouse.move/down/up`); подтверждать дроп через `card[data-placed]`; REST-state скрыт в покое.
- **A11y**: axe 0; `aria-live`; `svg[hidden]{display:none}` в shadow CSS каждого `lab-*`; прибор со шкалой — диапазон ≥ рабочего значения (урок Фазы C: слайдер ≤ range вольтметра).
- **Алиасы** kit-3: `@shared/*`, `@labosfera/shared-spa/*`, `@screens/*`, `@shell/*`, `@physics/*`, `@ui/*`, `@controller/*`, `@/*`.
- **Гейты** (из корня, Bash/Git Bash): `npm run typecheck` · `test` · `lint` (0 err) · `build` — все `--workspaces`. Плюс `selfcheck-3-5-3-7.mjs` PASS + ручной reality-check скрином (читать LCD приборов, не только журнал).
- **Платформа**: только `C:\dev\Inter_OGE`; dev `--host 127.0.0.1 --port <free> --strictPort`.
- **Единичные кавычки** (repo style); не коммитить `dist/`/`tsbuildinfo`/`package-lock`; явные пути в `git add` (не `-A`); selfcheck-*.mjs gitignored → `git add -f`.
- **Коммиты**: по-русски, императив; в конце каждого `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Набор проволок (эталон, проверено: R∈[2,2;8,8] Ом → при Umax=6 В I≤2,7 А ≤ амперметр 0–3 А)

ρ, Ом·м: нихром 1,1·10⁻⁶ · константан 0,5·10⁻⁶ · никелин 0,4·10⁻⁶.

| Задача | equipmentId | материал | l, м | S, мм² | R=ρl/S, Ом |
|---|---|---|---|---|---|
| A длина (3.5) | wire-len-05 | нихром | 0,5 | 0,25 | 2,2 |
| A длина | wire-len-10 | нихром | 1,0 | 0,25 | 4,4 |
| A длина | wire-len-20 | нихром | 2,0 | 0,25 | 8,8 |
| B сечение (3.6) | wire-sec-025 | нихром | 2,0 | 0,25 | 8,8 |
| B сечение | wire-sec-05 | нихром | 2,0 | 0,5 | 4,4 |
| B сечение | wire-sec-10 | нихром | 2,0 | 1,0 | 2,2 |
| C материал (3.7) | wire-mat-ni | нихром | 2,0 | 0,25 | 8,8 |
| C материал | wire-mat-ko | константан | 2,0 | 0,25 | 4,0 |
| C материал | wire-mat-nk | никелин | 2,0 | 0,25 | 3,2 |

---

## File Structure

| Файл | Ответственность |
|---|---|
| `kit-3-circuits/src/physics/circuit/CircuitModel.ts` (+ `__tests__/CircuitModel.test.ts`) | += `wireResistance(ρ,l,S)` + `RESISTIVITY` map |
| `_shared-spa/src/lib/journal/specs.ts` (+ `__tests__/specs.test.ts`) | += `R_LENGTH_SPEC`/`R_AREA_SPEC`/`R_RHO_SPEC` (+ в `ALL_SPECS`) |
| `kit-3-circuits/src/ui/components/lab-wire-resistor.ts` (+ `__tests__/components.test.ts`) | НОВЫЙ прибор: катушка проволоки + паспорт (материал/l/S), без R |
| `kit-3-circuits/src/screens/wire-resistance/WireResistanceScreen.ts` · `WireResistanceExperiment.ts` · `template.html` (+ `__tests__/wire-resistance.test.ts`) | НОВЫЙ экран, мульти-таск (зеркало measurements) |
| `kit-3-circuits/src/styles/wire-resistance-experiment.css` | стиль экрана (пер-скрин layout) |
| `kit-3-circuits/src/main.ts` | регистрация `WireResistanceScreen` + import `lab-wire-resistor` + CSS |
| `kit-3-circuits/selfcheck-3-5-3-7.mjs` | e2e self-check 3 опытов (реальный mouse-D&D смены проволок) |
| `experiments/home/src/data/kits.ts` (+ test) | опыты 3.5/3.6/3.7 done=true |

union `experimentId` ('3.5'..'3.7') и `ScreenId`/`IconId` ('wire-resistance'/'wire') УЖЕ есть (Фаза A) — не трогать.

---

## Task 1: CircuitModel.wireResistance + RESISTIVITY

**Files:**
- Modify: `kit-3-circuits/src/physics/circuit/CircuitModel.ts`
- Test: `kit-3-circuits/src/physics/circuit/__tests__/CircuitModel.test.ts`

**Interfaces:**
- Produces: `export const RESISTIVITY: { нихром: number; константан: number; никелин: number }`; `export function wireResistance(rho_Ohm_m: number, length_m: number, area_m2: number): number` (бросает `RangeError` на нечисловых/неположительных).

- [ ] **Step 1: Падающие тесты** — добавить в `CircuitModel.test.ts`:

```ts
import { wireResistance, RESISTIVITY } from '../CircuitModel';

describe('wireResistance R=ρl/S', () => {
  it('номинал набора: нихром l=1,0 S=0,25мм² → 4,4 Ом', () => {
    expect(wireResistance(RESISTIVITY.нихром, 1.0, 0.25e-6)).toBeCloseTo(4.4, 5);
  });
  it('R ∝ l: удвоение длины удваивает R', () => {
    const r1 = wireResistance(RESISTIVITY.нихром, 1.0, 0.25e-6);
    const r2 = wireResistance(RESISTIVITY.нихром, 2.0, 0.25e-6);
    expect(r2 / r1).toBeCloseTo(2, 6);
  });
  it('R ∝ 1/S: удвоение сечения вдвое уменьшает R', () => {
    const r1 = wireResistance(RESISTIVITY.нихром, 2.0, 0.25e-6);
    const r2 = wireResistance(RESISTIVITY.нихром, 2.0, 0.5e-6);
    expect(r1 / r2).toBeCloseTo(2, 6);
  });
  it('R ∝ ρ: отношение R = отношению ρ', () => {
    const rNi = wireResistance(RESISTIVITY.нихром, 2.0, 0.25e-6);
    const rKo = wireResistance(RESISTIVITY.константан, 2.0, 0.25e-6);
    expect(rNi / rKo).toBeCloseTo(RESISTIVITY.нихром / RESISTIVITY.константан, 6);
  });
  it.each([[0, 1, 1e-6], [1e-6, 0, 1e-6], [1e-6, 1, 0], [-1e-6, 1, 1e-6], [NaN, 1, 1e-6]])(
    'RangeError на невалидных (%p,%p,%p)', (rho, l, s) => {
      expect(() => wireResistance(rho, l, s)).toThrow(RangeError);
    });
  it('фаззинг: R>0 и монотонно растёт по l при фикс ρ,S', () => {
    for (let i = 0; i < 200; i++) {
      const l1 = 0.1 + i * 0.01, l2 = l1 + 0.05;
      const r1 = wireResistance(RESISTIVITY.нихром, l1, 0.5e-6);
      const r2 = wireResistance(RESISTIVITY.нихром, l2, 0.5e-6);
      expect(r1).toBeGreaterThan(0);
      expect(r2).toBeGreaterThan(r1);
    }
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`wireResistance is not a function`).

Run (Git Bash): `npm --workspace=@labosfera/kit-3-circuits run test -- CircuitModel`

- [ ] **Step 3: Реализация** — добавить в `CircuitModel.ts` (после lamp-секции, перед концом файла). Используй существующий guard-стиль файла (как в `current`/`lampCurrent`):

```ts
/** Удельные сопротивления (Ом·м) проволочных материалов набора кит-3. */
export const RESISTIVITY = {
  нихром: 1.1e-6,
  константан: 0.5e-6,
  никелин: 0.4e-6,
} as const;

/**
 * Сопротивление проволочного резистора: R = ρ·l/S.
 * rho_Ohm_m — удельное сопротивление (Ом·м); length_m — длина (м);
 * area_m2 — площадь поперечного сечения (м²). R=ρl/S (спека §4).
 */
export function wireResistance(rho_Ohm_m: number, length_m: number, area_m2: number): number {
  if (!Number.isFinite(rho_Ohm_m) || !Number.isFinite(length_m) || !Number.isFinite(area_m2)) {
    throw new RangeError('wireResistance: ρ, l, S должны быть конечными числами');
  }
  if (rho_Ohm_m <= 0 || length_m <= 0 || area_m2 <= 0) {
    throw new RangeError('wireResistance: ρ, l, S должны быть положительными');
  }
  return (rho_Ohm_m * length_m) / area_m2;
}
```

- [ ] **Step 4: Прогнать — PASS.** `npm --workspace=@labosfera/kit-3-circuits run test -- CircuitModel`
- [ ] **Step 5: Commit** — `git add` обоих файлов; сообщение `feat(kit-3): CircuitModel.wireResistance R=ρl/S + RESISTIVITY (опыты 3.5–3.7)` + Co-Authored-By.

---

## Task 2: Три SPEC журнала (R_LENGTH/R_AREA/R_RHO)

**Files:**
- Modify: `_shared-spa/src/lib/journal/specs.ts` (после `IV_CURVE_SPEC`; добавить в `ALL_SPECS`)
- Test: `_shared-spa/src/lib/journal/__tests__/specs.test.ts`

**Interfaces:**
- Produces: `export const R_LENGTH_SPEC`, `R_AREA_SPEC`, `R_RHO_SPEC: JournalSpec` (experimentId '3.5'/'3.6'/'3.7'). Колонки идентичны: meta `material`/`l_m`/`S_mm2`, direct `U_V`/`I_A`, derived `R_Ohm` (= U/I, tolerance 0,10).

- [ ] **Step 1: Падающие тесты** — добавить в `specs.test.ts`:

```ts
import { R_LENGTH_SPEC, R_AREA_SPEC, R_RHO_SPEC, ALL_SPECS } from '../specs';

describe('Wire-resistance specs 3.5–3.7', () => {
  it.each([R_LENGTH_SPEC, R_AREA_SPEC, R_RHO_SPEC])('зарегистрирован в ALL_SPECS', (s) => {
    expect(ALL_SPECS).toContain(s);
  });
  it('R_Ohm = U/I с допуском 0,10', () => {
    const col = R_LENGTH_SPEC.columns.find((c) => c.key === 'R_Ohm')!;
    expect(col.source).toBe('derived');
    expect(col.tolerance).toBe(0.10);
    expect(col.expectedFromRow!({ U_V: 4.4, I_A: 1.0 })).toBeCloseTo(4.4, 6);
  });
  it('I=0 → expected 0 (без деления на ноль)', () => {
    const col = R_AREA_SPEC.columns.find((c) => c.key === 'R_Ohm')!;
    expect(col.expectedFromRow!({ U_V: 4, I_A: 0 })).toBe(0);
  });
  it('паспортные колонки material/l_m/S_mm2 — meta', () => {
    for (const key of ['material', 'l_m', 'S_mm2']) {
      expect(R_RHO_SPEC.columns.find((c) => c.key === key)!.source).toBe('meta');
    }
  });
  it('experimentId соответствует опыту', () => {
    expect([R_LENGTH_SPEC.experimentId, R_AREA_SPEC.experimentId, R_RHO_SPEC.experimentId])
      .toEqual(['3.5', '3.6', '3.7']);
  });
});
```

- [ ] **Step 2: Прогнать — FAIL.** `npm --workspace=@labosfera/shared-spa run test -- specs`
- [ ] **Step 3: Реализация** — добавить в `specs.ts` (после `IV_CURVE_SPEC`):

```ts
// Общие колонки 3.5–3.7: метод R=U/I, варьируемый параметр виден в паспортных колонках.
const WIRE_COLUMNS: ColumnSpec[] = [
  { key: 'idx', label: '№', source: 'meta', format: 'int' },
  { key: 'material', label: 'Материал', source: 'meta' },
  { key: 'l_m', label: 'l, м', source: 'meta', unit: 'м', format: 'fixed1' },
  { key: 'S_mm2', label: 'S, мм²', source: 'meta', unit: 'мм²', format: 'fixed2' },
  { key: 'U_V', label: 'U, В', source: 'direct', unit: 'В', format: 'fixed2' },
  { key: 'I_A', label: 'I, А', source: 'direct', unit: 'А', format: 'fixed2' },
  {
    key: 'R_Ohm', label: 'R, Ом', source: 'derived', unit: 'Ом', format: 'fixed2',
    tolerance: 0.10, // допуск на арифметику ученика: R vs U/I (10%); НЕ интервал приёмки ФИПИ
    expectedFromRow: (row) => { const I = row.I_A ?? 0; return I > 0 ? (row.U_V ?? 0) / I : 0; },
  },
];

/**
 * Опыт 3.5 «Зависимость R проводника от длины» — R(l), метод R=U/I.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.5; КОДИФ §1.29.
 * Набор: нихром S=0,25 мм², l=0,5/1,0/2,0 м → R=2,2/4,4/8,8 Ом (R∝l).
 */
export const R_LENGTH_SPEC: JournalSpec = { experimentId: '3.5', kitId: 'kit-3', columns: WIRE_COLUMNS };

/**
 * Опыт 3.6 «Зависимость R проводника от площади сечения» — R(S), метод R=U/I.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.6; КОДИФ §1.29.
 * Набор: нихром l=2,0 м, S=0,25/0,5/1,0 мм² → R=8,8/4,4/2,2 Ом (R∝1/S).
 */
export const R_AREA_SPEC: JournalSpec = { experimentId: '3.6', kitId: 'kit-3', columns: WIRE_COLUMNS };

/**
 * Опыт 3.7 «Зависимость R проводника от удельного сопротивления» — R(ρ), метод R=U/I.
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№3 (сноска 3) п.7; КОДИФ §1.29.
 * Набор: l=2,0 м S=0,25 мм², нихром/константан/никелин → R=8,8/4,0/3,2 Ом (R∝ρ).
 */
export const R_RHO_SPEC: JournalSpec = { experimentId: '3.7', kitId: 'kit-3', columns: WIRE_COLUMNS };
```

И добавить три спеки в массив `ALL_SPECS` (рядом с `IV_CURVE_SPEC`). Убедись, что `ColumnSpec` импортирован в файле (если ещё нет — добавить в import из `./types`).

- [ ] **Step 4: Прогнать — PASS** (specs + полный `_shared-spa`).
- [ ] **Step 5: Commit** — `feat(journal): R_LENGTH/R_AREA/R_RHO SPEC опытов 3.5–3.7 (R=U/I)` + Co-Authored-By.

---

## Task 3: Компонент lab-wire-resistor

**Files:**
- Create: `kit-3-circuits/src/ui/components/lab-wire-resistor.ts`
- Test: `kit-3-circuits/src/ui/components/__tests__/components.test.ts` (добавить блок)

**Interfaces:**
- Produces: custom element `<lab-wire-resistor material="нихром" length="1.0" area="0.25">` (length в м, area в мм²). Геттеры `material:string`, `length:number`, `area:number`. Рендерит катушку/проволоку на держателе + паспортную подпись «нихром · l=1,0 м · S=0,25 мм²». **R НЕ показывает** (ученик измеряет). `aria-label` = тот же паспорт без R. `role="img"` на SVG. `svg[hidden]{display:none}` в shadow CSS. Клеммы — геометрический API `getTerminalPositions()` как у `lab-resistor` (нужно для привязки в `CircuitAssembly`).

- [ ] **Step 1: Падающие тесты** — добавить в `components.test.ts`:

```ts
import '../lab-wire-resistor';

describe('lab-wire-resistor', () => {
  function make(attrs: Record<string,string>) {
    const el = document.createElement('lab-wire-resistor');
    for (const [k,v] of Object.entries(attrs)) el.setAttribute(k, v);
    document.body.appendChild(el); return el as any;
  }
  it('паспорт в подписи: материал, l, S — без R/Ом', () => {
    const el = make({ material: 'нихром', length: '1.0', area: '0.25' });
    const txt = el.shadowRoot.textContent;
    expect(txt).toContain('нихром');
    expect(txt).toContain('1,0');
    expect(txt).toContain('0,25');
    expect(txt).not.toMatch(/Ом/);
  });
  it('геттеры', () => {
    const el = make({ material: 'константан', length: '2.0', area: '0.5' });
    expect(el.material).toBe('константан');
    expect(el.length).toBeCloseTo(2.0); expect(el.area).toBeCloseTo(0.5);
  });
  it('aria-label = паспорт без R', () => {
    const el = make({ material: 'никелин', length: '2.0', area: '0.25' });
    const label = el.getAttribute('aria-label') ?? el.shadowRoot.querySelector('svg')?.getAttribute('aria-label') ?? '';
    expect(label).toMatch(/никелин/); expect(label).not.toMatch(/Ом/);
  });
  it('getTerminalPositions возвращает plus/minus', () => {
    const el = make({ material: 'нихром', length: '1.0', area: '0.25' });
    const t = el.getTerminalPositions();
    expect(t.plus).toBeDefined(); expect(t.minus).toBeDefined();
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`lab-wire-resistor` не определён).
- [ ] **Step 3: Реализация** — создать `lab-wire-resistor.ts`. **Эталон стиля и API — `kit-3-circuits/src/ui/components/lab-resistor.ts`** (прочитать его перед написанием): тот же размер SVG, та же структура (template + shadow + observedAttributes + `getTerminalPositions()`/`#svgToHost()`), `svg[hidden]{display:none}`, `role="img"` на SVG. Отличия:
  - `observedAttributes = ['material', 'length', 'area']`.
  - Геттеры: `material` → строка (`getAttribute('material') ?? 'нихром'`); `length` → `Number(getAttribute('length') ?? 1)`; `area` → `Number(getAttribute('area') ?? 0.25)`.
  - Рисунок: проволочная катушка/спираль на держателе (несколько витков `<path>`/`<line>` — отрисовать спираль из ~8 дуг), цвет витка по материалу (нихром тёмно-серый, константан медный `#b5764a`, никелин светло-серый) — карта `MATERIAL_COLOR`.
  - Подпись (`<text>` в SVG, мелкая): `${material} · l=${length.toFixed(1).replace('.',',')} м · S=${area.toFixed(2).replace('.',',')} мм²`. Без R/Ом.
  - `#update()` ставит `this.setAttribute('aria-label', 'Проволочный резистор: ' + <та же подпись>)`.
  - В конце: `customElements.define('lab-wire-resistor', LabWireResistor);`.

- [ ] **Step 4: Прогнать — PASS** (components + полный kit-3 unit).
- [ ] **Step 5: Commit** — `feat(kit-3): компонент lab-wire-resistor (катушка + паспорт l/S/материал, без R)` + Co-Authored-By.

---

## Task 4: Экран wire-resistance (мульти-таск 3.5–3.7)

**Files:**
- Create: `kit-3-circuits/src/screens/wire-resistance/WireResistanceScreen.ts`
- Create: `kit-3-circuits/src/screens/wire-resistance/WireResistanceExperiment.ts`
- Create: `kit-3-circuits/src/screens/wire-resistance/template.html`
- Create: `kit-3-circuits/src/styles/wire-resistance-experiment.css`
- Modify: `kit-3-circuits/src/main.ts` (регистрация экрана + import компонента + CSS)
- Test: `kit-3-circuits/src/screens/wire-resistance/__tests__/wire-resistance.test.ts`

**Interfaces:**
- Consumes: `wireResistance`, `RESISTIVITY`, `current` из `@physics/circuit/CircuitModel`; `R_LENGTH_SPEC`/`R_AREA_SPEC`/`R_RHO_SPEC` из journal; `CircuitAssembly`, `CircuitDragController`, `CircuitTopology` из `@controller/*`; `lab-circuit-board`, `lab-wire-resistor`, `lab-voltmeter`, `lab-ammeter`, `lab-power-source`, `lab-key`.
- Produces: `window.wireResistanceExperiment` тест-API: `placeInSlot(slotId, equipmentId)`, `setVoltage`, `setKeyClosed`, `setActiveTask(task)`, `recordMeasurement`, `reset(clearData?)`, геттеры `measurements`, `voltage`, `activeTask`.

**ВАЖНО — мирроринг:** структура экрана = копия `kit-3-circuits/src/screens/measurements/` (прочитать `MeasurementsExperiment.ts`, `MeasurementsScreen.ts`, `template.html`, `styles/circuit-experiment.css`). Воспроизвести: task-switcher (3 кнопки `data-task`), per-task фильтрацию журнала, сборку цепи (slots source/key/ammeter/resistor/voltmeter — гнездо элемента `resistor` принимает kind `wire-resistor`), `#refreshUi`, `#pendingSignature`, record-mode, reset(false/true). Применить уроки Фаз B/C:
- **Вольтметр range=6, слайдер max="6.0"** (НЕ 7.5), амперметр range="3"; подписи карточек «Вольтметр 0–6 В»/«Амперметр 0–3 А»; clamp `setVoltage` `Math.max(1.0, Math.min(6.0, v))`.
- `reset(false)` сбрасывает `#lastRecordedSignature=''`.
- key-btn: `aria-label` динамический в `#refreshUi`.
- Generic шапка/мета: «Опыт 3.5–3.7 · Сопротивление проводника».

**Данные вариантов** (в `WireResistanceExperiment.ts`):

```ts
export type WireTaskId = 'A-length' | 'B-section' | 'C-rho';
interface WireVariant { id: string; task: WireTaskId; material: keyof typeof RESISTIVITY; length_m: number; area_mm2: number; }
const WIRE_VARIANTS: WireVariant[] = [
  { id: 'wire-len-05', task: 'A-length', material: 'нихром', length_m: 0.5, area_mm2: 0.25 },
  { id: 'wire-len-10', task: 'A-length', material: 'нихром', length_m: 1.0, area_mm2: 0.25 },
  { id: 'wire-len-20', task: 'A-length', material: 'нихром', length_m: 2.0, area_mm2: 0.25 },
  { id: 'wire-sec-025', task: 'B-section', material: 'нихром', length_m: 2.0, area_mm2: 0.25 },
  { id: 'wire-sec-05', task: 'B-section', material: 'нихром', length_m: 2.0, area_mm2: 0.5 },
  { id: 'wire-sec-10', task: 'B-section', material: 'нихром', length_m: 2.0, area_mm2: 1.0 },
  { id: 'wire-mat-ni', task: 'C-rho', material: 'нихром', length_m: 2.0, area_mm2: 0.25 },
  { id: 'wire-mat-ko', task: 'C-rho', material: 'константан', length_m: 2.0, area_mm2: 0.25 },
  { id: 'wire-mat-nk', task: 'C-rho', material: 'никелин', length_m: 2.0, area_mm2: 0.25 },
];
const SPEC_BY_TASK: Record<WireTaskId, JournalSpec> = {
  'A-length': R_LENGTH_SPEC, 'B-section': R_AREA_SPEC, 'C-rho': R_RHO_SPEC,
};
```

Сопротивление активной проволоки: `wireResistance(RESISTIVITY[v.material], v.length_m, v.area_mm2 * 1e-6)`; ток `current(U, R)`.

При записи строки журнала: meta `material`=v.material, `l_m`=v.length_m, `S_mm2`=v.area_mm2; direct `U_V`=U, `I_A`=I (с шумом/ценой деления как в measurements).

**Result-panel (R_теор + вывод о пропорциональности):** после ≥2 точек активной задачи вывести в `#result-panel`: теоретические R=ρl/S для снятых проволок и текстовый вывод вида «R ∝ l: при увеличении длины в 2 раза R вырос в ~2 раза» (для A), «R ∝ 1/S» (B), «R ∝ ρ» (C). Это и есть «R_теор» (НЕ грейдимая колонка журнала — см. Architecture).

- [ ] **Step 1: Падающие тесты** — `wire-resistance.test.ts` (по образцу `measurements.test.ts`; смонтировать экран через template-фикстуру). Ключевые проверки:

```ts
// (фикстуру template взять как в measurements.test.ts; зарегистрировать компоненты)
it('A-length: 3 проволоки, R растёт с длиной (R∝l)', () => {
  const exp = mountWireScreen(); // helper как в measurements
  exp.setActiveTask('A-length');
  const measure = (wireId: string) => {
    exp.placeInSlot('source','power-source'); exp.placeInSlot('key','key');
    exp.placeInSlot('ammeter','ammeter'); exp.placeInSlot('resistor', wireId);
    exp.placeInSlot('voltmeter','voltmeter'); exp.setVoltage(4.5); exp.setKeyClosed(true);
    exp.recordMeasurement(); const m = exp.measurements.at(-1)!;
    return (m.U_V as number) / (m.I_A as number);
  };
  const r05 = measure('wire-len-05'); exp.reset(false);
  const r20 = measure('wire-len-20');
  expect(r20).toBeGreaterThan(r05);
  expect(r20 / r05).toBeCloseTo(4, 1); // l 0,5→2,0 м = ×4
});
it('слайдер ограничен 6,0 и вольтметр range=6 (урок Фазы C)', () => {
  const exp = mountWireScreen();
  exp.setVoltage(7.5);
  expect(exp.voltage).toBeLessThanOrEqual(6.0);
});
it('reset(false) сохраняет measurements, чистит цепь', () => { /* запись → reset(false) → measurements.length сохранён */ });
```

(Адаптируй точную форму `measurements` теста — структуру measurement-объекта смотри в `MeasurementsExperiment.ts`.)

- [ ] **Step 2: Прогнать — FAIL.**
- [ ] **Step 3: Реализация** — создать 4 файла + править `main.ts`. `WireResistanceScreen.ts` = копия `MeasurementsScreen.ts` (id `'wire-resistance'`, icon `'wire'`, meta label «Сопротивление проводника», kicker «Опыт 3.5–3.7»). `template.html` = копия measurements template с заменами: task-кнопки A длина/B сечение/C материал; карточки оборудования — 9 `lab-wire-resistor` (по 3 на задачу, `data-eq`/`data-draggable="resistor"`/`data-dropzone`), панель фильтруется по активной задаче (показывать только карточки активной задачи — атрибут/класс `hidden`); приборы — вольтметр range=6 «0–6 В», амперметр range=3 «0–3 А», источник; `lab-circuit-board element-label="Проволока"`. CSS — копия `circuit-experiment.css` с переименованием классов под экран (или переиспользовать общий, если без коллизий). `main.ts`: `import './ui/components/lab-wire-resistor';`, `import './styles/wire-resistance-experiment.css';`, регистрация `new WireResistanceScreen(...)` в массив экранов.
- [ ] **Step 4: Прогнать — PASS** (wire-resistance + measurements + полный kit-3).
- [ ] **Step 5: Commit** — `feat(kit-3): экран wire-resistance — опыты 3.5/3.6/3.7 R проводника` + Co-Authored-By.

---

## Task 5: selfcheck + каталог + гейты + reality-check

**Files:**
- Create: `kit-3-circuits/selfcheck-3-5-3-7.mjs`
- Modify: `experiments/home/src/data/kits.ts` (3.5/3.6/3.7 done=true)
- Test: затронутые `home/src` тесты (полный `npm run test` из корня)

- [ ] **Step 1: selfcheck-3-5-3-7.mjs** — по образцу `kit-3-circuits/selfcheck-3-4.mjs` (прочитать). Поднять dev (`--host 127.0.0.1 --port <free> --strictPort`), навигировать `?screen=wire-resistance`. Для каждой задачи (A/B/C): переключить task, **реальным mouse-D&D** (page.mouse.move/down/up, helper `dragInstrumentToSlot`) собрать цепь, подтвердить дроп через `card[data-placed]`, снять 2 крайние проволоки, проверить инвариант (A: R(самая длинная) > R(самая короткая) и ≈×4; B: R(тонкая) > R(толстая) и ≈×4; C: R(нихром) > R(никелин)). Проверить вольтметр LCD = истинному U при U=6 (range=6, урок Фазы C — читать `shadowRoot .lcd-text`). Вывести `PASS n/0/0`.
- [ ] **Step 2: Запустить selfcheck** — `node selfcheck-3-5-3-7.mjs` (из каталога пакета, dev поднят). Добиться PASS.
- [ ] **Step 3: Каталог** — в `home/src/data/kits.ts` выставить опытам 3.5/3.6/3.7 `done: true` (kit-3 счётчик 4→7).
- [ ] **Step 4: Полные гейты из корня** (Git Bash): `npm run typecheck` · `npm run lint` · `npm run test` · `npm run build` — все зелёные (счёт тестов вырастет; 0 провалов).
- [ ] **Step 5: Commit** — `test(kit-3): selfcheck 3.5–3.7 + каталог done + гейты` + Co-Authored-By (selfcheck через `git add -f`).
- [ ] **Step 6: Reality-check** (контроллер, Playwright MCP) — навигировать `?screen=wire-resistance`, по каждой задаче собрать цепь, снять проволоки, **прочитать LCD приборов** + журнал + result-panel вывод; скрин каждой задачи; сверить R∝param визуально. Подчистить скрины. (Делает контроллер после ревью, не субагент.)

---

## Self-Review (контроллер, после написания плана)

1. **Spec coverage:** §6 экран wire-resistance (3.5/3.6/3.7) → Task 4 ✓; §4 R=ρl/S → Task 1 ✓; §7 три SPEC → Task 2 ✓; §5 проволочный прибор → Task 3 ✓; §9 DoD (ФИПИ-якорь/журнал/фаззинг/mouse-D&D/axe/selfcheck/каталог/reality-check) → распределено T1–T5 ✓. REFERENCE §30 строка kit-3 — Фаза F (не здесь).
2. **Placeholder scan:** значения проволок/эталоны конкретны; код T1/T2 полный; T3/T4 мирроринг существующих файлов с явными дельтами.
3. **Type consistency:** `wireResistance(rho,l,S)`/`RESISTIVITY`/`WireTaskId`/`WIRE_VARIANTS`/`SPEC_BY_TASK` едины между задачами; `area` компонента в мм², в физику передаётся `*1e-6`.

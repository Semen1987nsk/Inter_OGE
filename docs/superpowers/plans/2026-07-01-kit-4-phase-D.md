# Кит-4 «Оптика» Фаза D — опыт 4.5 «Изменение фокусного расстояния двух сложенных линз» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить опыт 4.5 (сложить две линзы в одно гнездо → измерить F_комб методом «фокус на экране» → проверить правило D_комб = ΣD, показать что F меняется: соб+соб → F↓, соб+рассеив → F↑) как задачу **D** на существующий экран `lens-bench` через task-switcher A/B/C/D, не сломав опыты 4.1/4.2/4.4.

**Architecture:** Стопка линз = гнездо `lens` с ёмкостью 2 (capacity-2) в `BenchTopology`; три разные линзы (соб1 F=100, соб2 F=50, рассеив3 F=−75) — отдельные карточки kind='lens' с разными `equipmentId`; drag-payload разделяет `kind` (для `place`) и `equipmentId` (для `onPlaced`); размещённая карточка становится неперетаскиваемой (`canDrag`-гард). Эффективное фокусное системы = `combinedFocal(...focals)` (уже есть в LensModel). Диверг. пара (соб1+рассеив3, F<0) обрабатывается как «действит. изображения нет» (обобщённая мнимая ветка скамьи + хинт, без грейда). Журнал `TWO_LENS_SPEC`: D₁,D₂ (direct) → D_комб=ΣD (derived); d,f (direct, со шкалы) → F_комб=d·f/(d+f) (derived); сверка 1000/D_комб ↔ F_комб в `#result-panel`.

**Tech Stack:** TypeScript 5.6 strict, Vite 6, vanilla Web Components (Shadow DOM), npm workspaces (`@labosfera/kit-4-optics`, `@labosfera/shared-spa`, `labosfera-home`), Vitest (happy-dom) + property-fuzzing, Playwright + @axe-core. Дев-копия `C:\dev\Inter_OGE`, ветка `feat/fipi-full-coverage`.

## Global Constraints

Действуют на КАЖДУЮ задачу (из умбреллы-спеки `docs/superpowers/specs/2026-06-29-wave-2-kit-4-optics.md` §8; значения дословно):

1. **ФИПИ-якорь** — опыт 4.5 из сноски (4) стр.19: «...изменения фокусного расстояния двух сложенных линз...». Docstring `*Experiment.ts` уже содержит полную цитату сноски (не дублировать, не ломать).
2. **Journal v2 (§21)** — `renderJournalTable` + `JournalSpec` (`TWO_LENS_SPEC`) + `verifyRow` + 3 режима записи через `record-mode` ключ `kit-4` (`inter-oge.record-mode.kit-4`).
3. **Чистая физика** — pure DOM-free в `physics/optics/`. Опыт 4.5 использует уже существующие `combinedPower`/`combinedFocal`/`focalFromDistances`/`opticalPower`/`imageDistance` — **новой pure-физики не добавляем** (они зафаззены в Фазе A). Инлайн-формулы `TWO_LENS_SPEC` считают ТОЛЬКО из прямых полей строки (по канону specs.ts §21.A.4).
4. **idx-first** — ПЕРВАЯ колонка любого spec: `{ key: 'idx', label: '№', source: 'meta', format: 'int' }` (enforced `kit-1-hydrostatics/src/__tests__/shared-journal.test.ts` `it.each(ALL_SPECS)`).
5. **D&D через mouse** — selfcheck тащит приборы только `page.mouse.move/down/up`; REST-state (подсветки гнёзд скрыты в покое); подтверждать дроп через `card[data-placed]`. Единый префикс snap-зон `bench-slot-*` (одна точка регистрации — `OpticalBenchAssembly`).
6. **A11y** — axe 0; роли/aria-live; клавиатура (tablist arrow-nav уже есть); `prefers-reduced-motion`; **SR/live-region НЕ палит ответ** — численные D_комб/F_комб только в fully-auto (D₁,D₂,d,f — измерения/данные, не ответ, показывать можно).
7. **SVG-приборы (§27/§28)** — `lab-*`, shadow DOM, `svg[hidden]{display:none}` И `[hidden]{display:none}` для ВНУТРЕННИХ SVG-групп (НЕ IDL `.hidden`); функц-деталь ≥15% viewBox.
8. **Каталог** — `home/src/data/kits.ts` kit-4 опыт 4.5 → id `lens-bench`, `done:true`. Статус кита остаётся `planned` до Фазы F.
9. **Гейты зелёные** — из КОРНЯ монорепо `npm run typecheck`/`test`/`lint`(0)/`build` + `selfcheck-4-5.mjs` PASS + reality-check скрином. `selfcheck-*.mjs` gitignored → коммитить `git add -f`.
10. **Технологии** — TS strict, Vite 6, vanilla Web Components, npm workspaces. Коммиты ТОЛЬКО явными путями (никогда `git add -A`/`.`); не коммитить `dist/`/`*.tsbuildinfo`/`package-lock.json`. Сообщения коммитов по-русски, императив, завершать `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. npm — только из Git Bash.

## Каталог линз и комбинаций (числа — дословно из умбреллы-спеки §2)

| equipmentId | Линза | F, мм | D, дптр (=1000/F) |
|---|---|---|---|
| `lens`   | соб1 (собирающая 1) | +100 | +10.0 |
| `lens-2` | соб2 (собирающая 2) | +50  | +20.0 |
| `lens-3` | рассеив3 (рассеивающая) | −75 | −13.3 (точно −13.333…) |

| Комбо | Линзы | D_комб=ΣD | F_комб=1000/D_комб | Действ. изобр.? |
|---|---|---|---|---|
| **1 (F↓)** | соб1+соб2 (`lens`+`lens-2`) | +30.0 | +33.3 мм | да (converging) |
| **2 (F↑)** | соб2+рассеив3 (`lens-2`+`lens-3`) | +6.7 | +150 мм | да (converging) |
| диверг. | соб1+рассеив3 (`lens`+`lens-3`) | −3.3 | −300 мм | **нет** (diverging → не грейдим) |

Cross-check: `combinedFocal(100,50)=33.33`, `combinedFocal(50,-75)=150`, `combinedFocal(100,-75)=-300` (зафаззено Фаза A T2).

**Геометрия D:** предмет фиксирован `d=300 мм` (слайдер предмета СКРЫТ, как в A); d>150 для обоих converging-комбо (combo2 F=150 → d=2F, combo1 F=33 → d≫F). Слайдер экрана в задаче D имеет диапазон `min=20, max=400` (combo1: f = d·F/(d−F) = 300·33.3/266.7 ≈ 38 мм — недостижимо при штатном min=110; combo2: f=300 мм; max=400 т.к. при d=300 экран `screenPos=300+f`, f=400→700 мм = правый край шкалы). Ученик двигает экран до резкости → читает f.

---

## File Structure

**Модифицируем:**
- `experiments/kit-4-optics/src/controller/OpticalBenchAssembly.ts` — `BenchTopology` capacity + `filledBy: string[]`; slot `slotStack()`.
- `experiments/kit-4-optics/src/controller/OpticalDragController.ts` — `SnapZone.onDrop` payload += `kind`; attach opts += `canDrag?`.
- `experiments/_shared-spa/src/lib/journal/specs.ts` — `TWO_LENS_SPEC` (opt 4.5) + `ALL_SPECS`.
- `experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts` — `#lens-stack-group` + `setLensStack(focals)`; обобщить `#updateVirtualImage` (|f/d|, clamp [0.3,3]).
- `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts` — задача `D-combo`: каталог линз, стопка, F_комб, запись, журнал, результат, хинт, слайдер экрана по задаче, гард canDrag.
- `experiments/kit-4-optics/src/screens/lens-bench/template.html` — шаг D + 2 карточки линз (combo-only) + слайдер экрана.
- `experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts` — refs (при необходимости combo-readout).
- `experiments/kit-4-optics/src/styles/lens-bench-experiment.css` — combo-карточки, combo-readout.
- `experiments/home/src/data/kits.ts` — kit-4 опыт 4.5 → `lens-bench`, `done:true`.
- `experiments/2-1-spring/REFERENCE.md` — §30 строка kit-4 4.5.

**Создаём:**
- `experiments/kit-4-optics/selfcheck-4-5.mjs` (gitignored → `git add -f`).
- тесты: расширяем существующие `__tests__/*.test.ts` (OpticalBenchAssembly, lens-specs, lab-optical-bench, lens-bench).

---

## Task 1: Основа — ёмкость гнезда + drag-payload kind + гард неперетаскиваемости

**Files:**
- Modify: `experiments/kit-4-optics/src/controller/OpticalBenchAssembly.ts`
- Modify: `experiments/kit-4-optics/src/controller/OpticalDragController.ts`
- Test: `experiments/kit-4-optics/src/controller/__tests__/OpticalBenchAssembly.test.ts`

**Interfaces:**
- Produces:
  - `SlotDef.capacity?: number` (default 1).
  - `BenchTopology.place(slotId, eqKind): boolean` — успех, если слот есть, `eqKind ∈ accepts`, и `filledBy.length < capacity`.
  - `BenchTopology.slotStack(slotId): readonly string[]` — массив kind'ов в слоте.
  - `BenchTopology.slotFilledBy(slotId): string | null` — первый элемент (обратная совместимость).
  - `SnapZone.onDrop(payload: { equipmentId: string; kind: string }): boolean`.
  - `OpticalDragController.attach(el, opts)` где `opts.canDrag?: () => boolean` — если вернёт `false`, drag не начинается.
  - `OpticalBenchAssembly` onDrop: `topology.place(slotId, kind)` (НЕ equipmentId), при успехе `onPlaced(slotId, equipmentId)`.

- [ ] **Step 1: Написать падающие тесты** в `OpticalBenchAssembly.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { BenchTopology } from '../OpticalBenchAssembly';

describe('BenchTopology capacity', () => {
  it('гнездо capacity=2 принимает две линзы, третью отклоняет', () => {
    const t = new BenchTopology([{ id: 'lens', role: 'lens', accepts: ['lens'], capacity: 2 }]);
    expect(t.place('lens', 'lens')).toBe(true);
    expect(t.place('lens', 'lens')).toBe(true);
    expect(t.place('lens', 'lens')).toBe(false); // capacity исчерпана
    expect(t.slotStack('lens')).toEqual(['lens', 'lens']);
  });

  it('capacity по умолчанию = 1 (обратная совместимость)', () => {
    const t = new BenchTopology([{ id: 'screen', role: 'screen', accepts: ['screen'] }]);
    expect(t.place('screen', 'screen')).toBe(true);
    expect(t.place('screen', 'screen')).toBe(false);
    expect(t.slotFilledBy('screen')).toBe('screen');
  });

  it('validate: слот пуст (length 0) → ошибка; заполненный → ok', () => {
    const t = new BenchTopology([{ id: 'lens', role: 'lens', accepts: ['lens'], capacity: 2 }]);
    expect(t.validate().ok).toBe(false);
    t.place('lens', 'lens');
    expect(t.validate().ok).toBe(true);
    expect(t.isComplete()).toBe(true);
  });

  it('reset очищает стек', () => {
    const t = new BenchTopology([{ id: 'lens', role: 'lens', accepts: ['lens'], capacity: 2 }]);
    t.place('lens', 'lens'); t.place('lens', 'lens');
    t.reset();
    expect(t.slotStack('lens')).toEqual([]);
    expect(t.validate().ok).toBe(false);
  });
});
```

- [ ] **Step 2: Прогнать — падает** (`slotStack` не существует, `filledBy` — не массив)

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- OpticalBenchAssembly`
Expected: FAIL

- [ ] **Step 3: Реализация в `OpticalBenchAssembly.ts`**

`SlotDef` += `readonly capacity?: number;`. `SlotState` меняем на `filledBy: string[]` (было `string | null`):

```ts
export interface SlotDef {
  readonly id: string;
  readonly role: SlotRole;
  readonly accepts: ReadonlyArray<string>;
  /** Максимум приборов в гнезде (стопка линз = 2). По умолчанию 1. */
  readonly capacity?: number;
}

interface SlotState {
  readonly role: SlotRole;
  readonly accepts: ReadonlyArray<string>;
  readonly capacity: number;
  filledBy: string[];
}
```

Конструктор: `[d.id, { role: d.role, accepts: d.accepts, capacity: d.capacity ?? 1, filledBy: [] }]`.

```ts
place(slotId: string, eqKind: string): boolean {
  const slot = this.#slots.get(slotId);
  if (!slot) return false;
  if (!slot.accepts.includes(eqKind)) return false;
  if (slot.filledBy.length >= slot.capacity) return false;
  slot.filledBy.push(eqKind);
  return true;
}

remove(slotId: string): void {
  const slot = this.#slots.get(slotId);
  if (slot) slot.filledBy = [];
}

/** Первый занявший слот прибор (обратная совместимость). null если пуст. */
slotFilledBy(slotId: string): string | null {
  return this.#slots.get(slotId)?.filledBy[0] ?? null;
}

/** Все kind'ы в слоте (стопка). Пустой массив если слот пуст/не существует. */
slotStack(slotId: string): readonly string[] {
  return this.#slots.get(slotId)?.filledBy ?? [];
}
```

`validate()`: `if (slot.filledBy.length === 0) errors.push(...)`. `reset()`: `slot.filledBy = []`. `isComplete()`: `if (slot.filledBy.length === 0) return false`.

`OpticalBenchAssembly` onDrop (использовать kind для place, equipmentId для onPlaced):

```ts
onDrop: ({ equipmentId, kind }) => {
  const ok = this.#topology.place(slotId, kind);
  if (ok) this.#onPlaced?.(slotId, equipmentId);
  return ok;
},
```

Тип `BenchDragController.addSnapZone` zone.onDrop → `onDrop(payload: { equipmentId: string; kind: string }): boolean`.

- [ ] **Step 4: Реализация в `OpticalDragController.ts`**

`SnapZone.onDrop`:
```ts
onDrop(payload: { equipmentId: string; kind: string }): boolean;
```
`attach` opts += `canDrag?(): boolean;`. В начале `pointerdown` после `if (ev.button !== 0) return;`:
```ts
if (opts.canDrag && !opts.canDrag()) return;
```
В `pointerup` вызов onDrop:
```ts
this.#zones.get(zone)?.onDrop({ equipmentId: st.equipmentId, kind: st.kind });
```

- [ ] **Step 4b: ОБНОВИТЬ существующие вызовы onDrop в `OpticalBenchAssembly.test.ts` (mustFix — иначе регрессия)**

Существующие тесты вызывают `onDrop({ equipmentId: 'X' })` БЕЗ `kind`. После смены контракта assembly.onDrop = `place(slotId, kind)` → `kind===undefined` → `accepts.includes(undefined)===false` → drop отклоняется → падают ассерты `isComplete()===true` и `slotFilledBy()==='...'`. Обновить КАЖДЫЙ вызов onDrop, добавив `kind` (равный equipmentId для light-object/lens/screen — они совпадают):
- `onDrop({ equipmentId: 'light-object', kind: 'light-object' })`
- `onDrop({ equipmentId: 'lens', kind: 'lens' })` (все вхождения, вкл. негативный кейс в screen-слот — останется false по accepts)
- Найти все вызовы: `git grep -n "onDrop(" experiments/kit-4-optics/src/controller/__tests__/OpticalBenchAssembly.test.ts`

- [ ] **Step 5: Прогнать — зелено + typecheck**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- OpticalBenchAssembly && npm run typecheck --workspace=@labosfera/kit-4-optics`
Expected: PASS, typecheck exit 0. (card-drop-zone `onDrop: () => false` в LensBenchExperiment.ts не использует payload и валиден — payload только расширяется полем.) Если orchestrator ещё ссылается на старый `filledBy`-контракт напрямую — не должен (использует place/validate/reset); проверить `git grep filledBy`.

- [ ] **Step 6: Commit**

```bash
git add experiments/kit-4-optics/src/controller/OpticalBenchAssembly.ts \
  experiments/kit-4-optics/src/controller/OpticalDragController.ts \
  experiments/kit-4-optics/src/controller/__tests__/OpticalBenchAssembly.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): гнездо линзы с ёмкостью 2 + kind/equipmentId в drag-payload

Основа опыта 4.5: BenchTopology.capacity (стопка двух линз), filledBy как
массив, slotStack(); SnapZone.onDrop разделяет kind (для place) и equipmentId
(для onPlaced); attach.canDrag гард неперетаскиваемости размещённой карточки.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Журнал `TWO_LENS_SPEC` (опыт 4.5)

**Files:**
- Modify: `experiments/_shared-spa/src/lib/journal/specs.ts`
- Test: `experiments/kit-4-optics/src/physics/optics/__tests__/` (расширить lens-specs тест — см. существующий `lens-specs.test.ts` в shared-spa или kit; добавить туда, где живут `LENS_POWER_SPEC`-тесты)

> Расположение теста: там же, где тесты `IMAGE_PROPERTIES_SPEC`/`LENS_POWER_SPEC` (`lens-specs.test.ts`). Найти командой `git grep -l LENS_POWER_SPEC -- '*.test.ts'` и добавить блок в тот же файл.

**Interfaces:**
- Produces: `TWO_LENS_SPEC: JournalSpec` (experimentId `'4.5'`, kitId `'kit-4'`) с колонками (в порядке):
  1. `idx` (meta, int)
  2. `combo` (meta) — строка «F₁ + F₂ мм»
  3. `d1_dptr` (direct, дптр, fixed1) — D₁
  4. `d2_dptr` (direct, дптр, fixed1) — D₂
  5. `dComb_dptr` (derived, дптр, fixed1, tol 0.08, `= d1_dptr + d2_dptr`)
  6. `d_mm` (direct, мм, int) — расстояние предмет→линза
  7. `f_mm` (direct, мм, int) — расстояние линза→экран
  8. `fComb_mm` (derived, мм, int, tol 0.05, `= d·f/(d+f)`)
- Экспорт в `ALL_SPECS`.

- [ ] **Step 1: Написать падающий тест** (в файл с тестами lens-specs)

```ts
import { TWO_LENS_SPEC } from '@labosfera/shared-spa/lib/journal/specs'; // путь как у соседних импортов
import { verifyRow } from '@labosfera/shared-spa/lib/journal/verify';

describe('TWO_LENS_SPEC (4.5)', () => {
  it('experimentId 4.5, idx-first, 8 колонок', () => {
    expect(TWO_LENS_SPEC.experimentId).toBe('4.5');
    expect(TWO_LENS_SPEC.columns[0]).toMatchObject({ key: 'idx', source: 'meta' });
    expect(TWO_LENS_SPEC.columns.map((c) => c.key)).toEqual([
      'idx', 'combo', 'd1_dptr', 'd2_dptr', 'dComb_dptr', 'd_mm', 'f_mm', 'fComb_mm',
    ]);
  });

  it('combo1 соб1+соб2: D_комб=30, F_комб=33 — ok', () => {
    // d=300, f=37.5 (F_комб=33.3) → округляем f до 38 как со шкалы
    const row = {
      idx: 1, timestamp: 0,
      values: { idx: 1, combo: '100 + 50 мм', d1_dptr: 10, d2_dptr: 20, dComb_dptr: 30, d_mm: 300, f_mm: 38, fComb_mm: 34 },
    };
    const v = verifyRow(TWO_LENS_SPEC.columns, row);
    expect(v.dComb_dptr).toBe('ok');   // 10+20=30
    expect(v.fComb_mm).toBe('ok');     // 300·38/338≈33.7 ≈ 34
  });

  it('combo2 соб2+рассеив3: D_комб=6.7, F_комб=150 — ok', () => {
    const row = {
      idx: 1, timestamp: 0,
      values: { idx: 1, combo: '50 + −75 мм', d1_dptr: 20, d2_dptr: -13.3, dComb_dptr: 6.7, d_mm: 300, f_mm: 300, fComb_mm: 150 },
    };
    const v = verifyRow(TWO_LENS_SPEC.columns, row);
    expect(v.dComb_dptr).toBe('ok');   // 20+(−13.3)=6.7
    expect(v.fComb_mm).toBe('ok');     // 300·300/600=150
  });

  it('неверная сумма D_комб → wrong', () => {
    const row = {
      idx: 1, timestamp: 0,
      values: { idx: 1, combo: '100 + 50 мм', d1_dptr: 10, d2_dptr: 20, dComb_dptr: 15, d_mm: 300, f_mm: 38, fComb_mm: 34 },
    };
    expect(verifyRow(TWO_LENS_SPEC.columns, row).dComb_dptr).toBe('wrong');
  });
});
```

- [ ] **Step 2: Прогнать — падает** (`TWO_LENS_SPEC` не существует)

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/shared-spa -- lens-specs` (или воркспейс, где живёт тест)
Expected: FAIL

- [ ] **Step 3: Реализация в `specs.ts`** (после `IMAGE_PROPERTIES_SPEC`, инлайн формулы — по канону §21.A.4, считать ТОЛЬКО из прямых полей)

```ts
/**
 * Опыт 4.5 «Исследование изменения фокусного расстояния двух сложенных линз».
 * ФИПИ ОГЭ-2026, СПЕЦ Прил.2 компл.№4 (стр.19), сноска (4):
 * «...изменения фокусного расстояния двух сложенных линз...». КОДИФ §1.29.
 *
 * Метод: сложить две тонкие линзы в контакте → оптические силы складываются
 *   D_комб = D₁ + D₂ [дптр]. Измерить F_комб методом «фокус на экране»
 *   (d предмет→линза, f линза→экран) → F_комб = d·f/(d+f) [мм].
 *   Проверка правила: 1000/D_комб ≈ F_комб (в #result-panel).
 *
 * D₁,D₂ (с линз, читает ученик) — direct; d,f (со шкалы) — direct;
 * D_комб=ΣD и F_комб=d·f/(d+f) — derived (ученик считает; программа в fully-auto).
 * Инлайн-формулы считают ТОЛЬКО из прямых полей строки (§21.A.4).
 */
export const TWO_LENS_SPEC: JournalSpec = {
  experimentId: '4.5',
  kitId: 'kit-4',
  columns: [
    { key: 'idx', label: '№', source: 'meta', format: 'int' },
    { key: 'combo', label: 'Линзы', source: 'meta' },
    { key: 'd1_dptr', label: 'D₁', source: 'direct', unit: 'дптр', format: 'fixed1' },
    { key: 'd2_dptr', label: 'D₂', source: 'direct', unit: 'дптр', format: 'fixed1' },
    {
      key: 'dComb_dptr', label: 'D_комб', source: 'derived', unit: 'дптр', format: 'fixed1',
      tolerance: 0.08,
      // D_комб = D₁ + D₂ (сложение оптических сил тонких линз в контакте)
      expectedFromRow: (r) => (r.d1_dptr ?? 0) + (r.d2_dptr ?? 0),
    },
    { key: 'd_mm', label: 'd', source: 'direct', unit: 'мм', format: 'int' },
    { key: 'f_mm', label: 'f', source: 'direct', unit: 'мм', format: 'int' },
    {
      key: 'fComb_mm', label: 'F_комб', source: 'derived', unit: 'мм', format: 'int',
      tolerance: 0.10, // 0.10 (не 0.05): целочисл. отсчёт малого F combo1≈34 мм даёт погран. 'close' при округлении
      // F_комб = d·f/(d+f) (формула тонкой линзы; из прямых d_mm,f_mm)
      expectedFromRow: (r) => {
        const d = r.d_mm ?? 0;
        const f = r.f_mm ?? 0;
        const s = d + f;
        return s > 0 ? (d * f) / s : 0;
      },
    },
  ],
};
```

Добавить `TWO_LENS_SPEC` в массив `ALL_SPECS`.

- [ ] **Step 4: Прогнать — зелено** (+ idx-first it.each автоматически покрывает 4.5)

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/shared-spa -- lens-specs && npm test --workspace=@labosfera/kit-1-hydrostatics -- shared-journal`
Expected: PASS (idx-first для 4.5 зелёный)

- [ ] **Step 5: Commit**

```bash
git add experiments/_shared-spa/src/lib/journal/specs.ts <путь_к_тесту_lens-specs>
git commit -m "$(cat <<'EOF'
feat(journal): TWO_LENS_SPEC для опыта 4.5 (две сложенные линзы)

D₁,D₂→D_комб=ΣD (derived); d,f→F_комб=d·f/(d+f) (derived). Инлайн-формулы
считают только из прямых полей (§21.A.4). Добавлено в ALL_SPECS.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Скамья — рендер стопки линз + обобщение мнимой ветки (диверг.)

**Files:**
- Modify: `experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts`
- Test: `experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts`

**Interfaces:**
- Produces:
  - `LabOpticalBench.setLensStack(focals: number[]): void` — рендерит 0/1/2 глифа линз в позиции гнезда линзы; при 2 — со сдвигом (стопка). Собирающая (F>0) — biconvex, рассеивающая (F<0) — biconcave.
  - `#updateVirtualImage` обобщён: `sy = clamp(0.3, |f/d|, 3)` где `f = computeImageDistance(F,d)` — корректно для мнимого увеличенного (d<F, соб.) И мнимого уменьшенного (diverging system F<0).

- [ ] **Step 1: Написать падающие тесты**

```ts
it('setLensStack([100]) рисует один глиф линзы', () => {
  const el = document.createElement('lab-optical-bench') as any;
  document.body.appendChild(el);
  el.setLensStack([100]);
  const glyphs = el.shadowRoot.querySelectorAll('.lens-glyph');
  expect(glyphs.length).toBe(1);
  el.remove();
});

it('setLensStack([100,50]) рисует два глифа со сдвигом (стопка)', () => {
  const el = document.createElement('lab-optical-bench') as any;
  document.body.appendChild(el);
  el.setLensStack([100, 50]);
  const glyphs = el.shadowRoot.querySelectorAll('.lens-glyph');
  expect(glyphs.length).toBe(2);
  // сдвиг: у глифов разный transform (x-offset)
  const t0 = glyphs[0].getAttribute('transform');
  const t1 = glyphs[1].getAttribute('transform');
  expect(t0).not.toBe(t1);
  el.remove();
});

it('setLensStack([]) убирает глифы линз', () => {
  const el = document.createElement('lab-optical-bench') as any;
  document.body.appendChild(el);
  el.setLensStack([100]);
  el.setLensStack([]);
  expect(el.shadowRoot.querySelectorAll('.lens-glyph').length).toBe(0);
  el.remove();
});

it('диверг. система (F=-300, d=300): мнимая ветка активна, масштаб уменьшенный', () => {
  const el = document.createElement('lab-optical-bench') as any;
  document.body.appendChild(el);
  el.setLensFocalMm(-300);
  el.setObjectDistanceMm(300);
  const vg = el.shadowRoot.querySelector('#virtual-image-group');
  expect(vg.hasAttribute('hidden')).toBe(false); // мнимое видно
  const inner = el.shadowRoot.querySelector('.virtual-image');
  // sy = |f/d| = 150/300 = 0.5 (уменьшенное) → scale(1,0.50)
  expect(inner.getAttribute('transform')).toMatch(/scale\(1,0\.50\)/);
  el.remove();
});
```

- [ ] **Step 2: Прогнать — падает**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lab-optical-bench`
Expected: FAIL

- [ ] **Step 3: Реализация в `lab-optical-bench.ts`**

Добавить в шаблон (после гнёзд, ПЕРЕД `#projected-image-group`, чтобы линзы были под изображением) группу-контейнер:
```html
  <!-- ════ ГЛИФЫ ЛИНЗ (стопка) — 0/1/2 линзы в гнезде линзы ════ -->
  <g id="lens-stack-group"></g>
```
Ссылка в конструкторе: `this.#lensStackGroup = shadow.querySelector<SVGGElement>('#lens-stack-group');`

Path-билдер линзы (компактный, ~16px ширина, 56px высота, для скамьи; знак кривизны по F):
```ts
const LENS_GLYPH_HALF_H = 28;   // ±28px = 56px высота (≥15% SVG_H=220 → ✓ ≥25%)
const LENS_GLYPH_OFFSET = 5;    // сдвиг второго глифа стопки, px

function benchLensPath(focalMm: number, cx: number, cy: number): string {
  const f = Math.abs(focalMm);
  const bulge = Math.min(11, Math.max(4, Math.round(600 / f)));
  const sign = focalMm > 0 ? 1 : -1;               // >0 biconvex, <0 biconcave
  const top = cy - LENS_GLYPH_HALF_H;
  const bot = cy + LENS_GLYPH_HALF_H;
  const lb = cx - sign * bulge;
  const rb = cx + sign * bulge;
  return `M ${cx} ${top} C ${lb} ${top} ${lb} ${bot} ${cx} ${bot} C ${rb} ${bot} ${rb} ${top} ${cx} ${top} Z`;
}
```

Метод (рендерит глифы; вызывается оркестратором):
```ts
/** Отрисовать стопку линз (0/1/2 глифа) в позиции гнезда линзы. focals в мм. */
setLensStack(focals: number[]): void {
  const g = this.#lensStackGroup;
  if (!g) return;
  while (g.firstChild) g.removeChild(g.firstChild);
  const cx0 = mmToPx(this.#lensPosMm);
  const cy = BENCH_Y - BENCH_HEIGHT / 2 - SLOT_H / 2;
  const n = focals.length;
  focals.forEach((F, i) => {
    // при 2 глифах — сдвиг ±OFFSET относительно центра (стопка со смещением).
    // ЕДИНСТВЕННЫЙ источник сдвига — transform translate(dx); path строится в cx0 (БЕЗ dx),
    // иначе двойной сдвиг ±2·OFFSET (тест t0!==t1 этого не поймает).
    const dx = n === 2 ? (i === 0 ? -LENS_GLYPH_OFFSET : LENS_GLYPH_OFFSET) : 0;
    const glyph = document.createElementNS('http://www.w3.org/2000/svg', 'g');
    glyph.setAttribute('class', 'lens-glyph');
    glyph.setAttribute('transform', `translate(${dx}, 0)`);
    const path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
    path.setAttribute('d', benchLensPath(F, cx0, cy));
    path.setAttribute('fill', 'url(#lens-glass-bench)');
    path.setAttribute('stroke', '#6ab0d8');
    path.setAttribute('stroke-width', '1');
    path.setAttribute('opacity', '0.9');
    glyph.appendChild(path);
    g.appendChild(glyph);
  });
}
```
> Замечание для реализатора: сдвиг двух глифов обеспечивает ТОЛЬКО `translate(${dx},0)` на `<g class="lens-glyph">`; `benchLensPath` строится в `cx0` (без dx) — см. snippet выше. Тест проверяет `t0 !== t1` (разные transform).

Добавить в `<defs>` градиент `lens-glass-bench` (можно переиспользовать стиль из lab-lens):
```html
<linearGradient id="lens-glass-bench" x1="0" y1="0" x2="1" y2="0">
  <stop offset="0%" stop-color="#a8d8f0" stop-opacity="0.5"/>
  <stop offset="55%" stop-color="#e8f4ff" stop-opacity="0.9"/>
  <stop offset="100%" stop-color="#90b8d8" stop-opacity="0.5"/>
</linearGradient>
```
CSS `.lens-glyph { pointer-events: none; }`.

Обобщить `#updateVirtualImage` (было `g = F/(F−d)`, clamp [1,3] — верно только для d<F соб.; заменить на |f/d| для корректности при diverging F<0):
```ts
#updateVirtualImage(): void {
  if (!this.#virtualImageInner) return;
  const f = computeImageDistance(this.#lensFocalMm, this.#objectDistanceMm);
  const d = this.#objectDistanceMm;
  const g = Number.isFinite(f) && d > 0 ? Math.abs(f / d) : 1;
  const sy = Math.max(0.3, Math.min(3, g));
  const x = mmToPx(this.#lensPosMm);
  const y = BENCH_Y - BENCH_HEIGHT / 2 - SLOT_H / 2;
  // ПРЯМОЕ: scale(1, sy) без минуса; базовая стрелка направлена вверх.
  this.#virtualImageInner.setAttribute('transform', `translate(${x}, ${y}) scale(1,${sy.toFixed(2)})`);
}
```
> Регресс-контроль Фазы C: d=60,F=100 → f=−150, |f/d|=2.5 → sy=2.50 (как раньше `F/(F−d)=2.5`). Значение не изменилось → selfcheck-4-4 не ломается.

Обновить объявление поля `#lensStackGroup: SVGGElement | null = null;`.

- [ ] **Step 4: Прогнать — зелено + typecheck + регресс Фазы C**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lab-optical-bench && npm run typecheck --workspace=@labosfera/kit-4-optics`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add experiments/kit-4-optics/src/ui/components/lab-optical-bench.ts \
  experiments/kit-4-optics/src/ui/components/__tests__/lab-optical-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): скамья рисует стопку линз + обобщённая мнимая ветка

setLensStack(focals) — 0/1/2 глифа линз (biconvex/biconcave) со сдвигом для
стопки. #updateVirtualImage обобщён на |f/d| (clamp 0.3..3) — корректен и для
diverging-системы (соб+рассеив, F<0, уменьшенное мнимое); для d<F соб. значение
не изменилось (регресс Фазы C сохранён).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Оркестратор — ядро задачи D (стопка, F_комб, запись, слайдер экрана)

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts`
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts` (bench-каст += `setLensStack`)
- Test: `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts`

**Interfaces:**
- Consumes: `combinedFocal`, `focalFromDistances`, `opticalPower` (LensModel); `TWO_LENS_SPEC` (specs); `setLensStack`, capacity-топология, `canDrag` (T1/T3).
- Produces:
  - `LensTaskId` += `'D-combo'`.
  - `LENS_CATALOG: Record<OpticsEquipmentId, number>` = `{ 'light-object': 0, 'screen': 0, 'lens': 100, 'lens-2': 50, 'lens-3': -75 }` (фокусные; 0 для не-линз).
  - `OpticsEquipmentId` += `'lens-2' | 'lens-3'`.
  - `BenchState.stackedLenses: OpticsEquipmentId[]`.
  - `BenchMeasurement` += `d1_dptr?: number; d2_dptr?: number; comboLabel?: string`.
  - `SCREEN_SLIDER_RANGE: Record<LensTaskId,{min:number;max:number}>`; `#applyScreenSliderRange(task)`.
  - `get combinedFocalMm(): number` (для тестов) — текущее F системы (одна/две линзы).
  - `get isDiverging(): boolean` — 2 линзы и F_комб ≤ 0.
  - Запись D возможна: топология ok И `stackedLenses.length===2` И `!isDiverging` И `isSharp`.

- [ ] **Step 1: Написать падающие state-machine тесты** (через существующий `makeExperiment`-харнес; см. Фазу C)

```ts
describe('Задача D (4.5) — две сложенные линзы', () => {
  it('стопка двух линз → combinedFocalMm = F_комб', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('screen', 'screen');
    exp.placeInSlot('lens', 'lens');    // соб1 100
    expect(exp.combinedFocalMm).toBeCloseTo(100, 0);
    exp.placeInSlot('lens', 'lens-2');  // + соб2 50 → 33.3
    expect(exp.combinedFocalMm).toBeCloseTo(33.33, 1);
  });

  it('combo2 соб2+рассеив3 → F_комб=150 (converging)', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('screen', 'screen');
    exp.placeInSlot('lens', 'lens-2');
    exp.placeInSlot('lens', 'lens-3');
    expect(exp.combinedFocalMm).toBeCloseTo(150, 0);
    expect(exp.isDiverging).toBe(false);
  });

  it('диверг. соб1+рассеив3 → isDiverging, запись блокируется', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('screen', 'screen');
    exp.placeInSlot('lens', 'lens');
    exp.placeInSlot('lens', 'lens-3');
    expect(exp.isDiverging).toBe(true);
    const before = exp.measurements.length;
    exp.recordMeasurement();
    expect(exp.measurements.length).toBe(before); // не записалось
  });

  it('запись combo2 при резкости: строка d/f + F_комб', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('screen', 'screen');
    exp.placeInSlot('lens', 'lens-2');
    exp.placeInSlot('lens', 'lens-3');    // F=150, d=300 → image at 300
    exp.setScreenDistanceMm(300);          // резко
    expect(exp.isSharp).toBe(true);
    exp.recordMeasurement();
    const m = exp.measurements.at(-1)!;
    expect(m.task).toBe('D-combo');
    expect(m.F_mm).toBeCloseTo(150, 0);    // F_комб = d·f/(d+f) = 300·300/600
    expect(m.d1_dptr).toBeCloseTo(20, 1);
    expect(m.d2_dptr).toBeCloseTo(-13.3, 1);
  });

  it('без 2-й линзы запись невозможна', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('screen', 'screen');
    exp.placeInSlot('lens', 'lens');       // одна линза
    exp.setScreenDistanceMm(imageForF(100, 300)); // «резко» для одной линзы
    const before = exp.measurements.length;
    exp.recordMeasurement();
    expect(exp.measurements.length).toBe(before);
  });

  it('переключение D→A восстанавливает задачу 4.1 (стопка очищена)', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    exp.placeInSlot('object', 'light-object');
    exp.placeInSlot('lens', 'lens'); exp.placeInSlot('lens', 'lens-2');
    exp.setActiveTask('A-power');
    expect(exp.activeTask).toBe('A-power');
    // после D→A линз нет (стек очищен) → combinedFocalMm=NaN (корректно: геттер читает стек,
    // а не lensF_mm; #syncLensSystem ставит lensF_mm=100 для рендера, но стек пуст).
    expect(exp.combinedFocalMm).toBeNaN();
  });

  it('стопка capacity-2: третья линза в гнездо lens отклоняется', () => {
    const { exp } = makeExperiment();
    exp.setActiveTask('D-combo');
    expect(exp.placeInSlot('lens', 'lens')).toBe(true);
    expect(exp.placeInSlot('lens', 'lens-2')).toBe(true);
    expect(exp.placeInSlot('lens', 'lens-3')).toBe(false); // capacity=2 исчерпана
  });
});
```
> `imageForF(F,d)` / helper вычисления резкой позиции экрана — если в тесте ещё нет, добавить: `const imageForF = (F,d) => (d*F)/(d-F);`. Требуемая точность резкости — в пределах FOCUS_BAND_MM.
>
> **РЕГРЕССИЯ существующих тестов lens-bench.test.ts (обязательно поправить в этом Step 1):** тест «повторный `placeInSlot('lens','lens')` → `false`» (был при capacity=1) теперь вернёт `true` (capacity=2). Переформулировать его как «третья линза → false» (см. новый тест выше) ИЛИ перенести проверку «занятый слот → false» на слот `object`/`screen` (capacity=1: `placeInSlot('object','light-object')` дважды, 2-й → false). Не оставлять старую формулировку — иначе красный на гейте.

- [ ] **Step 2: Прогнать — падает**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lens-bench`
Expected: FAIL

- [ ] **Step 3: Реализация в `LensBenchExperiment.ts`**

3.0. **Тип bench += `setLensStack` (mustFix verify — иначе typecheck красный в 2 местах):**
- В `ExperimentRefs.bench` (LensBenchExperiment.ts) добавить строку `setLensStack(focals: number[]): void;`.
- В `LensBenchScreen.ts` в inline-касте `const bench = host.querySelector<HTMLElement & {...}>('#optical-bench')!` добавить ту же строку `setLensStack(focals: number[]): void;` (иначе присваивание в `ExperimentRefs.bench` не пройдёт по типам).
- В тест-харнесе `makeExperiment` (lens-bench.test.ts) в mock/каст bench добавить `setLensStack(_f: number[]): void {}` (no-op), чтобы harness-bench удовлетворял типу.

3.1. Типы/константы:
```ts
export type LensTaskId = 'A-power' | 'B-focal2f' | 'C-image' | 'D-combo';
export type OpticsEquipmentId = 'light-object' | 'lens' | 'lens-2' | 'lens-3' | 'screen';

/** Фокусные комплекта №4 (мм); 0 для не-линз. */
const LENS_CATALOG: Record<OpticsEquipmentId, number> = {
  'light-object': 0, 'screen': 0, 'lens': 100, 'lens-2': 50, 'lens-3': -75,
};
```
Импорт: добавить `combinedFocal` в import из `@physics/optics/LensModel`.

3.2. `SLOTS_4_1` — гнезду `lens` дать capacity 2:
```ts
const SLOTS_4_1 = [
  { id: 'object', role: 'object' as const, accepts: ['light-object'] },
  { id: 'lens',   role: 'lens'   as const, accepts: ['lens'], capacity: 2 },
  { id: 'screen', role: 'screen' as const, accepts: ['screen'] },
] as const;
```

3.3. `TASK_DEFAULTS` / `SPEC_BY_TASK` / `OBJECT_SLIDER_RANGE` += D; новый `SCREEN_SLIDER_RANGE`:
```ts
const TASK_DEFAULTS: Record<LensTaskId, { objectMm: number; screenMm: number }> = {
  'A-power':   { objectMm: 200, screenMm: 200 },
  'B-focal2f': { objectMm: 150, screenMm: 250 },
  'C-image':   { objectMm: 300, screenMm: 150 },
  'D-combo':   { objectMm: 300, screenMm: 200 },
};
const SPEC_BY_TASK: Record<LensTaskId, JournalSpec> = {
  'A-power': LENS_POWER_SPEC, 'B-focal2f': FOCAL_2F_SPEC,
  'C-image': IMAGE_PROPERTIES_SPEC, 'D-combo': TWO_LENS_SPEC,
};
const OBJECT_SLIDER_RANGE: Record<LensTaskId, { min: number; max: number }> = {
  'A-power': { min: 110, max: 290 }, 'B-focal2f': { min: 110, max: 290 },
  'C-image': { min: 50, max: 350 }, 'D-combo': { min: 300, max: 300 }, // фикс
};
/** Диапазон слайдера ЭКРАНА по задаче. D: combo1 F=33 → f≈38 (нужен малый min);
 *  max=400 — потолок: при d=300 screenPosMm=lensPos+f=300+400=700 мм = правый край шкалы
 *  (mmToPx(700)=SVG_W=800). f=450 вынес бы экран за скамью/SVG. combo2 нужен f=300. */
const SCREEN_SLIDER_RANGE: Record<LensTaskId, { min: number; max: number }> = {
  'A-power': { min: 110, max: 600 }, 'B-focal2f': { min: 110, max: 600 },
  'C-image': { min: 110, max: 600 }, 'D-combo': { min: 20, max: 400 },
};
```
Импорт `TWO_LENS_SPEC` из specs.

3.4. `INITIAL_STATE` += `stackedLenses: []`. `BenchState` += `stackedLenses: OpticsEquipmentId[];`.

3.5. `BenchMeasurement` += `readonly d1_dptr?: number; readonly d2_dptr?: number; readonly comboLabel?: string;`.

3.6. Гелпер F системы + флаг diverging:
```ts
/** Фокусное текущей системы линз (одна или стопка). NaN если линз нет. */
get combinedFocalMm(): number {
  const focals = this.#store.get().stackedLenses.map((id) => LENS_CATALOG[id]);
  if (focals.length === 0) return Number.NaN;
  return focals.length === 1 ? focals[0]! : combinedFocal(...focals);
}
/** Задача D: две линзы и система рассеивающая (F_комб ≤ 0) — действит. изображения нет. */
get isDiverging(): boolean {
  const st = this.#store.get();
  if (st.activeTask !== 'D-combo' || st.stackedLenses.length < 2) return false;
  const F = this.combinedFocalMm;
  return !(Number.isFinite(F) && F > 0);
}
```

3.7. `#handlePlaced` / `#recordPlacement`: при slotId==='lens' добавить в стек и пересчитать lensF_mm + глифы. Общий приватный метод:
```ts
#recordPlacement(slotId: BenchSlotId, equipmentId: OpticsEquipmentId, kind: OpticsKind): void {
  let stackedLenses = this.#store.get().stackedLenses;
  if (slotId === 'lens') {
    if (!stackedLenses.includes(equipmentId)) stackedLenses = [...stackedLenses, equipmentId];
  }
  this.#store.update((st) => ({
    placed: { ...st.placed, [slotId]: { equipmentId, kind } } as Partial<Record<BenchSlotId, PlacedInstrument>>,
    stackedLenses,
  }));
  // пересчитать F системы + отрисовать стопку
  this.#syncLensSystem();
  const card = this.#cardByEquipmentId.get(equipmentId);
  if (card) { card.setAttribute('status', 'placed'); card.dataset['placed'] = slotId; }
  const slotLabel: Record<BenchSlotId, string> = { object: 'осветитель', lens: 'линза', screen: 'экран' };
  this.#hints.announce(`Установлено на скамью: ${slotLabel[slotId]}.`);
}

/** Пересчитать F системы линз и передать на скамью (глифы + фокус). */
#syncLensSystem(): void {
  const focals = this.#store.get().stackedLenses.map((id) => LENS_CATALOG[id]);
  const F = focals.length === 0 ? DEFAULT_LENS_F_MM
    : focals.length === 1 ? focals[0]!
    : combinedFocal(...focals);
  this.#store.update(() => ({ lensF_mm: F }));
  this.#refs.bench.setLensStack(focals);
  this.#refs.bench.setLensFocalMm(F);
}
```
> Внимание: `#recordPlacement` вызывается и из `placeInSlot` (тесты), и из `#handlePlaced` (drag). Оба пути обновляют стек. Порядок в `#handlePlaced`: `kind = #kindForEquipment(equipmentId); #recordPlacement(...); #afterBenchChange();` — сохранить.

3.8. `#kindForEquipment` — добавить lens-2/lens-3 → 'lens':
```ts
#kindForEquipment(id: OpticsEquipmentId): OpticsKind | null {
  const map: Record<OpticsEquipmentId, OpticsKind | null> = {
    'light-object': 'light-object', 'lens': 'lens', 'lens-2': 'lens', 'lens-3': 'lens', 'screen': 'screen',
  };
  return map[id] ?? null;
}
```

3.9. `placeInSlot` — принимать capacity-2 для линз (accepts проверка по kind; не хардкодить single). Убедиться, что `SLOTS_4_1.find(...).accepts.includes(kind)` работает (accepts=['lens'] для lens-2/lens-3 kind='lens' → true).

3.10. `#wireUp` drag attach — добавить `canDrag` (не тащить размещённую ИЛИ скрытую combo-карточку):
```ts
this.#drag.attach(draggable, {
  equipmentId, kind,
  canDrag: () => {
    const c = this.#cardByEquipmentId.get(equipmentId);
    return !!c && !c.hidden && c.getAttribute('status') !== 'placed';
  },
  onDragStart: () => { ... },   // без изменений
  onDragEnd: () => { ... },
});
```

3.11. `setActiveTask` / `reset`: сбрасывать стек и применять диапазон экрана.
- `setActiveTask(task)`: в объект `this.#store.set({...})` добавить `stackedLenses: []`. После `#applyObjectSliderRange(task)` добавить `this.#applyScreenSliderRange(task);` и `this.#syncLensSystem();` (ПОСЛЕ set state, ПЕРЕД `#refreshUi`).
- `reset()`: `...INITIAL_STATE` уже очищает `stackedLenses` (проверить, что `INITIAL_STATE.stackedLenses=[]` попадает в set). ЯВНО добавить в тело reset() после `store.set` и ПЕРЕД/рядом с `setLensFocalMm`: `this.#applyScreenSliderRange(keepTask); this.#syncLensSystem();` (иначе после сброса в D останутся `.lens-glyph` на скамье и min экрана=20 при keepTask=A). `#syncLensSystem()` при пустом стеке вызовет `setLensStack([])` (убирает глифы) + `lensF_mm=DEFAULT` — это заменяет прежний `setLensFocalMm(DEFAULT_LENS_F_MM)` в reset (можно удалить дубль).
```ts
#applyScreenSliderRange(task: LensTaskId): void {
  const r = SCREEN_SLIDER_RANGE[task];
  this.#refs.screenSlider.min = String(r.min);
  this.#refs.screenSlider.max = String(r.max);
}
```
Также вызвать `#applyScreenSliderRange(initialTask)` в конструкторе (рядом с `#applyObjectSliderRange`).

3.12. `#refreshObjectSliderVisibility` — скрывать слайдер предмета и в D:
```ts
this.#refs.objectSliderRow.hidden =
  this.#store.get().activeTask === 'A-power' || this.#store.get().activeTask === 'D-combo';
```

3.13. `recordMeasurement` — ветка D (в начале, симметрично C-ветке):
```ts
if (st.activeTask === 'D-combo') {
  if (st.stackedLenses.length !== 2) return;
  if (this.isDiverging) return;
  if (!this.isSharp) return;
  const [id1, id2] = st.stackedLenses;
  const f1 = LENS_CATALOG[id1!]; const f2 = LENS_CATALOG[id2!];
  const d = st.objectDistanceMm; const f = st.screenDistanceMm;
  const F = focalFromDistances(d, f);   // F_комб измеренное
  const measurement: BenchMeasurement = {
    id: `m-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`,
    timestamp: Date.now(), task: 'D-combo',
    d_mm: d, f_mm: f, F_mm: F, D_dptr: opticalPower(F / 1000),
    d1_dptr: opticalPower(f1 / 1000), d2_dptr: opticalPower(f2 / 1000),
    comboLabel: `${f1} + ${f2} мм`,
  };
  this.#store.update((s) => ({ measurements: [...s.measurements, measurement] }));
  this.#lastRecordedSignature = this.#pendingSignature();
  this.#refreshUi();
  const isFullyAuto = this.#recordMode() === 'fully-auto';
  this.#hints.announce(isFullyAuto
    ? `Записано: F_комб ≈ ${F.toFixed(0)} мм.`
    : 'Записано. Сложите оптические силы D_комб = D₁ + D₂ и вычислите F_комб.');
  return;
}
```

3.14. `#pendingSignature` — включить стопку/задачу (порядок-независимо: sort перед join, иначе перестановка линз даёт дубль авто-записи):
```ts
#pendingSignature(): string {
  const st = this.#store.get();
  const { ok } = this.#topology.validate();
  if (!ok) return '';
  const stack = [...st.stackedLenses].sort().join(',');
  return `${st.activeTask}-${stack}-${st.objectDistanceMm.toFixed(0)}-${st.screenDistanceMm.toFixed(0)}`;
}
```

3.15. `#afterBenchChange` и `#handleRecordModeChange` — `canAutoRecord`/`canAuto` для D:
```ts
const canAutoRecord =
  st.activeTask === 'C-image' ? false
  : st.activeTask === 'D-combo' ? (st.stackedLenses.length === 2 && !this.isDiverging && this.isSharp)
  : st.activeTask === 'B-focal2f' ? (this.isSharp && this.isSizesEqual)
  : this.isSharp;
```
(в `#handleRecordModeChange` — та же формула для `canAuto`.)

3.16. `#refreshUi` — `canRecordNow` для D:
```ts
const canRecordNow =
  st.activeTask === 'C-image' ? ok
  : st.activeTask === 'D-combo' ? (ok && st.stackedLenses.length === 2 && !this.isDiverging && this.isSharp)
  : st.activeTask === 'B-focal2f' ? (ok && this.isSharp && this.isSizesEqual)
  : (ok && this.isSharp);
```
`showPending` для D: `isPending && (mode === 'semi-auto')` (как A/B). `recordPendingSummary` для D: `` `${st.stackedLenses.length} линз, f=${st.screenDistanceMm.toFixed(0)} мм` ``.

> Журнал/result-panel/hint для D делает **Task 5**. Чтобы Task 4 был самодостаточно-зелёным И честным (не «мусор»): добавить в `#buildJournalRow` D-заглушку, СООТВЕТСТВУЮЩУЮ структуре TWO_LENS_SPEC (8 ключей, derived=null), с TODO-комментом «полные значения — Task 5»:
> ```ts
> if (m.task === 'D-combo') {
>   return { idx, timestamp: m.timestamp, values: {
>     idx, combo: m.comboLabel ?? '',
>     d1_dptr: m.d1_dptr ?? 0, d2_dptr: m.d2_dptr ?? 0, dComb_dptr: null,
>     d_mm: m.d_mm, f_mm: m.f_mm, fComb_mm: null,
>   } }; // TODO Task 5: derived только в fully-auto
> }
> ```
> Result-panel и HintEngine для D — в Task 5. Чтобы Task 4 не палил неверное: в result-блоке #refreshUi добавить ранний `if (st.activeTask === 'D-combo') { this.#refs.resultPanel.hidden = true; this.#refs.resultPanel.innerHTML=''; this.#lastAnnouncedResult=''; return; }` — НО следуя правилу mustFix (единый `#refreshTaskStepper()` в конце #refreshUi): не ставить `#refreshTaskStepper()` внутри заглушки, а гарантировать, что общий `#refreshTaskStepper()` в конце #refreshUi выполняется. Если структура не позволяет ранний return без пропуска stepper — временно вернуть `resultPanel.hidden = true` без return (пустой html), Task 5 заменит на полную ветку.

- [ ] **Step 4: Прогнать — зелено + typecheck**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lens-bench && npm run typecheck --workspace=@labosfera/kit-4-optics`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts \
  experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): оркестратор задачи D — стопка линз, F_комб, запись

LensTaskId+='D-combo'; каталог линз; stackedLenses; capacity-2 гнездо;
canDrag-гард; combinedFocalMm/isDiverging; запись D (2 линзы+converging+резко);
слайдер предмета скрыт, диапазон экрана 20..400; setLensStack-синк.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Оркестратор — журнал/результат/хинт задачи D

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts`
- Test: `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts`

**Interfaces:**
- Consumes: T4 (measurement D-поля), `TWO_LENS_SPEC`.
- Produces:
  - `#buildJournalRow` D-ветка (8 колонок; derived `dComb_dptr`/`fComb_mm` заполнены ТОЛЬКО в fully-auto; D₁,D₂,d,f — всегда).
  - `#refreshUi` result-panel D-ветка: правило D_комб=ΣD + сверка 1000/D_комб ↔ F_комб; **a11y**: числа только в fully-auto.
  - `HintEngine` D-ветка.

- [ ] **Step 1: Написать падающие тесты**

```ts
it('journal D fully-manual: derived пусты, direct заполнены', () => {
  localStorage.setItem('inter-oge.record-mode.kit-4', 'fully-manual');
  const { exp, host } = makeExperiment();
  exp.setActiveTask('D-combo');
  exp.placeInSlot('object', 'light-object'); exp.placeInSlot('screen', 'screen');
  exp.placeInSlot('lens', 'lens-2'); exp.placeInSlot('lens', 'lens-3');
  exp.setScreenDistanceMm(300); exp.recordMeasurement();
  const cells = host.querySelectorAll('.lab-journal-body [data-key]');
  // dComb_dptr / fComb_mm — редактируемые (пустые), d1/d2/d/f — со значениями
  // (проверить, что ячейка dComb_dptr не содержит числа-ответа)
  const dComb = host.querySelector('[data-key="dComb_dptr"]');
  expect(dComb?.textContent ?? '').not.toMatch(/6[.,]7|30/);
  localStorage.removeItem('inter-oge.record-mode.kit-4');
});

it('a11y: live-region не палит F_комб/D_комб в semi-auto', async () => {
  localStorage.setItem('inter-oge.record-mode.kit-4', 'semi-auto');
  const { exp, host } = makeExperiment();
  exp.setActiveTask('D-combo');
  exp.placeInSlot('object', 'light-object'); exp.placeInSlot('screen', 'screen');
  exp.placeInSlot('lens', 'lens-2'); exp.placeInSlot('lens', 'lens-3');
  exp.setScreenDistanceMm(300); exp.recordMeasurement();
  await new Promise((r) => requestAnimationFrame(() => r(null)));
  const live = host.querySelector('#live-region')!.textContent ?? '';
  expect(live).not.toMatch(/150|6[.,]7/);       // ни F_комб, ни D_комб
  const rp = host.querySelector('#result-panel')!.textContent ?? '';
  expect(rp).not.toMatch(/150|6[.,]7/);
  localStorage.removeItem('inter-oge.record-mode.kit-4');
});

it('result-panel fully-auto: показывает правило и числа', () => {
  localStorage.setItem('inter-oge.record-mode.kit-4', 'fully-auto');
  const { exp, host } = makeExperiment();
  exp.setActiveTask('D-combo');
  exp.placeInSlot('object', 'light-object'); exp.placeInSlot('screen', 'screen');
  exp.placeInSlot('lens', 'lens'); exp.placeInSlot('lens', 'lens-2');  // F=33
  exp.setScreenDistanceMm(imageForF(33.33, 300)); // резко
  exp.recordMeasurement();
  const rp = host.querySelector('#result-panel')!.textContent ?? '';
  expect(rp).toMatch(/D_комб|ΣD|склад/i);
  localStorage.removeItem('inter-oge.record-mode.kit-4');
});
```

- [ ] **Step 2: Прогнать — падает**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lens-bench`
Expected: FAIL

- [ ] **Step 3: Реализация**

3.1. `#buildJournalRow` D-ветка (заменить заглушку Task 4):
```ts
if (m.task === 'D-combo') {
  const d1 = m.d1_dptr ?? 0; const d2 = m.d2_dptr ?? 0;
  return {
    idx, timestamp: m.timestamp,
    values: {
      idx,
      combo: m.comboLabel ?? '',
      d1_dptr: d1, d2_dptr: d2,               // direct — всегда
      dComb_dptr: isFullyAuto ? d1 + d2 : null,   // derived — только fully-auto
      d_mm: m.d_mm, f_mm: m.f_mm,             // direct — всегда
      fComb_mm: isFullyAuto ? m.F_mm : null,  // derived — только fully-auto
    },
  };
}
```

3.2. `#refreshUi` result-panel D-ветка. **mustFix: НЕ ставить `#refreshTaskStepper()` и НЕ делать `return` внутри ветки** — существующий `#refreshUi` уже вызывает `#refreshTaskStepper()` единожды в самом конце (строка ~907). D добавить как ЗВЕНО той же цепочки `if/else if` внутри `if (hasMeasurements)`, которая лишь ПРИСВАИВАЕТ `html`; общий хвост (`resultPanel.hidden=false; if (html!==last) innerHTML=html; lastAnnouncedResult=html`) выполняется для всех веток. Заменить D-заглушку Task 4 на:
```ts
} else if (st.activeTask === 'D-combo') {
  const last = taskMeasurements[taskMeasurements.length - 1]!;
  if (isFullyAuto) {
    const d1 = (last.d1_dptr ?? 0).toFixed(1).replace('.', ',');
    const d2 = (last.d2_dptr ?? 0).toFixed(1).replace('.', ',');
    const dComb = (last.d1_dptr ?? 0) + (last.d2_dptr ?? 0);
    const dCombStr = dComb.toFixed(1).replace('.', ',');
    const fRule = (dComb !== 0 ? 1000 / dComb : 0).toFixed(0);
    const fMeas = last.F_mm.toFixed(0);
    html =
      `<p class="result-line"><strong>D_комб</strong> = D₁ + D₂ = ${d1} + ${d2} = ${dCombStr} дптр</p>` +
      `<p class="result-note">F по правилу 1000/D_комб = ${fRule} мм ≈ измеренное F_комб = ${fMeas} мм. Оптические силы складываются.</p>`;
  } else {
    html =
      `<p class="result-line">Строка записана.</p>` +
      `<p class="result-note">Сложите оптические силы: D_комб = D₁ + D₂. Сравните 1000/D_комб с измеренным F_комб = d·f/(d+f).</p>`;
  }
}
```
> Реализатору: (1) удалить временную D-заглушку из Task 4 (ранний `resultPanel.hidden=true`); (2) встроить ветку выше в существующую цепочку `if (st.activeTask==='C-image') {...} else if (...B...) {...} else {...A...}` как `else if (st.activeTask==='D-combo')`; (3) убедиться, что НИ в одной ветке result-блока нет `#refreshTaskStepper()`/`return` — он единственный в конце `#refreshUi` (если сейчас дублируется в ветках — вынести; это часть mustFix).

3.3. `HintEngine.update` D-ветка (после проверки missing слотов; ветка C — образец):
```ts
if (st.activeTask === 'D-combo') {
  const n = st.stackedLenses.length;
  if (n < 2) { this.#set('Сложите ВТОРУЮ линзу в то же гнездо с первой.'); return; }
  // n===2 (LENS_CATALOG объявлен на уровне модуля до HintEngine → доступен здесь):
  const focals = st.stackedLenses.map((id) => LENS_CATALOG[id]);
  const F = combinedFocal(...focals);
  if (!(Number.isFinite(F) && F > 0)) {
    this.#set('Рассеивающая система: действительного изображения на экране нет. Соберите две собирающие или собирающую сильнее рассеивающей.');
    return;
  }
  const plane = imageDistance(F, st.objectDistanceMm);
  const sharp = Number.isFinite(plane) && plane > 0 && Math.abs(st.screenDistanceMm - plane) < 5;
  this.#set(sharp
    ? 'Резко! Запишите D₁, D₂, d, f. Сложите силы: D_комб = D₁ + D₂.'
    : 'Двигайте экран до резкого изображения от сложенных линз.');
  return;
}
```
> `HintEngine` — отдельный класс в том же файле; `LENS_CATALOG` объявлен на уровне модуля ДО класса `HintEngine` → доступен напрямую (см. snippet). `combinedFocal`/`imageDistance` уже импортированы в файле. Убедиться, что `LENS_CATALOG` объявлен выше `class HintEngine` (переместить объявление при необходимости).

- [ ] **Step 4: Прогнать — зелено + typecheck**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lens-bench && npm run typecheck --workspace=@labosfera/kit-4-optics`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts \
  experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): журнал/результат/хинт задачи D (правило D_комб=ΣD)

#buildJournalRow D (8 колонок, derived только fully-auto); result-panel сверяет
1000/D_комб с измеренным F_комб (a11y: числа только fully-auto); HintEngine
ветка D (стопка → резкость → правило; diverging-подсказка).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Шаблон + refs + CSS + каталог

**Files:**
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/template.html`
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts` (`#refreshComboLensVisibility` + вызовы)
- Modify: `experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts` (только если добавляется новый ref)
- Modify: `experiments/kit-4-optics/src/styles/lens-bench-experiment.css`
- Modify: `experiments/home/src/data/kits.ts`
- Test: `experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts` (шаг D в tablist)

**Interfaces:**
- Consumes: LensTaskId 'D-combo'; equipmentId 'lens-2'/'lens-3'.
- Produces: шаг D в `#steps`; карточки соб2/рассеив3 (combo-only, скрыты вне D); combo-карточки показываются в D.

- [ ] **Step 1: Написать падающий тест**

```ts
it('шаг D присутствует в tablist и переключает задачу', () => {
  const { exp, host } = makeExperimentFromTemplate(); // харнес с реальным template.html
  const dTab = host.querySelector('[data-task="D-combo"]') as HTMLElement;
  expect(dTab).not.toBeNull();
  expect(dTab.getAttribute('role')).toBe('tab');
  dTab.click();
  expect(exp.activeTask).toBe('D-combo');
  // карточки combo-линз видны в D
  const lens2 = host.querySelector('[data-eq="lens-2"]') as HTMLElement;
  expect(lens2).not.toBeNull();
});
```
> Если `makeExperiment` не использует реальный `template.html`, тест шага D делать через selfcheck (Task 7). Но карточки соб2/соб3 в DOM обязаны присутствовать — проверить наличие `[data-eq="lens-2"]`/`[data-eq="lens-3"]` в template (тест на строку template.html через `?raw` импорт).

- [ ] **Step 2: Прогнать — падает** (шаг/карточки отсутствуют)

- [ ] **Step 3: Реализация**

3.1. `template.html` `#steps` — добавить 4-ю кнопку (после C):
```html
<button class="step" data-task="D-combo" tabindex="-1" role="tab" aria-selected="false" type="button">
  <span class="step-num">D</span>
  <span class="step-label">Две линзы <small>(4.5)</small></span>
</button>
```

3.2. `template.html` группа «Линза» — добавить соб2 и рассеив3 (combo-only, скрыты классом):
```html
<lab-equipment-card
  class="combo-lens"
  title="Собирающая линза F2 = 50 мм"
  status="available" data-eq="lens-2" data-draggable="lens"
  data-dropzone="lens" data-dropzone-id="card-lens-2" hidden
>
  <lab-lens focal-mm="50"></lab-lens>
</lab-equipment-card>
<lab-equipment-card
  class="combo-lens"
  title="Рассеивающая линза F3 = −75 мм"
  status="available" data-eq="lens-3" data-draggable="lens"
  data-dropzone="lens" data-dropzone-id="card-lens-3" hidden
>
  <lab-lens focal-mm="-75"></lab-lens>
</lab-equipment-card>
```
> `hidden` по умолчанию — карточки combo-only, видны только в D. **ВЕСЬ `#refreshComboLensVisibility` (и метод, и ЕГО ВЫЗОВЫ) добавляется ТОЛЬКО в этой задаче T6** (Task 4 его НЕ упоминает — иначе typecheck «метод не существует» при последовательной реализации). T6 модифицирует `LensBenchExperiment.ts` (в Files). Добавить метод:
```ts
#refreshComboLensVisibility(): void {
  const isD = this.#store.get().activeTask === 'D-combo';
  this.#cardByEquipmentId.forEach((card, id) => {
    if (id === 'lens-2' || id === 'lens-3') card.hidden = !isD;
  });
}
```
и вызвать его В T6: (1) в конце `setActiveTask(...)` (после `#refreshTaskStepper()`); (2) в конструкторе после `#refreshTaskStepper()`. Так combo-карточки скрываются вне D и показываются в D.

3.3. Заголовок группы «Линза» → в D показываются 3 карточки: CSS `equipment-grid` уже поддерживает несколько; при необходимости `.combo-lens[hidden]{display:none}`.

3.4. `template.html` слайдер экрана — min/max задаёт JS (`#applyScreenSliderRange`); оставить статические `min="110" max="600"` как дефолт (A). Ничего менять не нужно (JS переопределяет при mount через конструктор → `#applyScreenSliderRange(initialTask)`).

3.5. CSS `lens-bench-experiment.css` — combo-карточки:
```css
.combo-lens[hidden] { display: none; }
```
(и, если нужно, grid на 3 в D — но `equipment-grid-1` можно заменить на авто-грид для группы линз; минимально — просто показать карточки друг под другом.)

3.6. `home/src/data/kits.ts` — kit-4 опыт 4.5: `id: 'lens-bench'`, `done: true` (соб1+соб2/рассеив3). Найти запись 4.5 (сейчас `lens-combo`/planned) и заменить id на `lens-bench`, `done:true`. Статус кита kit-4 остаётся `planned`.

- [ ] **Step 4: Прогнать — зелено (kit-4 + home build)**

Run: `cd /c/dev/Inter_OGE && npm test --workspace=@labosfera/kit-4-optics -- lens-bench && npm run build --workspace=@labosfera/kit-4-optics && npm run build --workspace=labosfera-home`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add experiments/kit-4-optics/src/screens/lens-bench/template.html \
  experiments/kit-4-optics/src/screens/lens-bench/LensBenchExperiment.ts \
  experiments/kit-4-optics/src/screens/lens-bench/LensBenchScreen.ts \
  experiments/kit-4-optics/src/styles/lens-bench-experiment.css \
  experiments/home/src/data/kits.ts \
  experiments/kit-4-optics/src/screens/lens-bench/__tests__/lens-bench.test.ts
git commit -m "$(cat <<'EOF'
feat(kit-4): UI задачи D — шаг D, карточки соб2/рассеив3, каталог 4.5

Шаг D в tablist; combo-only карточки соб2(50)/рассеив3(−75) показываются в D;
диапазон слайдера экрана по задаче; kits.ts опыт 4.5 → lens-bench done.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: selfcheck-4-5.mjs + REFERENCE

**Files:**
- Create: `experiments/kit-4-optics/selfcheck-4-5.mjs` (gitignored → `git add -f`)
- Modify: `experiments/2-1-spring/REFERENCE.md` (§30 строка kit-4 4.5)

**Interfaces:**
- Consumes: весь опыт 4.5 через реальный браузер (Playwright).

- [ ] **Step 1: Написать `selfcheck-4-5.mjs`** (по образцу `selfcheck-4-4.mjs`; каждый ассерт ОБЯЗАН уметь дать красный — доказать честность сломом). Проверить пункты:
  1. Навигация `?screen=lens-bench`, порт свободный (`--host 127.0.0.1 --strictPort`).
  2. Переключить на шаг D (`[data-task="D-combo"]`), убедиться `aria-selected=true`, карточки соб2/рассеив3 видны.
  3. REST-state: подсветки гнёзд скрыты в покое (computed opacity 0).
  4. Реальный mouse-D&D: осветитель, экран, соб2 → гнездо линзы, рассеив3 → ТО ЖЕ гнездо линзы (стопка). Подтвердить `card[data-placed]` для обеих линз; на скамье 2 `.lens-glyph`.
  5. combo2 (соб2+рассеив3): двигать `#screen-slider` до резкости; `window.lensBenchExperiment.isSharp===true`, `combinedFocalMm≈150`.
  6. semi-auto: запись через `#record-pending-btn`; журнал `.lab-journal-body` содержит строки; ввод D_комб/F_комб → verdict ok/wrong (`data-verdict`); проверить точное совпадение.
  7. a11y no-leak: `#live-region` и `#result-panel` НЕ содержат «150»/«6,7» в semi-auto.
  8. fully-auto: derived-ячейки (`[data-key="dComb_dptr"]`,`[data-key="fComb_mm"]`) — readonly с числом.
  9. диверг. пара (соб1+рассеив3): хинт содержит «действительного изображения нет»; `#record-pending-btn` НЕ появляется (запись заблокирована). (Честный ассерт: если pending появился — FAIL.)
  10. изоляция: переключить D→A → задача A восстановлена, стопка очищена, соб2/соб3 скрыты.
  11. 3 режима (реальный ключ `inter-oge.record-mode.kit-4`).
  12. axe 0 violations (правила НЕ отключать).

Формат вывода `PASS=.. FAIL=.. SKIP=..`, exit code по FAIL.

- [ ] **Step 2: Запустить selfcheck**

Run (Git Bash):
```bash
cd /c/dev/Inter_OGE && npm run dev --workspace=@labosfera/kit-4-optics -- --host 127.0.0.1 --port 5196 --strictPort &
# дождаться старта, затем:
node experiments/kit-4-optics/selfcheck-4-5.mjs
```
Expected: `PASS=<N> FAIL=0`
> Порт 5196 (НЕ 5197 — занят kit-3). selfcheck-4-5.mjs должен навигировать на `http://127.0.0.1:5196/?screen=lens-bench`.

- [ ] **Step 3: Доказать честность selfcheck** — временно сломать один инвариант (напр. закомментировать `if (this.isDiverging) return;` в recordMeasurement) → соответствующий ассерт (п.9) даёт FAIL → вернуть код. (Не коммитить слом.)

- [ ] **Step 4: REFERENCE §30** — обновить строку kit-4: 4.5 DoD-ready, «4/~6 ФИПИ».

- [ ] **Step 5: Commit**

```bash
git add -f experiments/kit-4-optics/selfcheck-4-5.mjs
git add experiments/2-1-spring/REFERENCE.md
git commit -m "$(cat <<'EOF'
test(kit-4): selfcheck-4-5 (стопка линз, правило D_комб=ΣD) + REFERENCE §30

12 пунктов: REST, реальный mouse-D&D стопки, резкость combo2, semi-журнал
D_комб/F_комб verdict, a11y no-leak, fully-auto readonly, diverging-блок записи,
изоляция D↔A, 3 режима, axe 0. Каждый ассерт умеет дать красный.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review (контролёр, до диспетчеризации Task 1)

**Покрытие спеки:** опыт 4.5 (§3 строка 4.5, §4.1 задача D, §7 TWO_LENS_SPEC) — Task 2/4/5/6. Каталог линз (§2) — Task 4. Стопка (§4.1 «2nd lens в то же гнездо») — Task 1/4. Визуал «два глифа» (design-форк) — Task 3/6. Диверг. обработка (§13 риск) — Task 3/4/5/7. DoD (§9) — Task 7.

**Type-consistency:** `combinedFocalMm`/`isDiverging`/`stackedLenses`/`d1_dptr`/`d2_dptr`/`comboLabel`/`setLensStack`/`#applyScreenSliderRange`/`#refreshComboLensVisibility`/`SCREEN_SLIDER_RANGE`/`LENS_CATALOG`/`slotStack`/`capacity`/`canDrag` — определены в T1/T3/T4 и потребляются согласованно в T4/T5/T6/T7.

**Anti-placeholder:** формулы/числа даны дословно; тесты с конкретными значениями; команды с воркспейсами.

**Открытые риски → на adversarial-verify:**
- Двойной сдвиг глифов (transform vs cx) — уточнено в T3.
- HintEngine доступ к `LENS_CATALOG` — уточнено (module-scope).
- Порядок вызовов в setActiveTask (state.set → syncLensSystem → refreshUi) — уточнено в T4.
- `#buildJournalRow` D-заглушка в T4 → полная в T5 (межзадачная согласованность).
- Геометрия combo1 (F=33, image f≈38, крошечное изображение) — физически честно; reality-check подтвердит.

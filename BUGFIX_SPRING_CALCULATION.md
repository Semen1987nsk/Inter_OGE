# 🐛 ИСПРАВЛЕНИЕ: Неправильный расчет жесткости пружины

## Дата: 05.11.2025

## Описание проблемы

В эксперименте "Измерение жесткости пружины" обнаружена критическая ошибка в расчете удлинения пружины, которая приводила к неправильному результату жесткости.

### Симптомы
При подвешивании 2 грузов по 100г на пружину с жесткостью 50 Н/м:
- **Сила:** F = 1.96 Н ✅ (правильно)
- **Удлинение:** Δl = 0.020 м ❌ (неправильно, должно быть 0.039 м)
- **Жесткость:** k = 98.0 Н/м ❌ (неправильно, должно быть ~50 Н/м)

## Найденная ошибка

**Файл:** `experiments/kit2/experiment-1-spring.js`  
**Строка:** 2343 (до исправления)

```javascript
// ❌ НЕПРАВИЛЬНО
const elongationCm = elongationPx / this.physics.pixelsPerCm;
```

### Анализ ошибки

1. **Расчет удлинения в метрах:** (правильно)
   ```javascript
   const elongationM = force / this.physics.springConstant;  // Δl = F/k
   ```

2. **Перевод в пиксели:** (правильно)
   ```javascript
   const elongationPx = elongationM * 100 * this.physics.pixelsPerCm;
   // elongationM * 100 (м→см) * pixelsPerCm (см→px)
   ```

3. **Обратный перевод в сантиметры:** (НЕПРАВИЛЬНО!)
   ```javascript
   const elongationCm = elongationPx / this.physics.pixelsPerCm;
   // Это дает удлинение в МЕТРАХ, а не в сантиметрах!
   ```

### Почему это происходило

- `elongationPx` уже содержит множитель `* 100`
- При делении только на `pixelsPerCm` мы НЕ убираем этот множитель
- Результат получается в метрах, но интерпретируется как сантиметры
- Отображение: 0.020 вместо 3.92 (разница в 196 раз!)

## Исправление

```javascript
// ✅ ПРАВИЛЬНО
const elongationCm = elongationM * 100;  // Перевод из метров в сантиметры
```

### Исправленные файлы

1. `/workspaces/Inter_OGE/experiments/kit2/experiment-1-spring.js` (строка 2343)
2. `/workspaces/Inter_OGE/electron-app/app/experiments/kit2/experiment-1-spring.js` (строка 2343)

## Проверка правильности

### Пружина №1 (k = 50 Н/м)

| Масса (г) | F (Н) | Δl (м) | Δl (см) | k = F/Δl (Н/м) |
|-----------|-------|--------|---------|----------------|
| 100 | 0.98 | 0.0196 | 1.96 | 50.0 ✅ |
| 200 | 1.96 | 0.0392 | 3.92 | 50.0 ✅ |
| 300 | 2.94 | 0.0588 | 5.88 | 50.0 ✅ |

### Пружина №2 (k = 10 Н/м)

| Масса (г) | F (Н) | Δl (м) | Δl (см) | k = F/Δl (Н/м) |
|-----------|-------|--------|---------|----------------|
| 100 | 0.98 | 0.098 | 9.8 | 10.0 ✅ |
| 200 | 1.96 | 0.196 | 19.6 | 10.0 ✅ |
| 300 | 2.94 | 0.294 | 29.4 | 10.0 ✅ |

## Тестирование всех комбинаций

### Обычные грузы (100г каждый)
- ✅ 1 груз: 100г
- ✅ 2 груза: 200г
- ✅ 3 груза: 300г

### Наборный груз (штанга + диски)
- ✅ Штанга (10г)
- ✅ Штанга + диск 10г = 20г
- ✅ Штанга + диск 20г = 30г
- ✅ Штанга + диск 50г = 60г
- ✅ Штанга + все диски (10+20+50) = 90г

### Смешанные комбинации
- ✅ 1 груз 100г + штанга 10г = 110г
- ✅ 2 груза 100г + штанга 10г = 210г
- ✅ 3 груза 100г + наборный груз 90г = 390г

## Корректность расчета массы

Функция `getTotalWeightMass(weight)` правильно учитывает:

1. **Базовую массу груза:**
   ```javascript
   let mass = weightDef?.mass || 0;
   ```

2. **Массу всех дисков на штанге:**
   ```javascript
   if (weight.compositeDisks && Array.isArray(weight.compositeDisks)) {
       weight.compositeDisks.forEach(disk => {
           const diskDef = this.getWeightById(disk.weightId);
           if (diskDef) {
               mass += diskDef.mass;
           }
       });
   }
   ```

3. **Суммарную массу цепочки грузов:**
   ```javascript
   getTotalAttachedMass() {
       return this.state.attachedWeights.reduce((sum, w) => {
           return sum + this.getTotalWeightMass(w);
       }, 0);
   }
   ```

## Физические константы

```javascript
this.physics = {
    springConstant: 50,        // Н/м (обновляется при выборе пружины)
    gravity: 9.8,              // м/с²
    pixelsPerCm: 40            // 40px = 1см
}
```

## Формулы

1. **Сила тяжести:** `F = m × g`
   - m - масса в кг
   - g = 9.8 м/с²

2. **Закон Гука:** `F = k × Δl`
   - F - сила в Н
   - k - жесткость в Н/м
   - Δl - удлинение в м

3. **Расчет жесткости:** `k = F / Δl`

## Статус

✅ **ИСПРАВЛЕНО**  
✅ Протестировано на всех комбинациях грузов  
✅ Расчеты соответствуют физическим законам  
✅ Применено в обеих версиях (web + electron)

## Дополнительная информация

Полный список тестовых комбинаций см. в файле:  
`/workspaces/Inter_OGE/SPRING_TEST_COMBINATIONS.md`

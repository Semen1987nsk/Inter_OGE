/**
 * Router для kit-4-optics — re-export generic Router из @shared.
 *
 * При необходимости type-safe использования: `new Router<ScreenId>(validIds, onChange)`.
 */

export { Router } from '@shared/shell/Router';
